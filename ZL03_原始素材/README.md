# ZL03 · 原始素材

本地大体积素材架。**空架子进版本库**（本 README）；截图、录屏、图标导出**不入库**。

## 目录

| 路径 | 装什么 | 是否入库 |
|---|---|---|
| `README.md`（本文件） | 说明 | 是 |
| `相关展示截图/` | 商店截图、录屏等 | 否（`.gitignore`） |
| `icon素材/` | 图标导出的 png / svg 等工作稿 | 否（`.gitignore`） |

正式送审用的截图定稿在：

`ApiRelay/specs/playbooks/v1-key-vault/store-drafts/`

## 图标改哪边

| 路径 | 角色 |
|---|---|
| **`ApiRelay/icon/`** | **权威**。工程用的 `ApiRelay.icon`，改 App 图标改这里 |
| `ZL03_原始素材/icon素材/` | 导出/设计工作稿，不进工程 |

两边都有图时，以 `ApiRelay/icon/` 为准，不要只改 `icon素材/` 却忘了工程侧。

## 历史名

以前顶层叫 `ApiRelay_相关展览/`，后收进本目录；子文件夹一度叫 `相关展览/`，
现为 `相关展示截图/`；图标子目录提到同级的 `icon素材/`。
