# v1.13.10 新基线复验 · 阶段 2 前置条件核对

方法版本：MAIC-1.3.1  
阶段：2（方法依赖接入后的复验链）  
时间：2026-09-24（中国标准时间）  
分支：`v1.13.10`  
起始 HEAD：`ead076af9bd706a3bf4b97a67fc692cfcda58060`  
输入：`重基线复验/01-计划延续核对.md`、`00C-任务契约.json`、`00F-方法接入重基线记录.md`、旧阶段 `01`–`04` 与 `06`–`09`、两棵树的只读 Git 状态、方法提交差异、受保护文件内容。  
授权：仅对新基线做实施前核对并写本报告；不更改产品代码、旧报告、原工作树、暂存区或远端。

## 实际核对

- `git rev-parse HEAD` 为 `ead076a`，分支为 `v1.13.10`；`git merge-base --is-ancestor 25fe006 ead076a` 退出 0。方法提交 `ead076a` 相对旧基线只变更 12 个 ZL00/ZL02 方法文件，没有 `ApiRelay/` 或 `ZL01_具体说明/` 产品文件。方法文件在当前产品工作树已属 HEAD，未作为本地产品差异混入。
- 产品工作树暂存区为空；当前可见改动仍是既有 `ApiRelay/` 与 `ZL01_具体说明/` 的结构治理、测试和说明文件，以及本工作包复验记录。独立方法接入记录 `00F` 给出接入前后产品已跟踪 diff 与未跟踪内容摘要逐字相等的证据；本轮没有据此声称功能测试已经在新 HEAD 重跑。`git diff --check` 退出 0。
- 原工作树只读核对：仍为 `v1.13.9`、HEAD `25fe006896a816475e269e155530c1464cc19304`、暂存区为空。购买页面 `SettingsView.swift`、`Localizable.xcstrings`、工程文件及其他原树差异仍在原处；本轮没有对其写入或并入。v1.13.10 检查器退出 0，说明契约登记的原树只读快照 `f65cbd67583845544e575820540a90a8390db14257faff9b4a2a6d1ad445ecbd` 在该次检查中匹配。原 v1.13.9 工作包退出 4 的历史阻塞不因本次结果而解除。
- `ApiRelay.entitlements`、`Schema.swift`、`KeychainStore.swift` 的当前 SHA-256 依次为 `8c49887c42f110a6b6f92c1f85ba1d66fbb53e04df295a6a7d91e8394532a3c0`、`0a1537ab4296c9f17f125d9bf98147912262b3c75ef25ecefb11a679ba18c3a8`、`0693a04fbfca06f874f300dda394c854716206e8f556b0f398e39aa60795b6f7`；三者与旧 `04-不可逆标识基线.md` 一致。Xcode 项目文件的差异仍为 UI-test target 等产品工作包范围内容，不以其整文件 hash 作敏感值基线。
- 在本阶段写入前，检查器退出 0，正确路由到新链阶段 2，且报告暂存区空、产物身份有效、无写入冲突；检查器不是执行授权，也不替代独立审计。

## 判断

本次前置条件未发现要求推翻旧 W1–W5 产品计划的实质冲突，可以交阶段 3 明确“原计划延续、旧验收不可冒充新基线验收”的决定。此处仅认定重基线隔离与范围足以继续复验；旧 `xcresult` 仍是旧 HEAD 的原始测试证据，本报告不宣称重跑代码或阶段 7 完成。

方法观察：产品检查器能在不吸纳原树脏差异的前提下验证冻结快照；未发现需要迭代的方法问题。
