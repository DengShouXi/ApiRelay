# v1.13.10 新基线复验 · 阶段 7 独立最终验收

方法版本：MAIC-1.3.1  
阶段：7（方法依赖接入后的复验链）  
时间：2026-09-24（中国标准时间）  
分支：`v1.13.10`  
起始 HEAD：`ead076af9bd706a3bf4b97a67fc692cfcda58060`  
验收者：非阶段 4 实施者的独立验收角色  
输入：ZL00/00–04、本工作包 `00`、`00C`、`00F`、新链 `01`–`04`、`06`、`07`、旧计划与阶段 `06`–`09` 历史报告、阶段 7 提示词、实际 Git 状态与本轮原始 xcresult。  
授权：只读最终验收并新增本报告；不改其他文件，不暂存、提交、推送，不改原树或发布状态。

## 最终差异与隔离

- 阶段 4 的新链 `06` 和阶段 5 的新链 `07` 均有独立“写入状态：已停止”声明；阶段 5 固定结论为“审计通过，无阻塞问题，可以进入阶段7。”本次工作树仍为 `v1.13.10`／`ead076af9bd706a3bf4b97a67fc692cfcda58060`，暂存区为空，`git diff --check` 通过。阶段 7 报告写入前检查器退出 0，明确路由阶段 7，契约及产物身份有效、无写入冲突。
- 已入库的方法提交 `ead076a` 是旧 `25fe006` 的后续，范围仅 5 个 ZL00 方法文件与 7 个 ZL02 方法工作包文件；当前这些方法文件无工作区差异。再次分别以旧、新 HEAD 计算当前 App、工程、单测和 UI 测试已跟踪二进制 diff，摘要均为 `732cc00ddbd2901b8906b72e2f952d8b7701770a7fb1c42a9048fdf20492e519`。方法接入没有改变产品代码／测试输入；旧阶段 7 只保留旧 HEAD 的历史身份，本报告依据新链差异和新跑测试作决定。
- 当前产品差异均落在契约允许路径，Keychain、SwiftData、entitlements 等保护范围未出现计划外差异。原 `v1.13.9` 工作树仍为 `25fe006`、暂存区为空，1183 条 Git 可见状态摘要 `ee36963eab319adcbd3163c51c0d2488e310dc2e4a39919d447d68215f29007a` 与只读清单一致；产品检查器核对其向前只读指纹 `f65cbd67583845544e575820540a90a8390db14257faff9b4a2a6d1ad445ecbd`。原树购买和 Xcode 差异仍在原处，未并入本工作树；原 `v1.13.9` 检查器退出 4 仍是原包阻塞，不被本结论消除。
- entitlements、SwiftData Schema、KeychainStore 的 SHA-256 依次为 `8c49887c42f110a6b6f92c1f85ba1d66fbb53e04df295a6a7d91e8394532a3c0`、`0a1537ab4296c9f17f125d9bf98147912262b3c75ef25ecefb11a679ba18c3a8`、`0693a04fbfca06f874f300dda394c854716206e8f556b0f398e39aa60795b6f7`，与不可逆基线相等。App Bundle ID、Team ID、App Group、Keychain service、iCloud container 和 `1.0.1 (9)` 未发现漂移。本地 `main`、`v1.13.9`、`v1.14`、release tag 及其远端跟踪 refs 与契约值一致；`v1` 本地和远端跟踪 ref 尚为 `25fe006`。

## 新基线测试与行为边界

独立重新读取本轮四份原始 xcresult，而非抄用旧验收：iPhone 16 模拟器全套 `617/617`、原生 Mac UI `3/3`、Mac Catalyst UI `3/3`、iPad (A16)／iPadOS 26.5 fixture UI `4/4`，均 `Passed` 且失败、跳过、运行警告为 0。原始路径分别为 `/private/tmp/ApiRelay-v11310-rebaseline-iphone-all.xcresult`、`/private/tmp/ApiRelay-v11310-rebaseline-mac-ui-foreground.xcresult`、`/private/tmp/ApiRelay-v11310-rebaseline-catalyst-ui-foreground.xcresult`、`/private/tmp/ApiRelay-v11310-rebaseline-ipad-ui.xcresult`。iPad 四个真实 UI 节点覆盖立即后台锁定、重启后再锁、应用密码到设备验证的当前页切换、错误应用密码后的回滚与重试。方法检查器自测 84 项及项目自检红线均通过。

较早两份 Mac 与一份 Catalyst xcresult 是 UI runner 在启用自动化模式时超时，未进入 App 断言；前台条件下的本轮桌面 UI 重跑才是通过证据。该宿主条件依赖仍是无人值守自动化的环境风险，不能声称任意桌面后台条件下均稳定。现有结果也不把模拟器测试冒充 iPhone／iPad 真机生物识别、设备密码、真实 App Switcher 或跨设备同步验证。

状态 ownership 的产品实现与旧阶段 7 验收时相比字节未变，旧报告对 committed/draft、互斥 pending action、回收站可见集、生命周期信号到锁态单一来源及安全偏好通知顺序的源码判断可作历史交叉证据；本轮新测试在方法接入后的同一产品输入上通过，未发现新的状态来源或安全语义漂移。同步迁移和购买权益没有进入本工作包。

## 结论与后续闸门

新基线的本地范围、隔离、敏感标识、真实自动测试和阶段 5 审计均满足本轮阶段 7 验收。此前实时 `git ls-remote` 因宿主 DNS 不能解析 github.com，故本报告只确认本地与远端跟踪 refs，不宣称此刻远端实时状态已复核。既有用户授权的阶段 8 仍须由实施角色在提交前重新核对实时远端、按工作包精确暂存并遵守 A/B 流程；若网络或 refs 不符，应停止而非强推。阶段 9 必须另由独立角色核对实际远端；不改 `main`、tag，不做 Archive、TestFlight 或 App 审核。

方法观察：本轮未发现新的方法缺陷；保留桌面宿主超时与成功原始包，避免把“本地通过”误写成“所有自动化环境稳定”。

写入状态：已停止

验收通过，可以在用户明确授权后提交并上传 v1.13.10。
