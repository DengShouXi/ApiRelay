# v1.13.10 新基线复验 · 阶段 5 独立审计

方法版本：MAIC-1.3.1  
阶段：5（方法依赖接入后的复验链）  
时间：2026-09-24（中国标准时间）  
分支：`v1.13.10`  
起始 HEAD：`ead076af9bd706a3bf4b97a67fc692cfcda58060`  
审计者：非阶段 4 实施者的独立审计角色  
输入：ZL00/00–04、本工作包 `00`、`00C`、`00F`、新链 `01`–`04`、`06`、旧阶段计划与 `06`–`09`、阶段 5 提示词、两棵树 Git 实况及本轮七份 xcresult。  
授权：仅独立审计并新增本报告；不改产品代码、契约、原 `v1.13.9` 工作树，不暂存、提交、推送或进入阶段 7/8。

## 独立核对

1. 新链 `06-接入与自测报告.md` 有独立行“写入状态：已停止”。当前工作树为 `v1.13.10`／`ead076a`，暂存区为空；`git diff --check` 通过。工作包检查器在本报告写入前退出 0，身份有效、无写入冲突，路由阶段 5。其结论只作机械范围证据，不代替本审计。
2. `25fe006` 是 `ead076a` 的祖先；`git diff-tree` 对方法提交列出恰好 12 个文件（5 个 ZL00 修改、7 个 ZL02 新增），无 `ApiRelay/` 或 `ZL01_具体说明/`。这些方法文件当前无工作区差异。以旧 HEAD 与新 HEAD 分别计算当前 App／工程／测试的已跟踪二进制 diff，SHA-256 均为 `732cc00ddbd2901b8906b72e2f952d8b7701770a7fb1c42a9048fdf20492e519`；方法接入本身未改产品代码或测试输入。当前其余产品差异仍由契约白名单约束，受保护的 Keychain／SwiftData／entitlements 路径无差异。
3. 原工作树仍为 `v1.13.9`／`25fe006`，暂存区为空，Git 可见记录数 1183；本次独立计算 `git status --porcelain=v1 -z -uall` 摘要为 `ee36963eab319adcbd3163c51c0d2488e310dc2e4a39919d447d68215f29007a`，与 `00E` 清单一致。产品检查器验证完整只读指纹 `f65cbd67583845544e575820540a90a8390db14257faff9b4a2a6d1ad445ecbd`。购买页面、文案及 Xcode 差异仍留在原树；这仅确认新快照起向前隔离，不追认旧快照期间的逐文件历史。原 `v1.13.9` 工作包退出 4 的阻塞不因此解除。
4. `ApiRelay.entitlements`、`Schema.swift`、`KeychainStore.swift` SHA-256 依次为 `8c49887c42f110a6b6f92c1f85ba1d66fbb53e04df295a6a7d91e8394532a3c0`、`0a1537ab4296c9f17f125d9bf98147912262b3c75ef25ecefb11a679ba18c3a8`、`0693a04fbfca06f874f300dda394c854716206e8f556b0f398e39aa60795b6f7`，与旧、新敏感基线相同。工程内 App Bundle ID `com.apirelay.ApiRelay`、Team ID `8NDLKZ4SSF`、App 版本／构建 `1.0.1 (9)` 未漂移。受保护本地 refs（`main`、`v1.13.9`、`v1.14`、release tag）与契约相符；`v1` 本地及远端跟踪 ref 仍为 `25fe006`，产品远端 `origin/v1.13.10` 尚不存在，符合提交前状态。本轮尝试实时 `git ls-remote` 时宿主 DNS 无法解析 github.com，因此不把远端实时状态写成已验证；阶段 8 上传及阶段 9 远端复核仍须实际检查。
5. 独立从本轮原始包读取 `xcresulttool get test-results summary`：iPhone 16 模拟器全套 `617/617`、原生 Mac UI `3/3`、Mac Catalyst UI `3/3`、iPad (A16)／iPadOS 26.5 fixture UI `4/4`，四组均 `Passed`、失败／跳过／运行警告均 0。原始包依次是 `/private/tmp/ApiRelay-v11310-rebaseline-iphone-all.xcresult`、`/private/tmp/ApiRelay-v11310-rebaseline-mac-ui-foreground.xcresult`、`/private/tmp/ApiRelay-v11310-rebaseline-catalyst-ui-foreground.xcresult`、`/private/tmp/ApiRelay-v11310-rebaseline-ipad-ui.xcresult`。iPad 测试节点还逐项确认了立即后台锁定、重启再锁、当前页连续切换和错误应用密码回滚四个断言。
6. 早先桌面失败包 `/private/tmp/ApiRelay-v11310-rebaseline-mac-ui.xcresult`、`/private/tmp/ApiRelay-v11310-rebaseline-mac-ui-retry.xcresult`、`/private/tmp/ApiRelay-v11310-rebaseline-catalyst-ui.xcresult` 均为 `Failed`，唯一失败是 UI runner “Timed out while enabling automation mode”，通过数 0，未进入 App 断言；不能伪称这些包通过。相同产品输入在 Xcode 前台条件下重跑实际 UI 断言全部通过，故这些宿主启动超时目前不是产品阶段 5 阻塞，但仍是后续 CI／无人值守运行的环境稳定性风险，不得删除失败证据或声称任意宿主状态均稳定。
7. `bash ApiRelay/scripts/selfcheck.sh` 红线通过；方法检查器自测 84 项通过。超长文件和 `try?` 分布属于该脚本的信息报告，不在本次纯结构范围外追加产品修改。

## 裁决与边界

新基线是独立方法提交，产品 diff、敏感标识、原树隔离和四组本轮测试均有可复核证据。未发现阻止新链进入阶段 7 的问题。此结论不代表 iPad／iPhone 真机生物识别、系统设备密码、真实 App Switcher 或跨设备同步已由模拟器证明；也不批准阶段 8 上传。阶段 7 须由非实施者另行只读最终验收，并在其通过后按既有用户授权与实时 Git／远端闸门处理阶段 8。

方法观察：桌面 runner 在后台宿主条件下重现初始化超时，而前台重跑稳定通过；应把失败原始包与环境条件留作后续自动化稳定性证据，当前不据此改写通用方法。

写入状态：已停止

审计通过，无阻塞问题，可以进入阶段7。
