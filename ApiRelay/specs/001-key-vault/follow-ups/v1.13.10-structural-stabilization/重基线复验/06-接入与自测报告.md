# v1.13.10 新基线复验 · 阶段 4 接入与自测

方法版本：MAIC-1.3.1
阶段：4（方法依赖接入后的复验链）
时间：2026-09-24（中国标准时间）
分支：`v1.13.10`
起始 HEAD：`ead076af9bd706a3bf4b97a67fc692cfcda58060`
输入：新基线 `01`–`04`、`00C`、`00F`、旧阶段 4–7 报告、当前 Git 差异和下列新生成的 xcresult。
授权：用户批准接入方法提交、对齐文档、更新契约并独立复验；只有复验通过后才执行原产品阶段 8 A/B。未获授权改动原 `v1.13.9` 工作树、购买权益、同步或发布。

## 实际接入与范围

`v1.13.10` 已以快进方式接入独立远端复核过的方法提交 `ead076a`，方法文件成为 HEAD 内容，原 12 文件的定向保全 stash `3da805ddcb01d9294fc21436c86ed6afecc8df7d` 仍保留。旧阶段报告在旧 HEAD `25fe006` 的真实身份未被覆盖；产品契约将现行 `baselineHead` 更新至 `ead076a`，移除已入库文件的旧冻结状态，另建本复验链。接入过程未更改 App 代码或产品测试文件；对 `ApiRelay/ApiRelay`、工程、单测和 UI 测试的已跟踪二进制 diff，分别以旧 HEAD 和新 HEAD 计算，SHA-256 均为 `732cc00ddbd2901b8906b72e2f952d8b7701770a7fb1c42a9048fdf20492e519`。原有未跟踪产品文件在接入前后内容摘要一致，详见 `00F`；本轮新增的只有工作包复验文档。

原 `v1.13.9` 树仍为 `25fe006`，未暂存、未提交、未合并。当前 `v1.13.10` 暂存区为空，`git diff --check` 通过。`ApiRelay.entitlements`、`Schema.swift`、`KeychainStore.swift` 的 SHA-256 依次仍为 `8c49887c42f110a6b6f92c1f85ba1d66fbb53e04df295a6a7d91e8394532a3c0`、`0a1537ab4296c9f17f125d9bf98147912262b3c75ef25ecefb11a679ba18c3a8`、`0693a04fbfca06f874f300dda394c854716206e8f556b0f398e39aa60795b6f7`。

## 新基线实测

四组均使用本工作树的 `ApiRelay/ApiRelay.xcodeproj` 与共享 `ApiRelay` scheme，显式 `-parallel-testing-enabled NO`；桌面只运行 `ApiRelayUITests`。iPhone/iPad 使用设备 UUID，Mac 与 Catalyst 明确区分目的地。每组结果均由 `xcrun xcresulttool get test-results summary --format json` 从本轮原始包读取，而非沿用旧包。

| 组别 | 本轮通过 | 失败／跳过／运行警告 | 原始结果 |
| --- | ---: | --- | --- |
| iPhone 16 模拟器全套 | 617 | 0／0／0 | `/private/tmp/ApiRelay-v11310-rebaseline-iphone-all.xcresult` |
| 原生 Mac UI | 3 | 0／0／0 | `/private/tmp/ApiRelay-v11310-rebaseline-mac-ui-foreground.xcresult` |
| Mac Catalyst UI | 3 | 0／0／0 | `/private/tmp/ApiRelay-v11310-rebaseline-catalyst-ui-foreground.xcresult` |
| iPad (A16)／iPadOS 26.5 fixture UI | 4 | 0／0／0 | `/private/tmp/ApiRelay-v11310-rebaseline-ipad-ui.xcresult` |

桌面测试的第一次运行使用独立 DerivedData，Mac 与 Catalyst runner 均在“启用自动化模式”超时，未进入应用断言；原始失败包分别为 `/private/tmp/ApiRelay-v11310-rebaseline-mac-ui.xcresult` 和 `/private/tmp/ApiRelay-v11310-rebaseline-catalyst-ui.xcresult`。Mac 在 Xcode 默认 DerivedData 的第二次尝试仍因同一宿主超时，失败包为 `/private/tmp/ApiRelay-v11310-rebaseline-mac-ui-retry.xcresult`。随后只将 Xcode 置于前台，未在其打开的原 `v1.13.9` 项目窗口点击 Run、改文件或申请“控制电脑”权限；仍由 CLI 针对本 `v1.13.10` 工程重跑。前台条件下原生 Mac 与 Catalyst 的结果均为通过。保留失败证据是为了区分宿主时序与 App 断言，不把早先失败抹去。

方法检查器自测 84 项通过；`bash ApiRelay/scripts/selfcheck.sh` 红线通过。新基线产品检查器在本报告创建前退出 0，路由阶段 4；本报告停写后须重新运行并由非实施者审计，不能用本自测报告代替阶段 5 或 7。

## 停止点

没有修改 App 功能或敏感标识，没有提交、推送、推进 `v1`/`main`、打 tag、Archive、TestFlight 或送审。下一步仅由独立角色核对本轮差异和原始测试，并给出阶段 5 结论；若有阻塞，按本复验链处理，不直接执行阶段 8。

方法观察：桌面 UI runner 的自动化模式受前台条件影响；保留失败与成功原始包，避免把宿主启动失败误报成产品回归或把旧结果冒充新结果。

写入状态：已停止
