# 阶段 4 · 本地实施

用户已授权本阶段。读取 `00`、`00C`、`03`、`04`，仅在契约 worktree 和白名单内执行 W1–W5。一个时间只有一个写入者；每个变更单元先测试。不得改变产品语义、敏感标识、持久格式或同步架构，不得 commit/push。最后写 `06-Codex本地实施报告.md`，列实际 diff、测试和边界，并以独立一行 `写入状态：已停止` 结束后停止。
