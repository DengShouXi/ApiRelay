# v1.13.10 纯结构稳定化 · 总流程操作台

方法版本：MAIC-1.3.1

## 目标与授权

- 目标：不改变产品功能，补真正的 XCUITest，拆分职责过载文件，消除实际存在的第二状态来源。
- 基线：`v1.13.9` closure SHA `25fe006896a816475e269e155530c1464cc19304`。
- 现行实施基线：独立方法包提交 `ead076af9bd706a3bf4b97a67fc692cfcda58060`；接入、保全与历史身份见 [`00F-方法接入重基线记录.md`](./00F-方法接入重基线记录.md)。
- 唯一实施 worktree：`/Users/xitongzhili/.codex/worktrees/v1-13-10-structural/E02_ApiRelay_Github`。
- 用户授权：2026-09-24 明确要求实现下一阶段、测试并在同范围内修复直至成功；后又明确授权在方法提交接入、冻结文件对齐及独立复验通过后执行既有阶段 8 A/B 提交推送。
- 仍未授权：推进 `main`、tag、Archive/送审、`v1.14` 同步、UI 重设计或产品语义变化。`v1` 仅按既有上传流程在 B 成功后快进。

## 保护边界

1. `v1.13.9` 密码与身份验证语义保持冻结。
2. Bundle ID、Team ID、Keychain group/service、iCloud container、SwiftData schema/store path 不变。
3. 原主工作树中的 `project.pbxproj`、scheme 与治理实验差异属于其他工作，不复制、不覆盖、不整合。
4. 机械拆分与 ownership 迁移是不同变更单元；每个单元先测试再进入下一个。
5. KeyVault/SecureBackup 不新增 actor 边界，不改变 await、WAL 或最终 commit 顺序。
6. XCUITest 使用固定 DEBUG-only fixture，不读写生产 Keychain、CloudKit 或用户数据。

## 阶段

| 阶段 | 产物 | 状态 |
| --- | --- | --- |
| 1 | `01-Codex执行计划.md` | 已完成 |
| 2 | `02-Codex实施前检查.md` | 已完成 |
| 3 | `03-Codex最终计划.md`、`04-不可逆标识基线.md` | 已获用户授权 |
| 4 | `06-Codex本地实施报告.md` | 旧基线已实施并停写 |
| 5–7 | `07-Codex独立审计报告.md`、`08-Codex整改报告.md`、`09-Codex最终验收报告.md` | 旧基线已验收；历史报告保留 |
| 新基线复验 | `重基线复验/` | 在 `ead076a` 上复验，阶段 5 独立审计、阶段 7 独立最终验收均已通过 |
| 8–9 | 产品 A/B 提交上传与远端复核 | 阶段 8 已获用户授权，按精确范围执行；阶段 9 仍由独立角色执行 |

当前阶段通过契约、实际产物与 Git 状态推导，不以本表一句话代替检查器。

## 方法观察

启动时发现原工作树恰有 Xcode 工程与 scheme 的无关脏改动，因此本阶段强制使用独立 worktree；这是防止结构治理再次混入旧修改的必要边界。
