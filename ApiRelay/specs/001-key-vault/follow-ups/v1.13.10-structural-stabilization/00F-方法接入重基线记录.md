# v1.13.10 · MAIC-1.3.1 方法依赖接入

- 时间：2026-09-24（中国标准时间）
- 分支：`v1.13.10`
- 旧基线 HEAD：`25fe006896a816475e269e155530c1464cc19304`
- 新基线 HEAD：`ead076af9bd706a3bf4b97a67fc692cfcda58060`
- 来源：独立方法分支 `codex/maic-1-3-1-isolation`；其远端 HEAD 已由非实施者只读复核，提交仅含 5 个 ZL00 方法文件和 7 个 ZL02 方法工作包文件。
- 授权：用户明确批准保全现有产品改动后接入该提交、对齐工作包操作台的 3 处行尾空格、更新契约并独立复验；复验通过后才执行既有阶段 8 A/B 推送。

## 接入事实与保全

原 `v1.13.9` 工作树未修改、未暂存、未提交，仍在旧基线 HEAD。接入前，`v1.13.10` 有 78 条 Git 可见差异，其中 12 个方法文件与产品差异混在同一工作树。方法文件被精确存入可恢复的定向 stash `3da805ddcb01d9294fc21436c86ed6afecc8df7d`；stash 检查仅列出这 12 个文件。未使用整体 stash、reset、清理或强制合并。

随后将 `v1.13.10` 以 `--ff-only` 快进至已远端复核的 `ead076a`。11 个方法文件与接入前逐字节相同；方法包 `00-总流程操作台.md` 的前三个 Markdown 行尾空格由已提交版本取代，正文事实未变。此后方法文件无工作区差异；定向 stash 保留供追溯，不在产品 A/B 提交范围。

接入前后，产品范围 `ApiRelay/` 与 `ZL01_具体说明/` 的已跟踪二进制 diff 摘要均为 `f52fc58c5257f30128983e9f750f6a9ea2dddb3acd44004bf19c64d42b6eb4cd`，未跟踪文件内容摘要均为 `388f9b002398cf849d3d33bde607e88141314d9436f812213167a8332ca2412e`。产品差异从 66 条保持为 66 条，暂存区仍空；这只证明接入过程未改产品差异，不代替产品功能验收。

## 契约处理

产品契约的 `baselineHead` 改为 `ead076a`。方法文件已经属于该 HEAD，故移除旧的 `frozenExternalFiles` / `frozenExternalReview`；它们的独立审计历史仍由原 ZL02 方法包和 Git 提交保留，不作为产品阶段的可修改路径。原阶段 1–7 报告继续保持旧 HEAD 身份，不覆盖、不删改；新基线复验产物单独放在 `重基线复验/`，其结论和检查器通过后才可放行阶段 8。

本记录不宣称产品阶段 8、A/B 上传或阶段 9 已完成。`main`、`v1.13.9`、发布 tag、Archive、TestFlight 与 App 审核均不在接入动作内。

方法观察：旧阶段产物的真实 HEAD 不应被改写成新基线；本次保留旧报告并为新基线另建复验链。
