# 阶段 1 — 密钥库（v1）

**用途**：Stage1 密钥保险库实现与小迭代；对应 Git 分支 `v1` / `v1.N`。

**权威目录**：[`../v1-key-vault/`](../v1-key-vault/)  
**建议起步**：[`../v1-key-vault/00-README.md`](../v1-key-vault/00-README.md)

**返回索引**：[`索引.md`](./索引.md)

## 怎么用

1. 打开 `00-README.md`，看「端到端顺序」。  
2. 在 `phases/` 做当前 `P0N`；写完走 `P0N-save.md`（备注英→中、commit、push `v1.N`）。  
3. **当前进度**：`v1.12` 已关 → 下一开工 **`v1.13`（上架材料）**。  
4. P13 之后严格按：`10-verify` →（授权）`12-release-T062` → `20-push` → `30` / `40`。

## 易混点（一次说清）

| 容易混的事 | 正确说法 |
|------------|----------|
| P08 是否含商店描述/截图 | **否**。P08 = T056–T061；商店材料 = **P13 / T063–T066** |
| 材料齐了是否等于已上架 | **否**。上架是 **T062**，见 `12-release-T062.md`，须明确授权 |
| CloudKit 未 Deploy 能否写功能 | **能**（Checkpoint **2a** 后）。**2b** 是 TestFlight/正式同步硬门槛，已在 `v1.9` 完成 |
| 小迭代用 tag 吗 | **不用**。开发用分支 `v1.N`；商店才打 `release/1.0.0` |

当前小迭代以 [`../BRANCHES.md`](../BRANCHES.md) 台账为准。
