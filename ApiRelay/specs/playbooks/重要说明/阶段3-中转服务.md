# 阶段 3 — 中转服务（v3）

**用途**：Stage3 中转 / Worker 等；对应 Git 分支 `v3` / `v3.N`。

**权威目录**：[`../v3-relay-service/`](../v3-relay-service/)  
**建议起步**：[`../v3-relay-service/00-README.md`](../v3-relay-service/00-README.md)

**返回索引**：[`索引.md`](./索引.md)

## 怎么用

1. **须等 Stage2 验收通过后再开始实现**（见 ROADMAP）。  
2. 打开 `00-README.md`，按 `phases/` 与 `P0N-save.md` 推进并 push `v3.N`。  
3. 范围争议以 [`../../ROADMAP.md`](../../ROADMAP.md) 为准。
