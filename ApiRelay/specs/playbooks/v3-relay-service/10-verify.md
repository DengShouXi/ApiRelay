# V3 — 检测 / 验收提示词

---

请对 **V3（`003-relay-service`）** 做版本级验收。

## 检查焦点

1. 部署引导是否可被非技术用户走通（或明确剩余门槛）
2. 轮换验收：换原始密钥后下游配置不变
3. 流式转发正确性；双存储一致性
4. 可撤销共享；消费上限
5. 第二档 IAP；隐私文案与问卷已更新
6. 客户端仍无明文泄露；Worker 日志无密钥/prompt 落盘不当

输出 Pass/Fail；通过则打开 `20-push.md`。
