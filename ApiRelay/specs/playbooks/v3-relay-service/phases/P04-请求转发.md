# V3 · Phase 4 — 请求转发（实现提示词）

---

请实现按平台协议转发推理请求，**含流式响应**。

## 要点

- OpenAI 兼容 vs Anthropic 原生；流式格式差异
- HTTPS only；错误分类与超时
- 延迟与冷启动如实可观测（至少日志/诊断钩子，禁明文）

汇报：支持的协议列表与流式验收方式。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后，按顺序另开操作：

1. 打开并粘贴提示词：`../05-annotate-branch.md`（更新 `BRANCHES.md` 英+中备注）
2. 再打开并粘贴提示词：`../15-phase-push.md`（在分支 `v0.3` 上 commit，打 **tag `v0.3.4`**，再 `git push`）

说明：第三层是 **tag `v0.3.4`**，不是新建分支；人继续留在 `v0.3` 做下一 Phase。
