# V3 · Phase 5 — 逐笔用量与原始密钥轮换（实现提示词）

---

请实现 Worker 侧逐笔记账回传 App，以及**原始密钥轮换**（中转密钥与下游配置不变）。

## 要点

- 这是相对 V2「看平台脸色」的用量的升级：完全自主
- 轮换是核心价值演示路径：换上游钥 → 下游工具配置零修改
- 历史逐笔与聚合展示口径与 V2 字段 additive 扩展

汇报：轮换验收步骤（建议写进 quickstart）。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后，按顺序另开操作：

1. 打开并粘贴提示词：`../05-annotate-branch.md`（更新 `BRANCHES.md` 英+中备注）
2. 再打开并粘贴提示词：`../15-phase-push.md`（在分支 `v0.3` 上 commit，打 **tag `v0.3.5`**，再 `git push`）

说明：第三层是 **tag `v0.3.5`**，不是新建分支；人继续留在 `v0.3` 做下一 Phase。
