# V3 · Phase 2 — 部署引导（实现提示词）

---

请实现「非技术用户在自己的 Cloudflare 账号部署 Worker」的引导式流程（产品最大难点）。

## 要点

- 优先 Cloudflare API Token 自动部署，而不是甩 `wrangler deploy` 文档
- 失败可恢复、步骤可重试；权限范围最小化并披露
- 费用/免费额度上限如实告知

不要在本阶段完成全部协议转发。汇报引导步骤线框或已实现界面流。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后：

1. 打开同目录：[`P02-save.md`](./P02-save.md)  
2. 复制其中「请为」起的整段给 Cursor  
   → 写 `BRANCHES.md` 英+中备注，推送到小阶段分支 **`v0.3.2`**（不是 tag）

测试代码只放在：`ApiRelay/ApiRelayTests/V3/Phase02_Deploy/`  
临时乱写调试放：`ApiRelay/DebugScratch/`（已 gitignore，不会上传）。
