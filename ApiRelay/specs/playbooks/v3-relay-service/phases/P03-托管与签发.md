# V3 · Phase 3 — 托管与中转密钥签发（实现提示词）

---

请实现：原始密钥推送到用户 Worker 存储 + 一把原始密钥签发多把中转密钥（一工具一把）。

## 要点

- 对方（或下游）永远拿不到原始密钥
- App 侧 Keychain 与 Worker 侧一致性校验
- 中转密钥元数据进 SwiftData（additive）；明文策略仍守红线

汇报：签发/吊销 API 与一致性校验策略。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后，按顺序另开操作：

1. 打开并粘贴提示词：`../05-annotate-branch.md`（更新 `BRANCHES.md` 英+中备注）
2. 再打开并粘贴提示词：`../15-phase-push.md`（在分支 `v0.3` 上 commit，打 **tag `v0.3.3`**，再 `git push`）

说明：第三层是 **tag `v0.3.3`**，不是新建分支；人继续留在 `v0.3` 做下一 Phase。
