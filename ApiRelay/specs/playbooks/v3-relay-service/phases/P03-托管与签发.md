# V3 · Phase 3 — 托管与中转密钥签发（实现提示词）

---

请实现：原始密钥推送到用户 Worker 存储 + 一把原始密钥签发多把中转密钥（一工具一把）。

## 要点

- 对方（或下游）永远拿不到原始密钥
- App 侧 Keychain 与 Worker 侧一致性校验
- 中转密钥元数据进 SwiftData（additive）；明文策略仍守红线

汇报：签发/吊销 API 与一致性校验策略。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后：

1. 打开**同目录**完整上传提示词：[`P03-save.md`](./P03-save.md)  
2. 把该文件里「请为」起的整段复制给 Cursor（会写 `BRANCHES.md` 英+中备注，在分支 `v0.3` 上 commit，打 tag **`v0.3.3`** 并 push）

不要把上传步骤粘进本次实现对话。
