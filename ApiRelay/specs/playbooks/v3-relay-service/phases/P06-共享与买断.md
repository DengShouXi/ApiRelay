# V3 · Phase 6 — 可撤销共享与第二档买断（实现提示词）

---

请实现：为同事/朋友签发专属中转密钥（可撤销、可单独统计、可设消费上限）+ 解锁中转的买断。

## 要点

- 这是 V2「加密传递」的正确升级：可撤销
- 产品 ID：`com.apirelay.iap.relay`；V1/V2 界面此前不得已提及，本阶段才出现
- `EntitlementTier.relay` 从此可达；单测覆盖升降级

汇报：共享生命周期与 IAP 路径。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后：

1. 打开同目录：[`P06-save.md`](./P06-save.md)  
2. 复制其中「请为」起的整段给 Cursor  
   → 写 `BRANCHES.md` 英+中备注，推送到小阶段分支 **`v0.3.6`**（小迭代分支）

测试代码只放在：`ApiRelay/ApiRelayTests/V3/Phase06_ShareIAP/`  
临时乱写调试放：`ApiRelay/DebugScratch/`（已 gitignore，不会上传）。
