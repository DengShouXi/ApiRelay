# V2 完成后 — 切换到 V3 分支提示词

---

请 `git checkout 003-relay-service`，确认状态，并指向：

`ApiRelay/specs/playbooks/v3-relay-service/00-README.md`

提醒：V3 是双技术栈（Swift + Worker TS/JS），且涉及 prompt 过中转的重大隐私变更；先读 ROADMAP V3「重难点」再开做。
