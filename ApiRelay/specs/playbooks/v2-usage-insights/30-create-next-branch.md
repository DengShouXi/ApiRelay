# V2 完成后 — 创建 V3 分支提示词

---

请从最新 **`main`（已含 V2）** 创建分支 `003-relay-service` 并推送（推送前确认）。

可选：创建 `specs/003-relay-service/README.md` 占位。  
不要从旧的 `002-usage-insights` 直接分支。  
完成后打开 `40-checkout-next-branch.md`。
