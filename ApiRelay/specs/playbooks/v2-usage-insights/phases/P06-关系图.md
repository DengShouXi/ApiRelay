# V2 · Phase 6 — 关系图 US8（实现提示词）

---

请实现三列桑基式关系图（左平台 / 中密钥 / 右使用方），**不要**做自由布局思维导图。

## 要点

- 基数：平台→密钥 1:n；密钥↔工具 n:n；共享密钥样式区分
- 点击节点高亮关联路径；连线粗细可映射用量
- Mac 优先；iPhone 降级为横向滚动紧凑版或隐藏入口
- 零第三方 SDK：不引入力导向布局库

汇报：Mac / iPhone 分别的入口与降级策略。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后，按顺序另开操作：

1. 打开并粘贴提示词：`../05-annotate-branch.md`（更新 `BRANCHES.md` 英+中备注）
2. 再打开并粘贴提示词：`../15-phase-push.md`（在分支 `v0.2` 上 commit，打 **tag `v0.2.6`**，再 `git push`）

说明：第三层是 **tag `v0.2.6`**，不是新建分支；人继续留在 `v0.2` 做下一 Phase。
