# V2 · Phase 8 — 上架与收尾（实现提示词）

---

请完成 Stage2 → App Store **2.0.0** 所需材料与工程收尾（DC-013 已废止；每阶段独立上架）。

## 要点

- App Store 元数据、隐私问卷、截图
- 隐私政策是否覆盖：联网调上游、管理凭证、通知、加密传递（仍无「prompt 过中转」——那是 V3）
- quickstart / 安全复查（含探活诚实性）
- 合并 `v2` → `main`，钉 `MARKETING_VERSION=2.0.0`，打 tag `release/2.0.0`（需我明确授权再执行）

完成后进入 `10-verify.md`。


---

## 本阶段完成后（这段不要复制进实现对话）

Checkpoint 通过后：

1. 打开同目录小迭代上传提示词：[`P08-save.md`](./P08-save.md)  
2. 复制其中「请为」起的整段给 Cursor  
   → 写 `BRANCHES.md`（英→中），推送到小迭代分支 **`v2.8`**

测试只放：`ApiRelay/ApiRelayTests/V2/Phase08_Store/`  
临时调试：`ApiRelay/DebugScratch/`（不上传）
