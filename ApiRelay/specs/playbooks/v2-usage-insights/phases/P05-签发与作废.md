# V2 · Phase 5 — App 内签发与作废 US3（实现提示词）

---

请实现仅 OpenRouter 支持的 App 内签发/作废；其余平台入口提前置灰并说明原因。

## 要点

- 走 Provider 模块；失败隔离
- 签发后密钥进入保管流（Keychain + 元数据），不在 UI 长期展示明文
- 作废需确认；与「最近删除」语义协调（以 tasks/spec 为准）

不要实现中转签发（那是 V3）。汇报能力矩阵与置灰文案。


---

Checkpoint 通过后：[`../05-annotate-branch.md`](../05-annotate-branch.md) → [`../15-phase-push.md`](../15-phase-push.md)（在大阶段分支上 commit，并打 **tag** 推送）。
