# V2 · Phase 5 — App 内签发与作废 US3（实现提示词）

---

请实现仅 OpenRouter 支持的 App 内签发/作废；其余平台入口提前置灰并说明原因。

## 要点

- 走 Provider 模块；失败隔离
- 签发后密钥进入保管流（Keychain + 元数据），不在 UI 长期展示明文
- 作废需确认；与「最近删除」语义协调（以 tasks/spec 为准）

不要实现中转签发（那是 V3）。汇报能力矩阵与置灰文案。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后：

1. 打开**同目录**完整上传提示词：[`P05-save.md`](./P05-save.md)  
2. 把该文件里「请为」起的整段复制给 Cursor（会写 `BRANCHES.md` 英+中备注，在分支 `v0.2` 上 commit，打 tag **`v0.2.5`** 并 push）

不要把上传步骤粘进本次实现对话。
