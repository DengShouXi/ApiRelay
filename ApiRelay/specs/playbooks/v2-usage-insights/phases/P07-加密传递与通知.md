# V2 · Phase 7 — 加密传递与通知（实现提示词）

---

请实现 US9 加密传递 + 三类本地通知。

## 加密传递

- 复用 V1 备份格式的 `purpose=transfer` 与 `scope`
- 经系统分享面板；**不经服务器、不可撤回**，须如实告知
- 门闩策略按 spec（通常不可关闭类）

## 通知

- 余额不足 / 密钥失效 / 每周摘要
- 设置项在 V2 出现（V1 故意未放的 FR-021b）

不要实现可撤销的「给他人中转密钥」（V3）。汇报披露文案与通知触发条件。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后：

1. 打开**同目录**完整上传提示词：[`P07-save.md`](./P07-save.md)  
2. 把该文件里「请为」起的整段复制给 Cursor（会写 `BRANCHES.md` 英+中备注，在分支 `v0.2` 上 commit，打 tag **`v0.2.7`** 并 push）

不要把上传步骤粘进本次实现对话。
