# V2 · Phase 2 — 管理凭证与四平台适配器（实现提示词）

---

请**只实现**管理类高权限凭证配置 + OpenRouter / OpenAI / Anthropic / DeepSeek 四个 Adapter。

## 要点

- Admin / Management Key：门闩不可关闭；风险明确披露
- 各 Adapter 按其真实能力声明矩阵；不支持的能力不得假装支持
- 自动刷新读取管理凭证 **MUST NOT** 触发门闩（显式命名如 `readCredentialForAutomatedRefresh`）
- 明文仍不得进入可观察状态 / 日志

不要做完整看板 UI（下一阶段）。汇报各平台能力矩阵表。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后，按顺序另开操作：

1. 打开并粘贴提示词：`../05-annotate-branch.md`（更新 `BRANCHES.md` 英+中备注）
2. 再打开并粘贴提示词：`../15-phase-push.md`（在分支 `v0.2` 上 commit，打 **tag `v0.2.2`**，再 `git push`）

说明：第三层是 **tag `v0.2.2`**，不是新建分支；人继续留在 `v0.2` 做下一 Phase。
