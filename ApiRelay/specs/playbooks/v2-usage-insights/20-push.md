# V2 — 上传（push）提示词

---

请上传 **V2 分支 `002-usage-insights`**。

1. 展示 status/diff/log；经我确认后再 commit  
2. `git push -u origin HEAD`（如需要）  
3. 可选：开 PR 合并 `main` 并打 tag（需明确授权）  
4. 禁止 force push；不要开始 V3 代码  

完成后提示 `30-create-next-branch.md`。
