# V2 — 每个小 Phase 上传提示词

先完成 [`05-annotate-branch.md`](./05-annotate-branch.md)，再贴本段。

---

请上传 V2 小阶段里程碑：`v0.2.N`（N = 刚完成的 Phase 编号）。

步骤：展示 status/diff → 确认 BRANCHES 备注 → 经我同意后 commit → `git push -u origin v0.2.N` → 报告 hash。禁止 force push；不要删 `v0.1.*` / `v0.0.*`。
