# v1.11 App Icon + SF Symbol / 资源验收清单（人工）

对照 [P11-AppIcon与资源.md](./P11-AppIcon与资源.md)。

## App Icon

- [ ] iOS `AppIcon`：**Any / Dark / Tinted** 三槽均有 1024 图（非空槽）
- [ ] Mac idiom：16 / 32 / 128 / 256 / 512 的 1x 与 2x 均有图
- [ ] 主屏 / Dock：**16～32 等效尺寸**仍能看出「钥匙/保险库」剪影
- [ ] 图标内无文字、无第三方品牌标
- [ ] `AccentColor` 与图标主色同源（视觉不打架）

## SF Symbol 语义

- [ ] 存在单一映射源（如 `AppSymbols` / `PresetCatalog` 扩展）；View 内无大面积散落魔法字符串
- [ ] 四 Tab 符号与设计表一致：`square.stack.3d.up` / `laptopcomputer` / `trash` / `gearshape`
- [ ] 复制 = `doc.on.doc`，查看 = `eye`，密钥默认 = `key.fill`
- [ ] 上游平台 id → SF 隐喻表已落地；**无**商标 Logo 图
- [ ] 使用方工具 `iconSymbol` 经收口；自建工具有兜底符号
- [ ] 设置行 leading 图标语义不重复、不冲突

## Asset Catalog

- [ ] 命名符合约定；未把设计源文件塞进 `Assets.xcassets`
- [ ] 深色模式下列表 SF 与系统语义色可读；无需另套 PNG

## 商店截图草稿（预热 T065）

- [ ] 4～6 张草稿已存约定目录，文件名含 `-draft`
- [ ] 无真实密钥；无「系统级强制」「无法绕过」文案
- [ ] 目录内有一行说明：非正式，正式以 **T065** 为准

## 非回归

- [ ] P10 四 Tab IA 未改
- [ ] 门闩仍为单一设置项；ViewModel 无新增明文字段
- [ ] My Mac / Catalyst 可编译
