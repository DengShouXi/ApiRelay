# V1 · Phase 6 — US6 设置与加密备份（实现提示词）

> **历史提示词（当时 `v1.6`）。** 现行热修是 `v1.13.9`，正式规格见 [`身份验证方案.md`](../../身份验证方案.md)（宪法 v2.17.0）。本页正文可留当时完成项；不要按「导出前门闩不可关闭」或「验证方式单一字段管查看+复制」或「组合档可未设密保存」或「始终可见应用主密码行」写新代码——那些已被 13.9 / U1 / U2 / U11 方案覆盖。

把下面整段复制给 Cursor，一次只做这一阶段。

---

请**只实现 V1 Phase 6（设置与加密备份）**。V2 设置项（刷新间隔、汇率、单价、提醒）不显示占位入口。

## 必读

- `tasks.md` → **Phase 6**（T045–T050）
- DC-021：安全偏好同步 / 外观与默认视角仅本机
- 备份格式预留 `purpose`（fullBackup / transfer）与 `scope`（给 V2 加密传递用）

## 本阶段要点

- PreferencesService 聚合 UserPreferences + DevicePreferences
- SecureBackup：AES-GCM；仅密文落盘。当时要求「导出前门闩不可关闭」——**已被 13.9 覆盖**：不验证时导出可直达；其他档按当前验证方式。现行见 [`身份验证方案.md`](../../身份验证方案.md)
- App 锁、自动锁定、切换器遮罩。**现行（FR-021）**：自动锁定与剪贴板自动清除是设置首页各一行子页（开关 + 可增删时长列表），MUST NOT 做成首页开关+步进器或不可删固定档。剪贴板自动清除可关。
- V1 设置项清单（含清除全部数据）。当时「验证方式单一开关管辖查看+复制」——**已被 13.9 覆盖**：三行是验证方式 / 自动锁定 / 取用验证；查看与复制共用取用验证，不得拆成两个开关
- eraseAllUserData：二次确认、清 Keychain/SwiftData/本机偏好；不吊销 StoreKit

## 硬约束

- 改外观不得写入 CloudKit 同步实体（单测专项）
- Checkpoint 6 通过后继续

汇报：设置持久化与清除全部数据路径（SC-014）。


---

## 本阶段完成后（这段不要复制进实现对话）

Checkpoint 通过后：

1. 打开同目录小迭代上传提示词：[`P06-save.md`](./P06-save.md)  
2. 复制其中「请为」起的整段给 Cursor  
   → 写 `BRANCHES.md`（英→中），推送到小迭代分支 **`v1.6`**

测试只放：`ApiRelay/ApiRelayTests/V1/Phase06_Settings/`  
临时调试：`ApiRelay/DebugScratch/`（不上传）
