# V1 密钥保险库 · 小迭代 Phase 12 — 保存并上传提示词

> **硬规则**：本小阶段内容写完后，必须执行本文件，打小迭代分支并 `git push`。备注必须 English → 简体中文。

**把下面从「请为」开始到文末整段复制给 Cursor。**  
本对话只做：写备注 + commit + 推送到**小迭代分支**。不要继续写实现代码。

---

请为 **V1 密钥保险库 · Phase 12（本地化验收与补漏）** 保存本小迭代并上传。

## 固定目标（写错迭代名视为失败）

| 项 | 必须使用的值 |
|----|----------------|
| 大阶段分支（第 2 层） | `v1` |
| **本小迭代分支（第 3 层 · 本次上传目标）** | `v1.12` |
| 上一小迭代（已完成） | `v1.11` / 热修 `v1.11.1` |
| 下一继续开发 | Stage1 上架材料（T063–T066）或产品安排的后续小修 |
| 远程 | `origin` |
| 台账 | `ApiRelay/specs/playbooks/BRANCHES.md` |
| 实现提示词（对照，勿在本对话实现） | `phases/P12-本地化验收.md` |
| 验收 | `phases/P12-验收清单.md` |

## A. 更新迭代备注（必须英 → 中）

编辑 `BRANCHES.md`，为小迭代 **`v1.12`** 写入/更新 English + 简体中文（本地化验收、Catalog 补漏、硬编码清零；不含商店元数据）。

## B. 提交并推到小迭代分支 `v1.12`

1. 确保在 `v1.12` 上工作。  
2. 暂存本 Phase 实现 + playbook + `BRANCHES.md` + `00-README.md`。  
3. **禁止**加入：`build/`、`DebugScratch/`、`00.md`、密钥、`.env`、设计源大文件。  
4. message：`fix(v1.12): localization audit and String Catalog gaps`  
5. `git push -u origin v1.12`  
6. 同步大阶段线：`v1` merge/`reset --soft` 对齐 `v1.12` tip 后 push（按仓库现行习惯前进 tip，勿回退）。  

## C. 完成汇报

- 小迭代 `v1.12` @ `<hash>` 已在远程  
- 上一步：`v1.11` / `v1.11.1`  
- 下一步：Stage1 上架材料（**T063–T066**）或 `10-verify`
