# V1 · Phase 9 — 可用性加固 + T014b Checkpoint 2b（实现提示词）

把下面整段复制给 Cursor，一次只做这一阶段。

> **时间线说明（补档）**：本 Phase 对应小迭代分支 **`v1.9`**（历史上曾用旧名 `v0.1.9`）。  
> 主要提交：  
> - `7c77778` — vault UX 加固（多钥、回收站、双视角流）  
> - `32318c2` — 关闭 T014b CloudKit Production Checkpoint 2b  
> Playbook 目录曾漏建 Phase 9 文件；本文件为**补全台账与实现对照**，不是要求重做已落地代码。

---

请**只实现 / 核对 V1 Phase 9（可用性加固 + T014b）**。不要顺手做 Phase 10 UI 重设计。

## 必读

- `BRANCHES.md` → `v1.9` 备注
- `tasks.md` → **T014b / Checkpoint 2b**
- 本目录验收：[`P09-验收清单.md`](./P09-验收清单.md)

## 本阶段要点（相对 v1.8）

### A. 可用性加固（UX）

- 多钥录入 / 查看 / 删除路径稳定
- **软删除回收站**：密钥 / 上游账号 / 使用方工具；约 **30 天**后可 purge（`deletedAt` / `purgeAfter`）
- 按平台 vs 按使用方流程更清晰
- 中文错误文案（`ApiRelayError` + `Localizable`）
- DEBUG 默认可本机 SwiftData（便于开发）

### B. T014b Checkpoint 2b（同步硬门槛）

- CloudKit **Production** 完成 8 表与 §7.1 预留字段 Deploy 核对
- DEBUG：`CloudKitSchemaBootstrap` 一次性逼可选字段 schema（不进 Release 常态路径）
- Checkpoint 2b 通过后，才允许进入后续上架同步相关工作与 Phase 10

## 硬约束

- 不改门闩语义（查看+复制仍单一设置项）
- 明文不进 SwiftData / 可观察 UI 状态 / 日志
- 不得把本阶段做成「重做 UI 外壳」（那是 Phase 10）

## 汇报

- 回收站软删除与 purge 字段是否落库  
- T014b / Checkpoint 2b 是否已关闭  
- 测试目录：`ApiRelayTests/V1/Phase09_UXHardening/`

---

## 本阶段完成后（这段不要复制进实现对话）

1. 打开同目录：[`P09-save.md`](./P09-save.md)  
2. 推送到小迭代分支 **`v1.9`**  
3. 下一实现阶段：[`P10-UI重设计.md`](./P10-UI重设计.md) → `v1.10`
