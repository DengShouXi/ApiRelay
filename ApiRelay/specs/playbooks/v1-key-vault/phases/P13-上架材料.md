# V1 · v1.13 — Stage1 上架材料（实现提示词）

**用法**：把下方「复制给 Cursor 的整段」原样粘贴进对话。一次只做本阶段。  
**配套人工验收**：[`P13-验收清单.md`](./P13-验收清单.md)（ASC 填写与截图真机操作以你为主；Agent 可起草文案与工程核对）。  
**你亲自跟着做（零基础）**：[`P13-人工操作指南/00-README.md`](./P13-人工操作指南/00-README.md)（截图、贴 ASC、隐私问卷、Archive、**TestFlight 内测**）。  
**前置**：`v1.12` 已关账；本阶段在分支 **`v1.13`** 上工作。  
**草稿目录**：[`../store-drafts/v1.13/`](../store-drafts/v1.13/)

### 在整条收尾链里的位置

```text
P12 本地化（已关）
  → 【本阶段】P13 = T063–T066（材料就绪，不上架）
       人工：截图 → ASC 文案/隐私 → Archive → TestFlight 真机自测
  → P13-save → 推 v1.13、同步 tip v1
  → ../10-verify.md（Stage1 总验收）
  → ../12-release-T062.md（须你明确授权：main + release/1.0.0 + 提交审核）
  → ../20-push.md → ../30-create-next-branch.md → ../40-checkout-next-branch.md（进 V2）
```

- **P08** 只负责 T056–T061（安全审查）；商店材料**不在** P08。  
- **Checkpoint 2b / T014b** 已在 `v1.9` 关闭；本阶段只确认未回退，不重做 Deploy。  
- **T062** 不在本阶段；材料齐 ≠ 已提交审核。  
- **推荐**：T062 送审前完成 [`P13-人工操作指南/06-TestFlight内测.md`](./P13-人工操作指南/06-TestFlight内测.md)；不要跳过真机内测直接发布。

---

## 复制给 Cursor 的整段（从下一行开始）

请按 **`/speckit-implement`** 的纪律执行 **V1 小迭代 `v1.13`（Stage1 上架材料 T063–T066）**。  
本阶段**不是**开 V2、**不是**改密钥库信息架构、**不是**执行 T062（合并 `main` / 打 `release/1.0.0` / 提交审核——须另开对话**明确授权**，见 `12-release-T062.md`）。

### 0. Spec Kit 前置（必须先跑命令，再改文件）

在仓库根目录（含 `ApiRelay/.specify/` 的那一层）依次执行，并把 JSON / 输出贴进汇报：

```bash
# 0a. 确认当前分支就是 v1.13
git rev-parse --abbrev-ref HEAD
# 期望：v1.13

# 0b. Spec Kit 前置检查（有 tasks 才继续）
python3 ApiRelay/.specify/scripts/python/check_prerequisites.py --json --require-tasks --include-tasks
```

然后：

1. 解析 `FEATURE_DIR` / `AVAILABLE_DOCS`（路径一律用绝对路径）。  
2. 若 `FEATURE_DIR/checklists/` 存在：统计各清单 `[ ]` / `[x]`，按 `/speckit-implement` 规则决定是否暂停询问。  
3. **必读**（按顺序打开，读完再动手）：  
   - `ApiRelay/.specify/memory/constitution.md` → 安全文案 / 披露 / 本地化相关节  
   - `ApiRelay/specs/001-key-vault/spec.md` → **FR-024、FR-028、FR-033、FR-044、FR-045、FR-058**；**SC-010**；**DC-009**（DC-013 已废止）；**DC-027**（2b 已完成，本阶段只防回退）  
   - `ApiRelay/specs/001-key-vault/tasks.md` → Phase 8 **执行序**说明；**T063～T066**（本阶段）；**T062**（只列前置，不执行）；确认 **T014b / Checkpoint 2b** 已勾  
   - `ApiRelay/specs/001-key-vault/quickstart.md` → **§9** 上架前补充检查；可选复验 **§1.1b**（FR-025 / SC-001）  
   - `ApiRelay/specs/playbooks/BRANCHES.md` → `v1.13` 行（开工前可为「未开分支」）  
   - `ApiRelay/specs/playbooks/v1-key-vault/store-drafts/v1.11/`（T065 底稿）  
   - 本文件 + [`P13-验收清单.md`](./P13-验收清单.md)  
4. 若存在 `.specify/extensions.yml` 的 `hooks.before_implement`：按 skill 规则处理。

### 1. 本阶段目标（对照 tasks）

| 目标 | 任务 | 说明 |
|------|------|------|
| ASC 元数据 + 商店描述（en + zh-Hans） | **T063** | 卖点仅限保管 / 分发 / 权益；**禁止**提中转、用量看板、探活 |
| 隐私问卷 + 产品内隐私说明对齐 | **T064** | 如实声明 iCloud 钥匙串同步明文等；与 T059b 法律文本一致、禁机翻矛盾 |
| 商店截图定稿 | **T065** | 无真实密钥；禁「系统级强制」「无法绕过」；可以 `store-drafts/v1.11` 为底 |
| 提交前工程/元数据核对 | **T066** | 钉 `MARKETING_VERSION = 1.0.0`（开发期可为 `1.0`，**提交前**必须钉死，见 BRANCHES）；IAP id `com.apirelay.iap.unlimited_keys`；恢复购买可达；`PrivacyInfo.xcprivacy` + 加密出口声明已在工程内；**推荐** Archive 上传后经 TestFlight 真机自测 |
| 不在本阶段 | **T062** | 见 [`../12-release-T062.md`](../12-release-T062.md)；送审前应已 TestFlight 自测 OK |

### 2. 允许改动的文件面（越界即停）

| 允许 | 禁止 |
|------|------|
| `ApiRelay/specs/playbooks/v1-key-vault/store-drafts/**` | 改 Tab 信息架构 / 门闩语义 / Keychain / 付费业务逻辑 |
| `MARKETING_VERSION`（钉 `1.0.0`）及直接相关的工程版本字段 | 未授权执行 T062（push `main`、打 `release/*`、ASC「提交审核」） |
| 产品内**已有**隐私说明入口的文案对齐（若缺入口，只报告，不新开大功能） | 开 V2、探活、用量、中转文案或功能；重做 P10 / 整 App 重翻 |
| 本目录 `P13-*.md`、`v1-key-vault/00-README.md`、`BRANCHES.md`（save 时） | 修改宪法 / 重排 FR 编号；把商店材料写回 P08 |

### 3. 实现顺序（严格）

1. 跑完 §0；确认在 `v1.13`（若尚未开分支：从已关账的 `v1` tip 拉 `v1.13`，经你确认后再建）。  
2. **T066 工程闸门先核对**（可改则改，不可改则记入验收清单待你操作）：  
   - `MARKETING_VERSION` → **`1.0.0`**  
   - `PrivacyInfo.xcprivacy`、`ITSAppUsesNonExemptEncryption` / 出口声明仍在  
   - 设置内「恢复购买」可达（FR-028）  
   - IAP 产品 ID = `com.apirelay.iap.unlimited_keys`  
3. **T063**：起草 ASC 元数据（副标题、推广文本、描述、关键词）**英语 + 简体中文** → `store-drafts/v1.13/`；卖点边界按 SC-010。  
4. **T064**：对照 T059b 法律文本，写隐私问卷对齐表；缺漏只补说明文案，不编造收集行为。  
5. **T065**：基于 `store-drafts/v1.11` 产出正式截图集（假数据/掩码）；中英各一套或一套图 + 双语说明。  
6. 更新 [`P13-验收清单.md`](./P13-验收清单.md) 中 Agent 可勾项；提醒你完成 ASC 侧人工项。  
7. **不要** commit / push（留给 [`P13-save.md`](./P13-save.md)）。  
8. 汇报：改了哪些文件、T063–T066 各自状态、明确写出「T062 未执行」。

### 4. 文案与截图存放约定

```text
ApiRelay/specs/playbooks/v1-key-vault/store-drafts/v1.13/
  README.md                 # 正式材料目录说明
  metadata-en.md            # T063 英语
  metadata-zh-Hans.md       # T063 简体中文
  privacy-questionnaire.md  # T064 问卷对齐表（非机翻法律正文）
  screenshots/              # T065 定稿图（文件名带 locale）
```

- 法律正文若已在别处（T059b），本阶段**只引用路径 + 对齐检查**，禁止用机翻另起炉灶。  
- 截图可用 Simulator / Catalyst；界面须为 Release 或接近上架外观（勿露 DEBUG 覆盖入口文案）。

### 5. 硬约束

- 一次只改本阶段相关文件；顺路优化只报告不动手。  
- 明文红线、门闩单一设置项不变。  
- 商店与安全文案禁止「系统级强制」「无法绕过」。  
- **不得**在描述/截图/关键词中承诺未交付能力（中转、用量看板、探活）。  
- T062 必须另授权；本阶段结束只表示「材料就绪」。

---

## 本阶段完成后（这段不要复制进实现对话）

1. 你本人按 [`P13-人工操作指南/00-README.md`](./P13-人工操作指南/00-README.md) 做完：截图 → ASC 文案/隐私 → Archive → **TestFlight 内测**，再按 [`P13-验收清单.md`](./P13-验收清单.md) 勾完。  
2. 打开 [`P13-save.md`](./P13-save.md) → 上传 **`v1.13`**，同步 tip **`v1`**。  
3. 打开 [`../10-verify.md`](../10-verify.md) 做 Stage1 总验收。  
4. 总验收通过且 TestFlight 自测 OK 后，**另开对话**并明确授权 → [`../12-release-T062.md`](../12-release-T062.md)（可选用已测构建送审）。  
5. **不要**在材料阶段执行 `30-create-next-branch.md` 开 `v2` 实现线。  
6. **不要**跳过 TestFlight 直接点 ASC「提交以供审核」。
