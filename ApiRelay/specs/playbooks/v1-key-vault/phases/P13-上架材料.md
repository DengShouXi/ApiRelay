# V1 · v1.13 — Stage1 上架材料（实现提示词）

**用法**：把下方「复制给 Cursor 的整段」原样粘贴进对话。一次只做本阶段。  
**配套人工验收**：[`P13-验收清单.md`](./P13-验收清单.md)（ASC 填写与截图真机操作以你为主；Agent 可起草文案与工程核对）。  
**前置**：`v1.12` 已关账；本阶段在分支 **`v1.13`** 上工作。

---

## 复制给 Cursor 的整段（从下一行开始）

请按 **`/speckit-implement`** 的纪律执行 **V1 小迭代 `v1.13`（Stage1 上架材料 T063–T066）**。  
本阶段**不是**开 V2、**不是**改密钥库信息架构、**不是**执行 T062（合并 `main` / 打 `release/1.0.0` / 提交审核——须你另开对话**明确授权**）。

### 0. Spec Kit 前置（必须先跑命令，再改文件）

在仓库根目录（含 `ApiRelay/.specify/` 的那一层）依次执行，并把 JSON / 输出贴进汇报：

```bash
# 0a. 确认当前分支就是 v1.13
git rev-parse --abbrev-ref HEAD
# 期望：v1.13

# 0b. Spec Kit 前置检查（有 tasks 才继续）
python3 ApiRelay/.specify/scripts/python/check_prerequisites.py --json --require-tasks --include-tasks
```

然后：

1. 解析 `FEATURE_DIR` / `AVAILABLE_DOCS`（路径一律用绝对路径）。  
2. 若 `FEATURE_DIR/checklists/` 存在：统计各清单 `[ ]` / `[x]`，按 `/speckit-implement` 规则决定是否暂停询问。  
3. **必读**（按顺序打开，读完再动手）：  
   - `ApiRelay/.specify/memory/constitution.md` → 安全文案 / 披露 / 本地化相关节  
   - `ApiRelay/specs/001-key-vault/spec.md` → **FR-024、FR-028、FR-033、FR-044、FR-045、FR-058**；**SC-010**；**DC-009**（DC-013 已废止）  
   - `ApiRelay/specs/001-key-vault/tasks.md` → **T063～T066**（本阶段交付）；**T062**（只列前置，不执行）；确认 **T014b / Checkpoint 2b** 已勾  
   - `ApiRelay/specs/001-key-vault/quickstart.md` → §9 上架前补充检查  
   - `ApiRelay/specs/playbooks/BRANCHES.md` → `v1.13`  
   - `ApiRelay/specs/playbooks/v1-key-vault/store-drafts/v1.11/`（T065 底稿）  
   - 本文件 + [`P13-验收清单.md`](./P13-验收清单.md)  
4. 若存在 `.specify/extensions.yml` 的 `hooks.before_implement`：按 skill 规则处理。

### 1. 本阶段目标（对照 tasks）

| 目标 | 任务 | 说明 |
|------|------|------|
| ASC 元数据 + 商店描述（en + zh-Hans） | **T063** | 卖点仅限保管 / 分发 / 权益；**禁止**提中转、用量看板、探活 |
| 隐私问卷 + 产品内隐私说明对齐 | **T064** | 如实声明 iCloud 钥匙串同步明文等；与 T059b 法律文本一致、禁机翻矛盾 |
| 商店截图定稿 | **T065** | 无真实密钥；禁「系统级强制」「无法绕过」；可以 `store-drafts/v1.11` 为底 |
| 提交前工程/元数据核对 | **T066** | `MARKETING_VERSION = 1.0.0`；IAP id `com.apirelay.iap.unlimited_keys`；恢复购买可达；`PrivacyInfo.xcprivacy` + 加密出口声明已在工程内 |
| 不在本阶段 | **T062** | 合并 `main` / tag `release/1.0.0` / 上传构建——**仅当你明确授权**时另做 |

### 2. 允许改动的文件面（越界即停）

| 允许 | 禁止 |
|------|------|
| `ApiRelay/specs/playbooks/v1-key-vault/store-drafts/**`（截图与文案草稿/定稿） | 改 Tab 信息架构 / 门闩语义 / Keychain / 付费业务逻辑 |
| 商店文案草稿目录（本 playbook 约定路径，见 §4） | 开 V2、探活、用量、中转文案或功能 |
| `MARKETING_VERSION`（钉 `1.0.0`）及直接相关的工程版本字段 | 未授权执行 T062（push `main`、打 `release/*`、ASC 点「提交审核」） |
| 产品内**已有**隐私说明入口的文案对齐（若缺入口，只报告，不新开大功能） | 重做 P10 UI / 整 App 重翻 |
| 本目录 `P13-*.md`、`v1-key-vault/00-README.md`、`BRANCHES.md`（save 时） | 修改宪法 / 重排 FR 编号 |

### 3. 实现顺序（严格）

1. 跑完 §0；确认在 `v1.13`。  
2. **T066 工程闸门先核对**（可改则改，不可改则记入验收清单待你操作）：  
   - `MARKETING_VERSION` 现为 `1.0` → 钉为 **`1.0.0`**（T066）  
   - 确认 `PrivacyInfo.xcprivacy`、`ITSAppUsesNonExemptEncryption` / 出口声明仍在  
   - 确认设置内「恢复购买」入口可达（FR-028）  
   - 确认 IAP 产品 ID 与 `com.apirelay.iap.unlimited_keys` 一致  
3. **T063**：起草 ASC 元数据（App 名称副标题、推广文本、描述、关键词）**英语 + 简体中文**，写入约定草稿文件；卖点边界按 SC-010。  
4. **T064**：对照 T059b 法律文本，列出隐私问卷应勾选项与产品内说明对齐表；缺漏只补说明文案，不编造收集行为。  
5. **T065**：基于 `store-drafts/v1.11` 产出正式截图集（假数据/掩码；无真实密钥）；中英各一套或一套图 + 双语说明（按 ASC 实际需要）。  
6. 更新 [`P13-验收清单.md`](./P13-验收清单.md) 中 Agent 可勾项；提醒你完成 ASC 侧人工项。  
7. **不要** commit / push（留给 [`P13-save.md`](./P13-save.md)）。  
8. 汇报：改了哪些文件、T063–T066 各自状态、明确写出「T062 未执行」。

### 4. 文案与截图存放约定

```text
ApiRelay/specs/playbooks/v1-key-vault/store-drafts/v1.13/
  README.md                 # 说明：正式材料目录；对应 T063–T065
  metadata-en.md            # T063 英语元数据
  metadata-zh-Hans.md       # T063 简体中文元数据
  privacy-questionnaire.md  # T064 问卷对齐表（非机翻法律正文）
  screenshots/              # T065 定稿图（文件名带 locale）
```

- 法律正文若已在别处（T059b），本阶段**只引用路径 + 对齐检查**，禁止用机翻另起炉灶。  
- 截图可用 Simulator / Catalyst；界面须为 Release 或接近上架外观（勿露 DEBUG 覆盖入口文案）。

### 5. 硬约束

- 一次只改本阶段相关文件；顺路优化只报告不动手。  
- 明文红线、门闩单一设置项不变。  
- 商店与安全文案禁止「系统级强制」「无法绕过」。  
- **不得**在描述/截图/关键词中承诺未交付能力（中转、用量看板、探活）。  
- T062 必须另授权；本阶段结束只表示「材料就绪」，不表示「已提交审核」。

---

## 本阶段完成后（这段不要复制进实现对话）

1. 你本人按 [`P13-验收清单.md`](./P13-验收清单.md) 勾完 ASC / 截图 / 版本号。  
2. 打开 [`P13-save.md`](./P13-save.md) → 上传 **`v1.13`**，同步 tip **`v1`**。  
3. 然后按需：`10-verify.md` →（你明确授权后）**T062** → tag `release/1.0.0`。  
4. **不要**在本阶段执行 `30-create-next-branch.md` 开 `v2`。
