# V1 · Phase 5 — US5 付费解锁（实现提示词）

把下面整段复制给 Cursor，一次只做这一阶段。

---

请**只实现 V1 Phase 5（付费解锁）**。中转权益属 V3，界面与文案不得提及中转。

## 必读

- `tasks.md` → **Phase 5**（T038–T044a）
- DC-023：产品 ID `com.apirelay.iap.unlimited_keys`；家庭共享开启；预留 `com.apirelay.iap.relay` 但不创建、不展示
- FR-028 恢复购买；FR-029 降级只读

## 本阶段要点

- EntitlementService：StoreKit 2 为真相源；EntitlementSnapshot 离线兜底
- DEBUG override；restorePurchases 入口
- 超免费额度：只读保留、可看可删、不可新增
- 付费墙仅「无限密钥」一档
- StoreKit Configuration；单测 `tier == .relay` 在 V1 不可达

## 硬约束

- 家庭共享与产品 ID 是不可逆决策，必须与 plan A5 一字不差
- MUST NOT 要求 `transaction.environment == AppTransaction.environment`（会变成扣了钱却不是会员）
- 只挡非 Xcode 包里的 `.xcode` 假交易；TestFlight `.sandbox` 必须放行
- 苹果 `.verified` 成功后，不得因本地再查档位失败而报购买失败
- MUST NOT 做界面/启动参数「一键会员」
- Checkpoint 5 通过后继续

汇报：沙盒购买 / 恢复 / 降级三条路径结果。


---

## 本阶段完成后（这段不要复制进实现对话）

Checkpoint 通过后：

1. 打开同目录小迭代上传提示词：[`P05-save.md`](./P05-save.md)  
2. 复制其中「请为」起的整段给 Cursor  
   → 写 `BRANCHES.md`（英→中），推送到小迭代分支 **`v1.5`**

测试只放：`ApiRelay/ApiRelayTests/V1/Phase05_Entitlement/`  
临时调试：`ApiRelay/DebugScratch/`（不上传）
