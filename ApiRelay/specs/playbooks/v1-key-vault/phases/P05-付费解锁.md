# V1 · Phase 5 — US5 付费解锁（实现提示词）

把下面整段复制给 Cursor，一次只做这一阶段。

---

请**只实现 V1 Phase 5（付费解锁）**。中转权益属 V3，界面与文案不得提及中转。

## 必读

- `tasks.md` → **Phase 5**（T038–T044a）
- DC-023：产品 ID `com.apirelay.iap.unlimited_keys`；家庭共享开启；预留 `com.apirelay.iap.relay` 但不创建、不展示
- FR-028 恢复购买；FR-029 降级只读

## 本阶段要点

- EntitlementService：StoreKit 2 为真相源；EntitlementSnapshot 离线兜底
- DEBUG override；restorePurchases 入口
- 超免费额度：只读保留、可看可删、不可新增
- 付费墙仅「无限密钥」一档
- StoreKit Configuration；单测 `tier == .relay` 在 V1 不可达

## 硬约束

- 家庭共享与产品 ID 是不可逆决策，必须与 plan A5 一字不差
- Checkpoint 5 通过后继续

汇报：沙盒购买 / 恢复 / 降级三条路径结果。

---

Checkpoint 通过后：先贴 [`../05-annotate-branch.md`](../05-annotate-branch.md)，再贴 [`../15-phase-push.md`](../15-phase-push.md) 上传本 Phase 里程碑分支。
