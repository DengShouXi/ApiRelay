# V1 · Phase 4 — US2 指派与双视角（实现提示词）

把下面整段复制给 Cursor，一次只做这一阶段。

---

请**只实现 V1 Phase 4（指派使用方与双视角）**。用量聚合属 V2，本阶段只做密钥列表分组。

## 必读

- `tasks.md` → **Phase 4**（T032–T037a）
- DC-011 / FR-007 / FR-008a（多对多、共享密钥计数）
- `GroupingTests` 要求：共享密钥在两个视角下去重后总数一致

## 本阶段要点

- ConsumerTool CRUD；预置项可隐藏/重命名、不可物理删除
- KeyAssignment 多对多；删除工具不级联删密钥
- `byPlatform` / `byConsumer` **共用同一分组函数**；shared 独立小计且计数一次
- 双视角 UI；共享标记；自定义上游平台界面

## 硬约束

- 不得为两个视角各写一份分组逻辑
- V1 界面仍不得出现用量/探活入口
- Checkpoint 4 通过后继续

汇报：共享密钥计数与删除工具行为是否符合 tasks Checkpoint。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后，按顺序另开操作：

1. 打开并粘贴提示词：`../05-annotate-branch.md`（更新 `BRANCHES.md` 英+中备注）
2. 再打开并粘贴提示词：`../15-phase-push.md`（在分支 `v0.1` 上 commit，打 **tag `v0.1.4`**，再 `git push`）

说明：第三层是 **tag `v0.1.4`**，不是新建分支；人继续留在 `v0.1` 做下一 Phase。
