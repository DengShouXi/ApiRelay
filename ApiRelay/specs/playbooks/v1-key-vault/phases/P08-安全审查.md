# V1 · Phase 8 — 安全审查与收尾（实现提示词）

把下面整段复制给 Cursor，一次只做这一阶段。

---

请**只执行 V1 Phase 8（安全审查与上架准备中的工程侧）**。商店元数据/隐私问卷/截图推迟到 V2（DC-013），但工程内 PrivacyInfo 与加密声明应已在 Phase 1 完成。

## 必读

- `tasks.md` → **Phase 8**（T056–T062）
- `quickstart.md` 全量（跳过标注为 V2 的条目）
- 宪法 IX：不得声称「系统级强制」「无法绕过」

## 本阶段要点

- 明文泄露排查（SwiftData / CloudKit / 日志 / maskedHint）
- KeychainStore 无 ACL；masterpw 不同步
- 文案与风险披露审查
- 本地化验收（英/简中/未覆盖语言回退英语）
- 法律文本人工双语（禁机翻）
- 无障碍：Dynamic Type、VoiceOver；掩码不读全文；门闩后可逐字符
- quickstart 人工走一遍
- **T062：合并回 main 并打 tag**（若你要求我执行合并/打 tag，请在对话里明确授权）

## 硬约束

- 安全清单未全绿不得宣称 V1 完成
- 完成后进入版本级 `10-verify.md`

汇报：清单逐项结果 + 是否建议执行 verify / push。


---

## 本阶段完成后（这段不要复制进实现对话）

实现 Checkpoint 通过后，按顺序另开操作：

1. 打开并粘贴提示词：`../05-annotate-branch.md`（更新 `BRANCHES.md` 英+中备注）
2. 再打开并粘贴提示词：`../15-phase-push.md`（在分支 `v0.1` 上 commit，打 **tag `v0.1.8`**，再 `git push`）

说明：第三层是 **tag `v0.1.8`**，不是新建分支；人继续留在 `v0.1` 做下一 Phase。
