# V1 · Phase 1 — 工程配置（实现提示词）

把下面整段复制给 Cursor，一次只做这一阶段。

---

请按 `/speckit-implement` 的纪律，**只实现 V1 Phase 1（工程配置）**，不要进入 Phase 2 及以后。

## 必读

- `ApiRelay/specs/ROADMAP.md`（确认范围是 V1）
- `ApiRelay/specs/001-key-vault/tasks.md` → **Phase 1**（T001–T009a）
- `ApiRelay/specs/001-key-vault/plan.md` → A5（Bundle / 容器 / App Group）
- `ApiRelay/.specify/memory/constitution.md`
- `.cursor/rules/` 下三条规则（改动范围 / 架构边界 / 密钥明文红线）

## 本阶段任务（以 tasks.md 勾选为准）

- T002 / T002a：Swift 6、iOS 18 / macOS 15、Mac Catalyst、Bundle ID 统一为 `com.apirelay.ApiRelay`
- T003：单元测试 target `ApiRelayTests`
- T004：entitlements（iCloud 容器、App Group、keychain-access-groups）
- T005 / T005a：Face ID 说明、加密出口声明、PrivacyInfo、`zh-Hans` + String Catalog
- T006：目录骨架（**不要**创建 Provider / Insights / Network）
- T007–T009a：共享 Error / Diagnostics / DTO / `KeyHealthServing` 协议占位

## 硬约束

- 一次只改本阶段相关文件；发现顺路优化只报告不动手。
- 明文红线、Keychain 不设 `kSecAttrAccessControl`。
- 完成后在 `tasks.md` 把本阶段任务标为 `[X]`。
- 跑通：`xcodebuild build`（及空测试若已就绪）。
- **Checkpoint 1 未过，禁止开始 Phase 2。**

做完后用一两段话汇报：改了哪些文件、Checkpoint 是否通过。若通过，提示我打开下方「本阶段完成后」中的 save 提示词（不要把上传混进本次实现）。


---

## 本阶段完成后（这段不要复制进实现对话）

Checkpoint 通过后：

1. 打开同目录小迭代上传提示词：[`P01-save.md`](./P01-save.md)  
2. 复制其中「请为」起的整段给 Cursor  
   → 写 `BRANCHES.md`（英→中），推送到小迭代分支 **`v1.1`**

测试只放：`ApiRelay/ApiRelayTests/V1/Phase01_Setup/`  
临时调试：`ApiRelay/DebugScratch/`（不上传）
