# V1 · Phase 3 — US1 保管与取出（实现提示词）

把下面整段复制给 Cursor，一次只做这一阶段。

---

请**只实现 V1 Phase 3（保管与取出密钥）**。不要做使用方指派、付费墙、设置页全量（那些是 Phase 4–6）。

## 必读

- `tasks.md` → **Phase 3**（T015 起至 Phase 3 Checkpoint）
- `contracts/` §3.1 / §3.2（KeyVaultServing、RevealGate）
- `quickstart.md` 相关 US1 步骤
- 密钥明文红线：明文只在 Keychain / 局部作用域 / 限时剪贴板

## 本阶段要点

- 四档门闩（含主密码）；查看与复制**共用一道门闩**；不跨操作缓存确认
- 主密码：PBKDF2、恒定时间比较、`masterpw` ThisDeviceOnly 不同步；可重置并如实告知
- 剪贴板：expirationDate + localOnly；清除前校验仍是本产品写入
- KeyVault 录入/列表/掩码/查看/复制/最近删除 30 天
- 录入关闭自动更正等（DC-025）；重复录入提示
- 对应单元测试（RevealGate / MasterPassword / Clipboard / Vault）

## 硬约束

- `@Published` 等可观察状态不得持有明文；日志不得打印明文（含前几位）
- 自动类读取命名须显式（如 `readSecretForAutomatedUse`），V1 可只声明
- Checkpoint 3 通过后再开 Phase 4（或并行的 5/6）

汇报：按 quickstart US1 能否走通。


---

## 本阶段完成后（这段不要复制进实现对话）

Checkpoint 通过后：

1. 打开同目录小迭代上传提示词：[`P03-save.md`](./P03-save.md)  
2. 复制其中「请为」起的整段给 Cursor  
   → 写 `BRANCHES.md`（英→中），推送到小迭代分支 **`v1.3`**

测试只放：`ApiRelay/ApiRelayTests/V1/Phase03_Vault/`  
临时调试：`ApiRelay/DebugScratch/`（不上传）
