# 切换到大阶段分支 `v0.2` 提示词

把下面整段复制给 Cursor。

---

请切换到 **`v0.2`**，确认 `git status -sb` 与跟踪关系，并打开：

`ApiRelay/specs/playbooks/v2-usage-insights/00-README.md`

之后 V2 每个小 Phase：实现用 `phases/P0N-*.md`，保存用同目录 **`P0N-save.md`**（分支 `v0.2`，tag `v0.2.N`）。
