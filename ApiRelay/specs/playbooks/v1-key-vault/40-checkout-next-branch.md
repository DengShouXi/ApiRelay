# V1 完成后 — 切换到 V2 分支提示词

---

请切换到 `v0.2.1`，确认 `git status -sb` 干净或说明未提交项，并指向：

`ApiRelay/specs/playbooks/v2-usage-insights/00-README.md`

提醒：V2 Phase 草案以届时 `tasks.md` 为准；每完成一个小 Phase 仍走「备注 → `15-phase-push`」。
