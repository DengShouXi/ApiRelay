# V1 — 大阶段收尾核对

把下面复制给 Cursor。

---

请核对大阶段 `v0.1`：已完成的小迭代分支 `v0.1.1`…`v0.1.8` 均在远程；`BRANCHES.md` 每条有英+中备注；测试在对应 `ApiRelayTests/V1/Phase*/`。  
通过后打开 `30-create-next-branch.md` 创建大阶段 **`v0.2`**（不要预建小迭代）。禁止 force push / `v0.4`。
