# H1 · 验证地基

> **开工前必读**：同目录下的 `02-执行须知.md`。里面的五条铁律对本步骤全部生效。
> **执行模型**：便宜模型即可。本步骤全部是新建文档与脚本，内容已在下面写死，不需要任何判断。

---

## 这一步要达到什么

装上四样机器能自动跑的检查，让「不影响 App 跑起来、所以永远没人管」的问题，
变成「过不了自检」。

**本步骤 MUST NOT 修改任何 `.swift` 业务代码**，唯一允许改的 Swift 文件是第 2 步里那**一行注释**。

## 前置条件

无。这是第一步。

## 工作目录

所有命令与路径都以**仓库根**为基准，即包含 `.cursor/` 和 `ApiRelay/` 的那一层：

```
/Users/xitongzhili/Desktop/MacTransfer/08_Code/E02_ApiRelay_Github
```

先确认位置：

```bash
pwd && ls -d .cursor ApiRelay
```

看不到 `.cursor` 和 `ApiRelay` 两个目录就**停手报告**。

## 改哪些文件

| 文件 | 动作 |
|---|---|
| `ApiRelay/scripts/selfcheck.sh` | 新建 |
| `ApiRelay/ApiRelayTests/V1/Phase02_Data/KeychainStoreTests.swift` | 修改**一行**（加注释） |
| `.cursor/rules/implementation-closeout.mdc` | 新建 |
| `.cursor/commands/closeout.md` | 新建（`.cursor/commands/` 目录也要新建） |
| `ApiRelay/.specify/templates/spec-template.md` | 插入一节 |
| `ApiRelay/.github/agents/speckit.implement.agent.md` | 追加两行 |

## 不许碰的文件

除上表六个之外，**任何文件都不许改**。特别点名：

- 任何 `ApiRelay/ApiRelay/**` 下的 `.swift`（业务代码，本步骤一行都不该动）
- `ApiRelay/specs/**`
- `ApiRelay/.specify/memory/constitution.md`
- `ApiRelay/ApiRelay.xcodeproj/project.pbxproj`
- 其余七个已有的 `.cursor/rules/*.mdc`

---

# 第 1 步 · 新建自检脚本

新建目录 `ApiRelay/scripts/`，在其中新建 `selfcheck.sh`，内容**原样照抄**下面整段
（这段脚本已在本仓库实测通过，不要改动任何一行）：

```bash
#!/bin/bash
# ApiRelay 项目自检
#
# 用法：bash ApiRelay/scripts/selfcheck.sh
# 退出码：0 = 无红线问题；1 = 有红线问题
#
# 只做「能数出来」的检查，结论 100% 可信，不做任何需要判断的事。
# 需要判断的（这个设计有没有坑、提示用户看不看得懂）交给人或贵模型。

set -uo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
if [ ! -d "$ROOT/ApiRelay.xcodeproj" ]; then
    echo "✗ 定位失败：$ROOT 下没有 ApiRelay.xcodeproj"
    echo "  本脚本必须放在 <仓库根>/ApiRelay/scripts/selfcheck.sh"
    exit 1
fi
cd "$ROOT" || exit 1

SRC="ApiRelay"
TESTS="ApiRelayTests"
LINE_LIMIT=400
FAIL=0

echo "════════════════════════════════════════════════════"
echo " ApiRelay 项目自检    $(date '+%Y-%m-%d %H:%M')"
echo " 目录：$ROOT"
echo "════════════════════════════════════════════════════"

# ─────────────────────────────────────────────
# 红线 1：测试不得绕过 KeychainStore.makeForTests()
# ─────────────────────────────────────────────
echo ""
echo "【红线 1】测试是否绕过 KeychainStore.makeForTests()"
HITS=$(grep -rn "KeychainStore(" "$TESTS" --include="*.swift" 2>/dev/null \
        | grep -v "Support/KeychainStore+Tests.swift" \
        | grep -v "自检豁免" || true)
if [ -n "$HITS" ]; then
    echo "  ✗ 失败"
    echo "$HITS" | sed 's/^/     /'
    echo ""
    echo "     测试 bundle 由 ApiRelay.app 宿主加载，进程带的是 App 的 entitlements，"
    echo "     而 keychain-access-groups 只有一个组——直接构造会落进 App 自己那个组，"
    echo "     setUp/tearDown 的清理会删掉用户本机真实的主密码与密钥明文。"
    echo "     改用 KeychainStore.makeForTests()。"
    echo "     确有必要读生产条目时，在该行加注释：// 自检豁免: <理由>"
    FAIL=1
else
    echo "  ✓ 通过"
fi

# ─────────────────────────────────────────────
# 报告 1：过大的源文件
# ─────────────────────────────────────────────
echo ""
echo "【报告 1】超过 ${LINE_LIMIT} 行的生产源文件（越靠前越该拆）"
BIG=$(find "$SRC" -name "*.swift" -type f -exec wc -l {} + 2>/dev/null \
        | grep -v " total$" \
        | awk -v lim="$LINE_LIMIT" '$1 > lim {printf "     %6d  %s\n", $1, $2}' \
        | sort -rn)
if [ -n "$BIG" ]; then
    echo "$BIG"
    COUNT=$(echo "$BIG" | wc -l | tr -d ' ')
    echo "     —— 共 $COUNT 个。改到哪个就顺手拆哪个，不要专门开大重构。"
else
    echo "     （无）"
fi

# ─────────────────────────────────────────────
# 报告 2：被吞掉的错误
# ─────────────────────────────────────────────
echo ""
echo "【报告 2】生产代码 try? 分布（每一处都在说「这个失败可以忽略」）"
TRYQ=$(grep -rc "try?" "$SRC" --include="*.swift" 2>/dev/null \
        | awk -F: '$2 > 0 {printf "     %4d  %s\n", $2, $1}' \
        | sort -rn)
if [ -n "$TRYQ" ]; then
    echo "$TRYQ"
    TOTAL=$(echo "$TRYQ" | awk '{s+=$1} END {print s+0}')
    echo "     —— 合计 $TOTAL 处。写不出「为什么这个失败可以忽略」的，就该改成显式处理。"
else
    echo "     （无）"
fi

# ─────────────────────────────────────────────
# 报告 3：接缝有没有用起来
# ─────────────────────────────────────────────
echo ""
echo "【报告 3】每个协议的生产实现数 / 测试替身数"
echo "     替身数为 0 = 这个接缝定了但没用起来，相关测试只能用真零件"
printf "     %-30s %8s %8s\n" "协议" "生产实现" "测试替身"
for p in $(grep -rho "^protocol [A-Za-z]*" "$SRC" --include="*.swift" 2>/dev/null \
            | awk '{print $2}' | sort -u); do
    prod=$(grep -rl ":[[:space:]]*$p\b\|,[[:space:]]*$p\b" "$SRC" --include="*.swift" 2>/dev/null | wc -l | tr -d ' ')
    fake=$(grep -rl ":[[:space:]]*$p\b\|,[[:space:]]*$p\b" "$TESTS" --include="*.swift" 2>/dev/null | wc -l | tr -d ' ')
    mark=""
    [ "$fake" -eq 0 ] && mark="  ← 无替身"
    printf "     %-30s %8s %8s%s\n" "$p" "$prod" "$fake" "$mark"
done

# ─────────────────────────────────────────────
# 报告 4：运行环境判断散落处
# ─────────────────────────────────────────────
echo ""
echo "【报告 4】「现在是不是在跑测试」判断了几次（应当只有一处权威）"
ENVJ=$(grep -rn "XCTestConfigurationFilePath" "$SRC" --include="*.swift" 2>/dev/null || true)
if [ -n "$ENVJ" ]; then
    echo "$ENVJ" | sed 's/^/     /'
    N=$(echo "$ENVJ" | wc -l | tr -d ' ')
    echo "     —— 共 $N 处。多于 1 处就意味着每处各自决定测试时怎么变，迟早不一致。"
else
    echo "     （无）"
fi

# ─────────────────────────────────────────────
# 报告 5：各层代码量
# ─────────────────────────────────────────────
echo ""
echo "【报告 5】各层代码量"
for d in App UI Business Data Shared; do
    if [ -d "$SRC/$d" ]; then
        n=$(find "$SRC/$d" -name "*.swift" | wc -l | tr -d ' ')
        l=$(find "$SRC/$d" -name "*.swift" -exec cat {} + 2>/dev/null | wc -l | tr -d ' ')
        printf "     %-10s %3s 个文件  %6s 行\n" "$d" "$n" "$l"
    fi
done
tn=$(find "$TESTS" -name "*.swift" 2>/dev/null | wc -l | tr -d ' ')
tl=$(find "$TESTS" -name "*.swift" -exec cat {} + 2>/dev/null | wc -l | tr -d ' ')
tc=$(grep -rc "func test" "$TESTS" --include="*.swift" 2>/dev/null | awk -F: '{s+=$2} END {print s+0}')
printf "     %-10s %3s 个文件  %6s 行  %s 个用例\n" "测试" "$tn" "$tl" "$tc"

# ─────────────────────────────────────────────
echo ""
echo "════════════════════════════════════════════════════"
if [ "$FAIL" -eq 0 ]; then
    echo " 红线检查：全部通过"
else
    echo " 红线检查：有失败项，见上方 ✗"
fi
echo "════════════════════════════════════════════════════"
exit "$FAIL"
```

**验证 1**：

```bash
bash ApiRelay/scripts/selfcheck.sh; echo "退出码: $?"
```

**通过标准**：输出里能看到「【红线 1】」到「【报告 5】」五个小节，
其中【报告 5】显示 `UI 14 个文件 9018 行`（数字可能因后续改动略有出入，
但**必须显示出各层的文件数和行数**，不能是 0）。

此时**退出码预期是 1**，因为红线 1 会报出一处待豁免的位置——那正是第 2 步要处理的。
**看到退出码 1 属于正常，继续第 2 步。**

如果输出的是 `✗ 定位失败`，说明脚本没放对位置，检查它是不是在 `ApiRelay/scripts/` 下。

---

# 第 2 步 · 给一处合法用法加豁免注释

打开 `ApiRelay/ApiRelayTests/V1/Phase02_Data/KeychainStoreTests.swift`，找到第 52 行：

```swift
        let production = KeychainStore(accessGroup: nil)
```

改成：

```swift
        let production = KeychainStore(accessGroup: nil) // 自检豁免: 本用例专门验证测试条目在生产 Service 名下不可见，只读枚举、不写不删
```

**只加行尾注释，不许改这行的代码本身，不许改这个文件的其他任何地方。**

如果第 52 行的内容与上面不符，**停手报告**（说明文件被改过，提示词过期了）。

**验证 2**：

```bash
bash ApiRelay/scripts/selfcheck.sh > /dev/null; echo "退出码: $?"
```

**通过标准**：退出码为 `0`。

---

# 第 3 步 · 新建 Cursor 自动规则

新建 `.cursor/rules/implementation-closeout.mdc`，内容**原样照抄**：

```markdown
---
description: 实现收尾自检——防止「能跑起来就算做完」。这是本项目第一次重大事故的根因
alwaysApply: true
---

# 实现收尾自检

任何一次写了代码的对话，在向用户报告「做完了」之前，MUST 先跑自检并回答四问。
报告里缺这一节 = 没做完。

## 一、必须跑

```bash
bash ApiRelay/scripts/selfcheck.sh
```

红线项失败 MUST NOT 报告完成。报告里 MUST 附上这条命令的输出。

## 二、必须逐条回答的四问

1. **外部资源**：这次改动新增或碰到了哪些进程级共享资源（Keychain / SwiftData /
   CloudKit / UserDefaults / App Group 容器 / 文件系统 / StoreKit）？每一样在测试里怎么隔离？

   本项目第一次重大事故就漏在这一问：测试与 App 共用同一个 Keychain 组，
   `setUp` 的清理删掉了用户本机真实的主密码，表现为锁屏输入正确主密码仍提示「不正确」。

2. **失败路径**：这次加的功能有前提吗？前提不成立时，用户看到什么、怎么自己走出来？
   这个功能有几种失败，用户分得清吗？

3. **接缝**：新加的 protocol 有测试替身吗？没有的话，相关测试是不是只能用真零件？

4. **文件粒度**：这次改的文件超过 400 行了吗？超了就在报告里点名，让用户决定要不要顺手拆。
   MUST NOT 自作主张开始重构。

## 三、MUST NOT

- MUST NOT 用「测试全绿」代替上面四问。147 个测试全绿的那一天，用户被锁在 App 外面。
- MUST NOT 因为「不影响跑起来」就跳过某一问。本项目所有历史问题的共同点，
  正是「不影响跑起来，所以永远没人发现」。
- MUST NOT 把这四问的回答压缩成一句「已检查，无问题」。
```

**验证 3**：

```bash
head -4 .cursor/rules/implementation-closeout.mdc
```

**通过标准**：前四行分别是 `---`、`description: ...`、`alwaysApply: true`、`---`。

---

# 第 4 步 · 新建 Cursor 斜杠命令

`.cursor/commands/` 目录目前**不存在**，需要新建。然后在其中新建 `closeout.md`，
内容**原样照抄**：

```markdown
跑一遍项目自检，然后逐条回答收尾四问。

## 第一步

执行下面的命令，把输出**原样**贴出来，不要摘要：

```bash
bash ApiRelay/scripts/selfcheck.sh
```

红线项有失败就先停下来说明，不要继续往下答。

## 第二步

逐条回答，四问一个都不许省，不许合并成一句：

1. **外部资源**：本次改动新增或碰到了哪些进程级共享资源
   （Keychain / SwiftData / CloudKit / UserDefaults / App Group 容器 / 文件系统 / StoreKit）？
   每一样在测试里怎么隔离？

2. **失败路径**：本次加的功能有前提吗？前提不成立时用户看到什么、怎么自己走出来？
   这个功能有几种失败，用户分得清吗？

3. **接缝**：新加的 protocol 有测试替身吗？没有的话，相关测试是不是只能用真零件？

4. **文件粒度**：本次改的文件超过 400 行了吗？超了就点名，但**不要动手拆**。

## 第三步

列出「顺路发现但没有动手」的问题。没有就写「无」。
```

**验证 4**：

```bash
ls -1 .cursor/commands/
```

**通过标准**：输出里有 `closeout.md`。

---

# 第 5 步 · 需求模板加「前提与失败预案」节

打开 `ApiRelay/.specify/templates/spec-template.md`。

找到下面这段（在文件中部，`### Edge Cases` 那一节的末尾、`## Requirements` 之前）：

```markdown
- What happens when [boundary condition]?
- How does system handle [error scenario]?

## Requirements *(mandatory)*
```

在 `- How does system handle [error scenario]?` 与 `## Requirements *(mandatory)*` 之间
**插入**下面整段（保留前后各一个空行）：

```markdown
### 前提与失败预案 *(mandatory)*

<!--
  ACTION REQUIRED: 每条有前提的需求都必须在这里回答三问。
  本节是 2026-08-22「测试删除主密码」事故的整改产物——功能有前提，前提没了没人管，
  用户被锁在 App 外面走不出来。漏答这三问 = 需求没写完。
-->

**三问**：这个功能的前提是什么？前提不成立时用户怎么自己走出来？谁能破坏这个前提？

| 前提 | 前提不成立时会怎样 | 用户如何自己走出来 | 谁能破坏这个前提 |
|---|---|---|---|
| [例：本机已存主密码] | [例：解锁页永远验不过，用户被锁在外面] | [例：走「忘记主密码」，设备主人验证后清掉并落回生物识别] | [例：测试代码、另一台设备的同步、用户在别处重置] |

**失败要分得清**：不同的失败原因，用户看到的提示必须能区分。

| 失败原因 | 用户看到什么 | 用户该做什么 |
|---|---|---|
| [例：密码输错] | 「主密码不正确」 | 重试 |
| [例：本机根本没存主密码] | 「本机没有主密码」+ 恢复入口 | 走恢复流程 |
```

**不许改动这个文件的其他任何部分。**

**验证 5**：

```bash
grep -c "前提与失败预案" ApiRelay/.specify/templates/spec-template.md
grep -n "## Requirements" ApiRelay/.specify/templates/spec-template.md
```

**通过标准**：第一条输出 `1`；第二条输出的行号**大于**插入前的 81
（说明新内容插在了 `## Requirements` 之前，而不是之后）。

---

# 第 6 步 · Copilot 侧也装一道

打开 `ApiRelay/.github/agents/speckit.implement.agent.md`，找到文件**最末尾**的这一段：

```markdown
## Done When

- [ ] All tasks in tasks.md completed and marked `[X]`
- [ ] Implementation validated against specification, plan, and test coverage
- [ ] Extension hooks dispatched or skipped according to the rules in Mandatory Post-Execution Hooks above
- [ ] Completion reported to user with summary of completed work
```

在最后一行**之后**追加两行：

```markdown
- [ ] 已执行 `bash ApiRelay/scripts/selfcheck.sh`，红线项全部通过，输出已附在完成报告里
- [ ] 已按 `.cursor/rules/implementation-closeout.mdc` 逐条回答收尾四问（外部资源 / 失败路径 / 接缝 / 文件粒度）
```

**不许改动这个文件的其他任何部分。**

**验证 6**：

```bash
tail -3 ApiRelay/.github/agents/speckit.implement.agent.md
```

**通过标准**：能看到刚追加的两行。

---

# 全步骤完成后的总验证

依次跑下面三条，三条都要满足：

```bash
# 1. 自检通过
bash ApiRelay/scripts/selfcheck.sh > /dev/null; echo "自检退出码: $?"

# 2. 只动了该动的文件
git status --short

# 3. 业务代码一行没动
git diff --stat -- 'ApiRelay/ApiRelay/**/*.swift'
```

**通过标准**：

1. 自检退出码为 `0`。
2. `git status --short` 列出的**新增/修改**只应包含：
   - `ApiRelay/scripts/selfcheck.sh`（新增）
   - `.cursor/rules/implementation-closeout.mdc`（新增）
   - `.cursor/commands/closeout.md`（新增）
   - `ApiRelay/.specify/templates/spec-template.md`（修改）
   - `ApiRelay/.github/agents/speckit.implement.agent.md`（修改）
   - `ApiRelay/ApiRelayTests/V1/Phase02_Data/KeychainStoreTests.swift`（修改）
   - `ZL01_具体说明/`、`ZL02_研发档案/`（新增，中文导航与本方案文档，属正常）
   - `README.md`、`ApiRelay/specs/playbooks/重要说明/索引.md`（各加了一行导航链接）
   - 以及执行前工作区里**已经存在**的未提交改动（不要去动它们）

   出现清单外的文件 = 越界了，**停手报告**。

3. 第 3 条命令**必须没有输出**（业务代码一行都不该改）。有输出就是越界，停手报告。

---

# 完成后报告什么

按 `02-执行须知.md` 第六节的格式报告，另外**必须附上**：

- `bash ApiRelay/scripts/selfcheck.sh` 的**完整输出**（不要摘要）
- `git status --short` 的完整输出

---

# 不要做的事

- **不要**顺手修自检报告里列出的任何问题。报告里那些大文件、`try?`、无替身的协议，
  是留给后面 `H2` / `H3` / `H4` 处理的。**这一步只装工具，不修问题。**
- **不要**提交（`git commit` / `git push`），见 `02-执行须知.md` 铁律 3。
- **不要**因为自检报告「看起来很吓人」就扩大改动范围。
