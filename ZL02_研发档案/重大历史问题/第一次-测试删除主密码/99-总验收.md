# 99 · 总验收

> **开工前必读**：同目录 `02-执行须知.md`。  
> **执行模型**：**贵模型**（判断「改**对**了没有」，不是「改**完**了没有」）。  
> **前置**：`H1`～`H4` 均已执行并上传。  
> **本步默认不 commit**；负责人说「上传」再入库（可与台账/进度勾选一并）。

---

## 这一步要达到什么

确认整改线 **H1→H4 做对了**：机制在、死局有出口、机器能拦住同类事故。  
**不是**重新实现功能，也不是开新需求。

---

## 通过 / 失败标准

- **通过**：下列每一节的「机器验」全部 ✓，且「对不对」判断无红灯。  
- **失败**：任一红线命令失败，或发现「写了但接错 / 测绿但用户仍会卡死」。停手写明原文，**不许**自行开补丁掩盖。

---

# A · H1 验证地基

| 检查项 | 怎么验 | 期望 |
|---|---|---|
| 自检脚本 | `bash ApiRelay/scripts/selfcheck.sh` | 退出码 0；红线「测试绕过 makeForTests」通过 |
| 收尾规则 | 存在 `.cursor/rules/implementation-closeout.mdc` | 含「四问」与 `selfcheck.sh` |
| 收尾命令 | 存在 `.cursor/commands/closeout.md` | 指向 closeout |
| 需求模板 | `ApiRelay/.specify/templates/spec-template.md` | 含「前提与失败」类章节 |
| Keychain 豁免注释 | `KeychainStoreTests` 含自检豁免说明 | 生产前缀回归用例合法 |

**对不对**：H1 **不得**改业务行为；若发现业务逻辑被 H1「顺手」改掉 → 失败。

---

# B · H2 止血

| 检查项 | 怎么验 | 期望 |
|---|---|---|
| 环境唯一入口 | `rg 'XCTestConfigurationFilePath' ApiRelay/ApiRelay --glob '*.swift'` | **仅** `AppRuntime.swift` 一处 |
| 生产 Keychain 崩 | 代码含测试碰生产 `servicePrefix` 的 `preconditionFailure` | 有 |
| 警告当错 | `pbxproj` 含 `SWIFT_TREAT_WARNINGS_AS_ERRORS = YES` | Debug/Release 有 |
| RevealPolicy | 无 `RevealPolicy.none` 撞名用法；rawValue 仍可为 `"none"` | `.noVerification` |

**对不对**：止血是「当场崩」不是「打日志继续」；若测试仍能静默碰生产 UserDefaults/Keychain → 失败。

---

# C · H3a / H3b / H3c 降本

| 检查项 | 怎么验 | 期望 |
|---|---|---|
| 认接口 | `AppEnvironment` / 主要 Business 服务依赖为 `any XxxServing` | 编译通过且类型是 protocol |
| 假零件 | `ApiRelay/ApiRelay/Shared/Fakes/` 下 `#if DEBUG` Fake actors | ≥10 个；覆盖门闩/保险库/备份等 |
| 预览 | `AppEnvironment.makePreview` + Vault/Settings `#Preview` | 存在；人话：Xcode Canvas 能出界面 |

**对不对**：生产 `Release` 路径不得依赖 Fake；`makePreview` 仅 DEBUG。

---

# D · H4 失败预案（三项）

| 检查项 | 怎么验 | 期望 |
|---|---|---|
| 生物识别死局 | `biometryUnavailableForUnlock` + `recoverFromUnavailableBiometry` | 降级到 `.biometricOrPasscode`，非 `.noVerification` |
| 无明文可辨 | `VaultHomeViewModel` 对 `secretMissingOnDevice` 设文案；列表有标记 | 不吞成静默 / 笼统 copy.failed |
| 备份空明文 | `ImportSummary.keysWithoutSecretCount` + `BackupExportResult` | 导出/导入状态可带说明 |
| 单测 | `AppPrivacyControllerTests` / `SecureBackupTests` 相关用例 | 绿 |

**对不对**：恢复出口门槛必须是设备主人验证；不得用「不验证」废掉取明文门闩。

---

# E · 机器总闸（必须原样跑）

工作目录：`ApiRelay/`（含 `ApiRelay.xcodeproj`）。

```bash
bash scripts/selfcheck.sh
```

```bash
xcodebuild build \
  -project ApiRelay.xcodeproj \
  -scheme ApiRelay \
  -configuration Debug \
  -destination 'platform=macOS,variant=Mac Catalyst' \
  -derivedDataPath /tmp/ApiRelayAgentDD
```

```bash
xcodebuild test \
  -project ApiRelay.xcodeproj \
  -scheme ApiRelay \
  -configuration Debug \
  -destination 'platform=macOS,variant=Mac Catalyst' \
  -derivedDataPath /tmp/ApiRelayAgentDD
```

全部成功才算机器侧通过。

---

# F · 事故主线是否闭环（判断题）

用一句话回答：

> 若今天再跑一遍「测试删主密码」同类路径，用户还会不会被锁死且无出口？

期望答案：**不会**——Keychain 测试隔离 + 缺主密码/缺生物识别均有设备密码恢复出口。

若答「还会」或「不确定」→ **总验收失败**，列出缺口，不许粉饰。

---

# G · 明确不在本验收范围

- 中国大陆备案、商店 `1.0.1` / `main` / 新 tag  
- `ZL01` 文档重排、`tools/` 对话导航入库  
- 拆 `VaultHomeView`、Stage 2/3 失败预案  
- 人工 Canvas / 真机 Face ID 全路径（可建议负责人抽查，不阻塞机器验收）

---

## 执行记录（验收时填写）

| 节 | 结果 | 备注 |
|---|---|---|
| A H1 | ✓ | `selfcheck.sh` / closeout 规则与命令 / spec-template「前提与失败预案」/ KeychainStoreTests 豁免注释均在 |
| B H2 | ✓ | `XCTestConfigurationFilePath` 仅 `AppRuntime`；Keychain `preconditionFailure`；`SWIFT_TREAT_WARNINGS_AS_ERRORS` Debug+Release |
| C H3 | ✓ | `any XxxServing`；`Shared/Fakes/` 14 个文件；`makePreview` + Vault/Settings Preview |
| D H4 | ✓ | 生物识别死局→`.biometricOrPasscode`；无明文文案；`keysWithoutSecretCount`；相关单测绿 |
| E 机器总闸 | ✓ | selfcheck 红线通过；Catalyst **150 tests / 0 failures** |
| F 事故闭环 | ✓ | **不会再被同类路径锁死无出口**（测试 Keychain 隔离 + 缺主密码/缺生物识别均有设备密码恢复） |

**总裁决**：☑ 通过　☐ 失败  

### 旁注（不阻塞通过）

- `selfcheck` 报告 3「测试替身数」多为 0：脚本未把 `#if DEBUG` Fake 算进替身列；文件在 `Shared/Fakes/`，属统计口径问题，不是 H3b 没做。  
- 真机 Face ID / Canvas 人工抽查仍建议负责人点一眼，不阻塞本机器验收。

完成后更新同目录 `00-总览.md` 进度表勾选 `99`。
