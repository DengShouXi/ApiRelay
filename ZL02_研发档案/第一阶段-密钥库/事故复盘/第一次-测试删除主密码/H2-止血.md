# H2 · 止血

> **开工前必读**：同目录下的 `02-执行须知.md`。里面的五条铁律对本步骤全部生效。
> **执行模型**：便宜模型即可。改动点与验证方式已写死；验证方式是「故意触发，应该崩」或「命令有明确通过标准」。
> **前置**：`H1-验证地基` 已执行（`ApiRelay/scripts/selfcheck.sh` 可用）。

---

## 这一步要达到什么

1. 「是不是在跑测试」只在一处权威判断。
2. 测试碰到生产钥匙串命名空间 / 生产 `UserDefaults.standard` 时**当场崩**（不是警告）。
3. `CloudKitSyncMonitor` 默认不再往真实 `UserDefaults.standard` 写。
4. 清掉 `RevealPolicy.none` 与 `Optional.none` 撞名、以及测试里 `CaseIterable` 扩展冲突两类警告，并打开「有警告就编不过」。

## 工作目录

仓库根（含 `.cursor/` 与 `ApiRelay/`）：

```
/Users/xitongzhili/Desktop/MacTransfer/08_Code/E02_ApiRelay_Github
```

## 改哪些文件

| 文件 | 动作 |
|---|---|
| `ApiRelay/ApiRelay/Shared/AppRuntime.swift` | **新建** |
| `ApiRelay/ApiRelay/App/AppEnvironment.swift` | 改用 `AppRuntime` |
| `ApiRelay/ApiRelay/Data/SwiftData/Schema.swift` | 改用 `AppRuntime` |
| `ApiRelay/ApiRelay/App/AppPrivacyController.swift` | 改用 `AppRuntime`（两处） |
| `ApiRelay/ApiRelay/Data/Keychain/KeychainStore.swift` | 测试碰生产 Service 前缀当场崩 |
| `ApiRelay/ApiRelayTests/Support/KeychainStore+Tests.swift` | 无改动期望；若编译要传新参数再改 |
| `ApiRelay/ApiRelayTests/V1/Phase02_Data/KeychainStoreTests.swift` | 回归用例显式 `allowProductionNamespaceInTests: true` |
| `ApiRelay/ApiRelay/Data/SwiftData/CloudKitSyncMonitor.swift` | 默认 defaults 走运行时隔离；拒绝测试里用 `.standard` |
| `ApiRelay/ApiRelay/Shared/DTOs/DTOs.swift` | `RevealPolicy.none` → `noVerification`（rawValue 仍 `"none"`）+ `CaseIterable` |
| 所有引用 `RevealPolicy` 的 `.none` / `RevealPolicy.none` | 改为 `.noVerification` / `RevealPolicy.noVerification` |
| `ApiRelay/ApiRelayTests/V1/Phase08_Security/SecurityReviewTests.swift` | **删除**文件末尾 `extension RevealPolicy: CaseIterable` |
| `ApiRelay/ApiRelay.xcodeproj/project.pbxproj` | Debug/Release 工程级加 `SWIFT_TREAT_WARNINGS_AS_ERRORS = YES` |
| 同目录 `00-总览.md` | 进度表勾 H2（本步骤最后做） |

## 不许碰的文件

除上表外一律不许改。特别点名：

- `ApiRelay/specs/**`（`BRANCHES.md` 也不动——本步骤不许提交）
- `ApiRelay/.specify/memory/constitution.md`
- 付费墙相关代码与文案
- `ZL03_原始素材/`
- 其它 `.cursor/rules/*.mdc`（本步骤不新建规则）

---

# 第 1 步 · 新建 `AppRuntime.swift`

路径：`ApiRelay/ApiRelay/Shared/AppRuntime.swift`（本工程同步文件夹，**不要**改 `pbxproj` 去手工加文件）。

内容**原样**：

```swift
import Foundation

/// 运行环境的唯一权威入口。
///
/// 「是不是在跑 XCTest」只允许在这里读宿主注入的配置路径环境变量。
/// 其它文件 MUST 通过本类型查询，不得再各自 `ProcessInfo…environment[…]`。
///
/// 工程默认 MainActor 隔离；本类型供 `KeychainStore` / `CloudKitSyncMonitor` 等
/// actor 在非隔离上下文使用，因此成员一律 `nonisolated`。
enum AppRuntime: Sendable {
    /// 单元测试宿主（`TEST_HOST` = ApiRelay.app）时为 true。
    nonisolated static var isRunningTests: Bool {
        ProcessInfo.processInfo.environment["XCTestConfigurationFilePath"] != nil
    }

    /// 测试专用 UserDefaults suite。MUST 与生产 `.standard` 不同名。
    nonisolated static let testUserDefaultsSuiteName = "com.apirelay.tests.defaults"

    /// 按运行环境给出应使用的 UserDefaults。
    nonisolated static func userDefaultsForCurrentRuntime() -> UserDefaults {
        if isRunningTests {
            guard let defaults = UserDefaults(suiteName: testUserDefaultsSuiteName) else {
                preconditionFailure("AppRuntime: 无法创建测试 UserDefaults suite \(testUserDefaultsSuiteName)")
            }
            return defaults
        }
        return .standard
    }
}
```

### 验证

```bash
test -f ApiRelay/ApiRelay/Shared/AppRuntime.swift && grep -c XCTestConfigurationFilePath ApiRelay/ApiRelay/Shared/AppRuntime.swift
```

通过标准：文件存在，且输出为 `1`。

---

# 第 2 步 · 四处环境判断改走 `AppRuntime`

把下列四处的

```swift
ProcessInfo.processInfo.environment["XCTestConfigurationFilePath"] != nil
```

或

```swift
ProcessInfo.processInfo.environment["XCTestConfigurationFilePath"] == nil
```

分别改成 `AppRuntime.isRunningTests` / `!AppRuntime.isRunningTests`：

1. `ApiRelay/ApiRelay/App/AppEnvironment.swift` —— `init` 里 `let isTesting = …`
2. `ApiRelay/ApiRelay/Data/SwiftData/Schema.swift` —— `makeProductionContainer` 里 `let isTesting = …`
3. `ApiRelay/ApiRelay/App/AppPrivacyController.swift` —— `promptUnlockIfNeeded` 里 `guard … == nil else`
4. 同上文件 —— `syncSnapshotCover` 里 `guard … == nil else`

改完后生产源码里 **只允许** `AppRuntime.swift` 再出现 `XCTestConfigurationFilePath`。

### 验证

```bash
grep -rn "XCTestConfigurationFilePath" ApiRelay/ApiRelay --include="*.swift"
```

通过标准：恰好 1 行，且路径含 `AppRuntime.swift`。多于 1 行 → 停手。

---

# 第 3 步 · Keychain 测试碰生产前缀当场崩

打开 `ApiRelay/ApiRelay/Data/Keychain/KeychainStore.swift`。

1. 给 `init` 增加参数（放在参数列表末尾）：

```swift
allowProductionNamespaceInTests: Bool = false
```

2. 在 `init` 正文、赋值之前插入：

```swift
if AppRuntime.isRunningTests,
   servicePrefix == KeychainStore.productionServicePrefix,
   !allowProductionNamespaceInTests {
    preconditionFailure(
        "KeychainStore: 测试禁止使用生产 servicePrefix。请用 KeychainStore.makeForTests()；仅只读回归用例可传 allowProductionNamespaceInTests: true。"
    )
}
```

3. 打开 `ApiRelay/ApiRelayTests/V1/Phase02_Data/KeychainStoreTests.swift`，把那行生产构造改成：

```swift
let production = KeychainStore(accessGroup: nil, allowProductionNamespaceInTests: true) // 自检豁免: 本用例专门验证测试条目在生产 Service 名下不可见，只读枚举、不写不删
```

（保留原有「自检豁免」注释意图；参数名必须带上。）

### 验证（故意触发应当崩）

在 `ApiRelay/` 下用临时测试思路不强制新加文件；改为编译期能通过 + 现有回归用例仍绿即可。本步验证并入第 6 步全量 test。若第 6 步因本改动失败 → 停手，贴完整错误。

---

# 第 4 步 · UserDefaults 隔离 + 当场崩

打开 `ApiRelay/ApiRelay/Data/SwiftData/CloudKitSyncMonitor.swift`。

1. 把 `init` 的默认参数从：

```swift
defaults: UserDefaults = .standard
```

改成：

```swift
defaults: UserDefaults = AppRuntime.userDefaultsForCurrentRuntime()
```

2. 在 `init` 正文开头（赋值前）插入：

```swift
if AppRuntime.isRunningTests, defaults === UserDefaults.standard {
    preconditionFailure(
        "CloudKitSyncMonitor: 测试禁止使用 UserDefaults.standard。请注入 AppRuntime.userDefaultsForCurrentRuntime() 或独立 suite。"
    )
}
```

### 验证

并入第 6 步。`CloudSyncTests` 里已有 `UserDefaults(suiteName:)` 注入，不得改坏。

---

# 第 5 步 · 清 `RevealPolicy` 警告

## 5a · 改枚举

`ApiRelay/ApiRelay/Shared/DTOs/DTOs.swift` 中：

```swift
enum RevealPolicy: String, Sendable {
    case biometricOrPasscode   // LAPolicy.deviceOwnerAuthentication
    case biometricOnly         // LAPolicy.deviceOwnerAuthenticationWithBiometrics
    case masterPassword        // 应用层主密码（FR-003 / FR-038）
    case none                  // 不验证（默认）
}
```

改成：

```swift
enum RevealPolicy: String, Sendable, CaseIterable {
    case biometricOrPasscode   // LAPolicy.deviceOwnerAuthentication
    case biometricOnly         // LAPolicy.deviceOwnerAuthenticationWithBiometrics
    case masterPassword        // 应用层主密码（FR-003 / FR-038）
    /// 不验证（默认）。rawValue 固定为 `none`，兼容已写入 SwiftData / CloudKit 的旧值。
    case noVerification = "none"
}
```

**MUST NOT** 改 rawValue 字符串；已存用户数据与 iCloud 依赖 `"none"`。

## 5b · 删除测试扩展

删掉 `SecurityReviewTests.swift` 文件末尾整段：

```swift
extension RevealPolicy: CaseIterable {
    public static var allCases: [RevealPolicy] {
        [.biometricOrPasscode, .biometricOnly, .masterPassword, .none]
    }
}
```

## 5c · 全文替换 RevealPolicy 的 `.none`

凡类型为 `RevealPolicy` 的 `.none` / `RevealPolicy.none` 一律改为 `.noVerification` / `RevealPolicy.noVerification`。

**不要**改下列（它们不是 RevealPolicy）：

- `BiometryKind.none`
- `BackupProtectionChoice.none`
- `KeyDetailChromeCommand.none`
- `ModelConfiguration` 的 `cloudKitDatabase: .none`
- UIKit/AppKit 的 `.none`（如 `separatorStyle`、`textContentType`）

至少会改到的文件（以仓库现状为准，用搜索确认）：

- `AppLockSession.swift`
- `UserPreferences.swift`
- `UserPreferencesRepository.swift`
- `RevealGate.swift`（仅 `switch policy` 的 `case .none:`）
- `AppPrivacyController.swift`（`switch … revealPolicy` 的 `case .none:`）
- `SettingsView.swift`（策略相关）
- `PreferencesServiceTests.swift` / `KeyVaultServiceTests.swift` / `DataLifecycleTests.swift` / `RecentlyDeletedBatchTests.swift` / `RevealGateTests.swift` / `AppPrivacyControllerTests.swift`
- 若存在 `DebugSupport/Fakes/FakeSupport.swift` 且引用了 `RevealPolicy.none`，一并改（本步允许）

### 验证（字符串仍兼容）

```bash
grep -n 'case noVerification = "none"' ApiRelay/ApiRelay/Shared/DTOs/DTOs.swift
```

通过标准：有这一行。

另：

```bash
grep -rn "RevealPolicy\.none\|revealPolicy = \.none\|policy: \.none\|PreferencesPatch(revealPolicy: RevealPolicy\.none)\|case \.none:" ApiRelay --include="*.swift" | grep -i reveal || true
```

人工确认：残留命中若属于 `BiometryKind` / 其它枚举则忽略；**不得**再有 `RevealPolicy.none` 字面量。

---

# 第 6 步 · 打开「警告当错误」并构建验证

## 6a · `project.pbxproj`

在工程级（不是 target 级）`Debug` 与 `Release` 两处 `buildSettings` 中，各加一行（与其它 `SWIFT_*` 旁）：

```
SWIFT_TREAT_WARNINGS_AS_ERRORS = YES;
```

定位线索：两处都有 `GCC_WARN_UNUSED_VARIABLE = YES;` 的那个 **项目级**配置（同一文件里 target 级配置也有类似键，**不要**只加到 Tests target；要加到含 `ONLY_ACTIVE_ARCH` / 全局警告开关的那两段）。

## 6b · Mac Catalyst 构建

工作目录：`ApiRelay/`

```bash
rm -rf /tmp/ApiRelayAgentDD
xcodebuild build \
  -project ApiRelay.xcodeproj \
  -scheme ApiRelay \
  -configuration Debug \
  -destination 'platform=macOS,variant=Mac Catalyst' \
  -derivedDataPath /tmp/ApiRelayAgentDD
```

通过标准：`BUILD SUCCEEDED`。若因既有无关警告变成 error → **停手**，贴完整原文，不许自行关掉 `SWIFT_TREAT_WARNINGS_AS_ERRORS` 蒙混。

## 6c · 自检

仓库根：

```bash
bash ApiRelay/scripts/selfcheck.sh ; echo EXIT:$?
```

通过标准：退出码 `0`；报告 4 显示 `XCTestConfigurationFilePath` **恰好 1 处**（`AppRuntime.swift`）。

## 6d · 单测（可选但推荐；若机时紧张至少做 6b+6c）

```bash
xcodebuild test \
  -project ApiRelay.xcodeproj \
  -scheme ApiRelay \
  -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -derivedDataPath /tmp/ApiRelayAgentDD \
  CODE_SIGN_IDENTITY="-" \
  CODE_SIGNING_ALLOWED=YES \
  CODE_SIGNING_REQUIRED=YES
```

若 destination 不存在：按 `02-执行须知` 换名字；再不行停手。

通过标准：测试 0 failed。

---

# 第 7 步 · 更新 `00-总览.md` 进度

把进度表里 `H2-止血` 一行改为已完成，日期填当天；文件清单里 `H2-止血.md` 状态改为「✅ 已有且已执行」。文首「仍未写」提示相应改掉。

**MUST NOT** `git commit` / `git push`（见执行须知铁律 3）。

---

# 做完按 `02` 第六节格式报告
