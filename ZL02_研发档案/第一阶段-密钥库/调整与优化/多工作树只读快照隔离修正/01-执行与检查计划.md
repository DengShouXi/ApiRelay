# 执行与检查计划

- 阶段：方法缺陷修正计划
- 日期：2026-09-24
- 分支／起始 HEAD：`v1.13.10`／`25fe006896a816475e269e155530c1464cc19304`
- 输入：ZL00/00–04、现行检查器及测试、v1.13.10 工作包 00/00C/02/03、两个工作树 Git 状态
- 授权：用户「同意先修隔离规则」；不包含 App 代码、提交或上传

## 裁决

这是治理检查器与实例契约的组合缺陷，不是 v1.13.10 产品功能变更。保留原工作树，不暂存、不清理、不复制其文件。现行普通只读工作树的洁净要求继续有效；仅增加可选的“开工前已脏只读工作树快照”例外。

方法小版本升至 `MAIC-1.3.1`。v1.13.10 阶段 1–3 的 `MAIC-1.3` 历史产物按精确路径列入 legacy 映射，不重写历史身份。原工作树上尚未合并的 MAIC-1.5 实验属于另一工作链，本次不复制、不覆盖；日后整合必须单独裁决。

## 实施单元

1. 检查器增加可选 `repository.readOnlyWorktreeSnapshots`。每项精确登记路径、分支、HEAD、状态与内容的 SHA-256；必须同时在 `allowedReadOnlyWorktrees`。未登记工作树仍拒绝。
2. 对登记快照者，先禁止暂存，再核对分支、HEAD、完整 `git status --porcelain -z -uall` 原始状态和文件内容／类型／权限的规范摘要；摘要计算前后再次读取状态，发现并发变化即拒绝。现有 `porcelain()` 丢失重命名／复制双路径，因此快照基线和运行态都直接拒绝 `R/C`。无快照的只读工作树仍须干净。任何新增、删改、权限变化、HEAD/分支变化或暂存都拒绝。
3. 另增可选 `repository.frozenExternalFiles`：只允许本次独立方法任务的精确文件清单、Git 状态、普通文件类型、权限与内容 SHA-256；v1.13.10 主检查器对照现状核验后才把这些文件视为已核验的外部差异，不给产品阶段任意修改治理文件的白名单。其余路径继续按原白名单拒绝。独立方法审计完成后再冻结，未完成时不能用该例外放行。
4. 只给原工作树登记快照，不给 v1.13.10 的实施树开例外；更新 04 方法正文、模板／README、v1.13.10 契约，并保存指纹生成证据。快照只覆盖 Git 可见状态（当前展开为 1,181 个文件），不宣称覆盖 Git 忽略文件。
5. 针对通过、未登记、普通脏树、快照篡改、内容改变、新未跟踪文件、重命名、暂存、分支/HEAD 漂移、外部差异越界／篡改写自动测试。跑全量检查器测试、实际检查器、`git diff --check`。
6. 写停止报告后由另一个 AI 只读检查实现、所有差异及原工作树是否保持原状。未通过不得恢复产品实施。

## 停止条件

原工作树状态已变、受保护 refs 漂移、测试失败、出现业务代码差异、检查器接受未登记工作树或快照不匹配，立即停止并记录事实。不通过修改原工作树或放宽白名单求通过。

方法观察：把“有脏主工作树”当作工作树不存在导致现行契约从创建时就无法通过检查；需在方法闸门中保留原状并检测后续变化。
