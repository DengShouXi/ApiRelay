# 冻结文件类型／权限漏检整改

- 阶段：独立审计阻塞整改
- 日期：2026-09-24
- 分支／HEAD：`v1.13.10`／`25fe006896a816475e269e155530c1464cc19304`
- 方法版本：MAIC-1.3.1
- 实施者：Codex
- 输入：本包 04-独立审计报告.md 的唯一阻塞项
- 授权：用户已批准先修隔离规则；仅修复审计确认的漏检，不改 App、原工作树、提交或上传

## 修复

`frozenExternalFiles` 每项现要求 `kind: file` 和整数 `mode`，运行时用 `lstat` 核对普通文件类型与三位权限，再以不跟随末级符号链接的方式核对内容 SHA-256。文件变为可执行或被符号链接替换，即使 Git 两字符状态和内容未变，也退出 4。方法正文、README 和本包计划同步写明类型／权限冻结。

## 验证

- 新增 `chmod 755` 与普通文件替换为符号链接两个负例；与原正例一起通过。
- 检查器全量 84 项测试通过；`git diff --check` 通过。
- 原工作树指纹再次为 `838414ac8e8eb18dc8bc878ef48624881ad69684d541f5b3e934a262e3c2b809`，未被本次整改改动。
- v1.13.10 主检查器仍会因未冻结治理差异退出 4；复验前不放行产品实施。

方法观察：独立审计的权限变更反例发现了单元测试未覆盖的真实边界；不能把内容 hash 等同于完整文件状态冻结。

写入状态：已停止
