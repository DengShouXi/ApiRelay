# 购买权益检查计划（供独立角色）

阶段：1；日期：2026-09-24；分支：`v1.13.11`；起始 HEAD：`8e6c22109b9f5851dacb31b96e8915d855b0e0f3`；输入：01 执行计划、00D 基线、现行 StoreKit 代码/测试、项目治理；授权：只读检查，不写代码、不提交上传。

- **范围**：核对 `git status`、HEAD、暂存区、受保护 refs 与契约；找出购买 UI、业务权限、交易更新和额度之间的双重状态/错误吞掉/旧缓存授权路径。
- **用例**：购买成功/失败/取消/待处理、恢复、退款撤销、无网络/无账户、首次启动查询中、免费 3 条边界、跨设备状态变化；区分可自动化与必须 Sandbox/真机的部分。
- **隔离**：只读查看原 `13.9` 草稿的具体差异和归属，不改它、不复制 Xcode 工程整体差异；检查 `v1.14` 与密码冻结边界。
- **输出**：精确候选文件、缺陷证据、自动/真机测试矩阵、仍须用户决定的问题、是否可进入最终计划。检查者必须与后续实施者不同；不把预查报告当实施授权。

方法观察：未发现需要迭代的方法问题。
