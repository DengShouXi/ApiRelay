# 03 · Grok 实施前检查报告

## 0. 身份

- 阶段：2（Grok 实施前独立检查）
- 生成时间：2026-09-11 01:18 CST
- 分支：`v1.13.8`
- 起始 / 当前 HEAD：`bb5257fd28baf7e06c9da916fe48b278e15ec384`
- 输入：`00`、`00A`、`00B`、`01`、`02`、四份 `测试用例/`、全部 `提示词/`、`ZL00/00`–`04`、`AGENTS.md`、`.cursor/rules/project-governance.mdc`、本机 `/Users/xitongzhili/.agents/skills/multi-ai-collaboration/SKILL.md`、实际 Git 状态
- 授权：用户执行阶段 2 提示词，仅允许新增本文件；不是实施、提交或上传授权
- 状态：只读检查完成；等待 Codex 阶段 3 吸收，不得进入阶段 4

## 1. 检查前现场

| 项 | 结果 |
| --- | --- |
| 仓库根 | `/Users/xitongzhili/Desktop/MacTransfer/08_Code/E02_ApiRelay_Github`（含 `.git/`、`ApiRelay/`、`ZL00`–`ZL02`） |
| 当前分支 | `v1.13.8`，跟踪 `origin/v1.13.8` |
| HEAD / `v1` / `origin/v1` / `origin/v1.13.8` | 均为 `bb5257fd28baf7e06c9da916fe48b278e15ec384` |
| `main` / `origin/main` | `539447ea9984608c74989da48b8770cb3fe32a6c` |
| tag `release/1.0.0` | `5c5eed3f8390f51f5a26be9737bcc11f72e7d7fd` |
| `v1.13.9` | 不存在 |
| 暂存区 | 空 |
| 工作树 | 仅一个 worktree，指向本仓库 |
| `03` 在写入前 | 不存在，故继续 |

检查前已有草稿差异（与 `01` 第二节一致，另加本工作包与未跟踪 `tools/`）：

- 已修改未暂存：`.cursor/rules/project-governance.mdc`、`AGENTS.md`、`README.md`、`ZL00/00`、`ZL00/01`、`ZL01/00`、`ZL01/08`、`ZL01/14`、`ZL02_研发档案/第一阶段-密钥库/00-阶段总览.md`
- 未跟踪：`ZL00_项目总控/04-双AI协作与独立审计.md`、本工作包全部 23 个已有文件、`tools/`（只通过 `git status` 确认未跟踪，未打开、未读取、未处理）
- 产品范围无差异：`ApiRelay/ApiRelay`、`ApiRelay/ApiRelayTests`、constitution、ROADMAP、身份验证 spec / playbook 正文均无本次 diff

上述草稿是待审对象，不是已验收结论。

## 2. 本机技能与仓库提交范围（分开报告）

本机 Codex 入口：`/Users/xitongzhili/.agents/skills/multi-ai-collaboration/SKILL.md`（2026-09-11 00:48 修改）。`/Users/xitongzhili/.codex/skills/` 只有 `.system/`，没有同名 `multi-ai-collaboration` 副本。

技能正文优先服从各项目自己的规则；无项目方法时才用通用底线；未加载技能或项目规则时不得声称已自动唤醒；不携带本仓库路径、`v1.13.8` 或产品决定。描述字段已覆盖“让另一个AI做、你来检查”这类隐含接力。

**不属于 Git 提交 A/B，不得写入阶段 8 显式清单。** 阶段 3 须单列：保留、微调负例，或移除；并给出在新 Codex 任务中的验证办法。本阶段未新开 Codex 会话，**未验证**该技能会被 Codex 实际发现。

---

## 3. 测试域

### 3.1 显式 / 隐含唤醒及误触发

**预期：** 协作意图自动启用方法、先声明、先定位已有工作包；普通单 AI 请求不建包；只说“上传”不扩大授权。

**证据：** `ZL00/04` §1 按语义触发，并列了显式与隐含说法；`AGENTS.md` 与 `project-governance.mdc`（`alwaysApply: true`）有压缩触发段，且都写“不自动提交或上传”。`09` 的日常口令“上传”不在触发名单里。测试用例 `01` 的正向句与负向句，大部分能对上这些名单。

**缺口：**

1. 正式方法没有“显式 / 隐含 / 负例”矩阵；`测试用例/01` 只是句子清单，没有预期路由目标（AGENTS / Cursor / 技能 / 仅提示词）。
2. “找另一个模型复核完再上传”“多个窗口配合”“你帮我检查”未写入 AGENTS/Cursor 压缩名单；语义上可能命中，但两份入口用词已经开始分叉。
3. 负例“这个功能用了几个AI接口？”含“AI”，技能写“按意图不按口令”，存在误触发风险。
4. 本阶段没有用新对话实测自动唤醒，**触发可靠性未黑盒验证**。

**建议：** 阶段 3 在最终计划里放一张触发矩阵（含负例与“不扩大上传授权”）。AGENTS 与 Cursor 只保留短名单 + 指向 `04`，避免两份越抄越长、互相漂移。技能描述补一条负例。四份测试用例补预期入口。

### 3.2 Codex、Cursor、跨项目入口与未加载边界

**预期：** 本仓库 Codex 走 AGENTS；Cursor/Grok 走 alwaysApply；跨项目只找项目内规则；未加载规则的外部编辑器必须诚实，并有自包含提示词。

**证据：** `ZL00/04` §1 已写“不得承诺控制未加载它的工具”。技能同样写明。本工作包每份阶段提示词都是自包含入口。本次对话因用户粘贴阶段 2 提示词而执行，属于可靠后备，不是对自动触发的独立证明。

**缺口：** Codex 新任务能否发现 `~/.agents/skills`、Cursor 新对话在无提示词时是否必读 alwaysApply，均未实测。`ZL01/06` 对 `project-governance.mdc` 仍写“只指向 ZL00”，未反映已加入的语义触发段。`ZL01/01` 对 `ZL00` 的说明仍是“职责、冲突裁决、关账闸门”，未点名 `04`。

**建议：** 阶段 5/7 用新对话做黑盒。阶段 4 可给 `06`、`01` 各改一行路由说明。不要把技能内容复制进仓库。

### 3.3 固定任务契约与从产物 / Git 恢复阶段

**预期：** 契约只存稳定事实；动态阶段由有效产物 + Git 推导；身份不符的文件无效。

**证据：** `ZL00/04` §6–§7 已有工作包最低文件集、产物身份字段、`通用-只检查当前阶段.md` 决策树。本工作包 `00` 不手写动态阶段。`ZL01/14` 把内部阶段号交给通用提示词。按该决策树：`00B`/`01`/`02`/四份测试用例齐全且 `03` 原缺失 → 当前就是阶段 2，与现场一致。

**缺口：**

1. `00B` 要求的固定任务契约（工作包 ID、方法版本、风险级别、唯一工作区、当前写入角色、起始 HEAD、闸门）**尚未设计落点，ZL00/04 正文也没有这些字段**。
2. `ZL00/04` **没有方法版本标识**，但 `00B` 检查器要用“契约中的方法版本 vs 当前方法文件”。
3. 通用提示词还不知道契约/检查器；阶段 3–5 提示词已经预支它们存在。
4. `ZL01/14` §2 仍写“完整技术过程在”旧包 `v1.13.8-项目治理体系整理`，新包只在段首出现。只读恢复时可能打开错包。

**建议：** 阶段 3 规定契约模板在 `ZL00_项目总控/自动化/`，本工作包阶段 4 落一份示范契约（建议文件名 `00C-任务契约.md`）。给 `04` 增加方法版本（例如 `方法 v1`，与商店版本脱钩）。同步改通用提示词与 `14` 的“完整技术过程”指向。

### 3.4 双 AI、三 AI、同工作区、不同 worktree

**预期：** 一个规划审计负责人、一个当前执行者；同时一个写入者；worktree 必须写明唯一实施位置。

**证据：** `ZL00/04` §2 已写这三条。阶段提示词普遍要求“上一位 AI 已停止”。当前 `git worktree list` 只有本目录。

**缺口：** 没有可观察的“当前写入者”登记，双写只能靠纪律。不同 worktree 没有契约字段或模板。三 AI 只读任务怎样把结果交回规划审计，没有文件格式。本阶段无法注入双写入者，**并发停止未实测**。

**建议：** 契约增加 `planningRole` / `currentWriter` / `implementationWorktree`。检查器在两份未停止报告都自称写入者时失败。不要幻想文件系统锁。

### 3.5 陈旧附件、现行 13.9 路牌、未决默认值

**预期：** 附件只是候选证据；现行权威说身份验证落地在尚未创建的 `v1.13.9`；未决产品默认值在用户拍板前阻止规格定稿和代码实施。本检查**不拍板**取用验证默认值。

**证据：** `00A` 正确把附件降级。现行路牌一致指向计划中的 `v1.13.9`：`ZL01/14` §4、`follow-ups/identity-auth/spec.md`、`playbooks/身份验证方案.md`、主 spec / research / data-model / BRANCHES。`00` 禁止把附件“在 v1.13.8 改代码”当现行指令。

取用验证：现行方案只规定开启后的行为，**没有**记录 `revealAuthEnabled` / “取用验证出厂开还是关”。这与 `00A`“未决”一致。不得用附件里的默认 `true` 填这个空。

**缺口：** `ZL00/04` 正文没有“外部输入新鲜度”（分支、日期、权威、未决事项）检查步骤，也没有“未决安全/产品默认值闸门”。四份 `测试用例/` 也没有 13.8 vs 13.9、未决默认值这两条。没有这些规则时，旧附件仍可能被执行者直接当计划。

**建议：** 把新鲜度与未决闸门写入 `ZL00/04`，并最小补进 `ZL00/02`（附件与现行权威打架时停止）。把这两条补进现有测试用例（见 3.6），不要另造第五份以免全套提示词“四份”全部改名。

### 3.6 ZL02 与 Spec Kit 拆分

**预期：** 完善方法走 ZL02；真正改密码产品走现有 `follow-ups/identity-auth/`；先方法关账，再另授权建 `v1.13.9`；不得两份现行产品方案。

**证据：** `ZL00/04` §4、`ZL00/01`、本包 `00` 第五节已经拆开。本包保护边界禁止改 spec / Swift / constitution / ROADMAP。现场也没有这些 diff。

**缺口：** 四份测试用例没有“同一句话里既要改治理又要改密码”的拆包样本。`ZL00/04` 没有“两类工作用依赖连接、不复制产品方案”的操作句。

**建议：** 扩写 `测试用例/02` 或 `03`：一句话双来源 → 两个工作包 + 先后依赖。阶段 4 只回写方法，不碰身份验证正文。

### 3.7 轻量 / 标准 / 高风险与额度不对称

**预期：** 先按风险选流程，再按额度分工；高风险不得因 Grok 额度多或文件少而降级；执行者不自验。

**证据：** `ZL00/04` §1、§3 已有三级表和“额度只决定谁做”。默认 Codex 规划审计、Grok 检查执行。提示词索引第 5 条同此。`测试用例/03` 样本清楚。

**缺口：** 轻量任务的“另一角色针对性复查”没有产物名，容易被做成口头说一声。提示词仍要求阶段 2“完整阅读全部提示词”，与 `00B`“只读本阶段所需”不完全一致。确定性检查（链接、路径、Git）还没有工具，两个模型仍会靠人工扫。

**建议：** 轻量复查最少写一份短报告路径。阶段 3 按阶段收紧必读清单。检查器承担机械项。

### 3.8 密码高风险嵌套审计

**预期：** 密码 / 迁移 / CloudKit / 删除强制完整流程，并允许 W0、迁移、验证引擎、调用点、授权复用之间设闸门。

**证据：** `ZL00/04` 高风险行写了“标准流程 + 关键波次嵌套审计闸门”；§1 禁止因文件少降为简化流程。

**缺口：** 只有一句话，没有“闸门怎么嵌进阶段 1–9”（波次末是否等于一次阶段 5、谁放行、失败是否走 7R）。`测试用例/03` 没有 W0–W6 依赖样本。本包正确**不实施**这些波次，但方法正文若保持口号，以后 13.9 仍会只在末尾验一次。

**建议：** 阶段 3 写清嵌套闸门 = 同一工作包内重复“执行 → 针对性审计 → 放行”，不是另造一套阶段号。把 W0–W6 列为高风险样本，不在本包做产品实施。

### 3.9 方法候选去重、晋级、回归、防无限迭代

**预期：** 无问题只写一句；非阻塞候选带证据并可指纹去重；阻塞则停当前流程并进独立方法工作包；整改只验证已确认缺陷；跨项目技能不把本项目决定带走。

**证据：** `ZL00/04` §9 与 `00B` §2.6、`测试用例/04` 方向一致。本报告按要求写“方法观察”。

**缺口：** `04` 没有问题指纹、候选档案格式、晋级条件（几次同类证据才改 ZL00）、回归必须回跑的用例集。技能升级规则在测试用例里，不在 `04`。

**建议：** `04` 增加候选最小字段（证据、影响、拟改位置、回归用例、指纹）。非阻塞候选留在本包；阻塞才新建 ZL02 方法包。晋级必须另一角色验收。

### 3.10 旧 HEAD、缺产物、较新失败、异常暂存、双写

**预期：** 这些情况停止，不猜测、不自动修、不切换分支、不提交。

**证据：** `ZL00/04` 有“身份不匹配不得当有效前置”“较新失败不能用旧通过放行”（阶段 8 提示词更明确）。通用提示词按产物存在与否推阶段。本包 `00` 把暂存异常、HEAD 不符列为停止条件。现场 HEAD 匹配、暂存空、无 `v1.13.9`。

**缺口：** 故障矩阵主要写在 `测试用例/02` 和本包 `00`，**还没写进 `ZL00/04` 操作句**。没有检查器时，旧 HEAD / 缺输入却有后续报告 / 计划外暂存，要靠人发现。`tools/` 越界目前只能看 `git status`。

**建议：** 把停止条件写进 `04` 第八节/第十节。检查器实现这些项。阶段 5 用伪造身份块做注入，不要只 grep 关键词。

### 3.11 前置方法上传后才能创建 13.9

**预期：** 带着未上传的治理草稿不得创建 `v1.13.9`；验收通过 ≠ 上传授权 ≠ 建新分支授权。

**证据：** `00` §5、`00A` 点 7、阶段 8/9 提示词、`ZL01/14` §3 均禁止现在建 13.9。现场没有该分支。`ZL00/03` 有上传四闸，但**没有**“未关账的方法修改挡住下一产品热修分支”。

**建议：** `ZL00/03` 或 `04` 加一条依赖关账：创建下一产品分支前，前置方法工作必须最新有效验收通过、A/B 上传、阶段 9 复核、工作区对该范围洁净。本包阶段 4–9 仍不得建 13.9。

### 3.12 A/B 上传闭环

**预期：** 提交 A 用最终范围显式路径；B 只改 `ZL01/13`、`10`、`14`、`BRANCHES`；然后纯快进 `v1`；不第三次记账；不含技能、`tools/`、产品代码。

**证据：** `ZL01/09` §1.3 已是现行 A/B。阶段 8 提示词与之对齐，并排除本机技能和 `tools/`。`ZL00/03` 禁止通配暂存和预填未发生事实。

**缺口：** 阶段 8 还不能生成最终 A 清单，因为精确范围要等阶段 3。`BRANCHES.md` 仍写“工作分支 = 项目治理体系整理”，与 `14` 的新任务并列，存在路牌新旧并存；hash/tip 应留到 B，但“在做什么”是否阶段 4 就改，由阶段 3 决定。

**建议：** 阶段 3 预列 A 的候选路径（见第 6 节），阶段 4 报告再按实改收敛。B 的四文件不得提前写进阶段 4 内容提交。

---

## 4. 只读检查器：必要、可行、最小边界

**必要：** 是。阶段恢复和故障注入（HEAD、暂存、缺产物、较新失败、双写声明、保护范围）是机械事实。只靠模型通读，正是“旧通过结论放行”的来源。`00B` 把它列为架构，不是可选项。

**可行：** 是。不依赖第三方包：`git` porcelain + 文件存在判断 + 对报告文首身份字段的正则即可。本机有 `git` / `python3`。

**最小边界（阶段 3 必须写死，阶段 4 才实施）：**

| 项 | 要求 |
| --- | --- |
| 落点 | `ZL00_项目总控/自动化/`；模板也在这里 |
| 禁止 | 不得放 `tools/`；不得修改、暂存、切换、提交、上传；不得替代 ZL00 裁决 |
| 输入 | 工作包 `00` 路径；若存在则读契约 |
| 输出 | `00B` §2.3 那七项；非零退出表示必须停止 |
| 失败 | 缺 `00`、分支/HEAD 不符、身份不匹配、方法版本不符、较新失败报告、计划外暂存、保护范围 diff、双写入声明、缺必要输入却有后续报告 |
| 权威 | 检查器与 ZL00 冲突 → 停止，按 `ZL00/02` 裁决 |
| 复用 | 未来工作包只填契约、调用同一命令；本 ZL02 只留测试证据 |

本阶段**不实施**检查器。

## 5. 提示词矛盾与连续性

1. **“四份测试用例”写死在多份提示词里**，但四份未覆盖 `00A`/`02` 的产品压力场景。阶段 3 应扩写现有四份，避免改成五份导致全套提示词改名。
2. **通用提示词不知道契约/检查器/方法版本**，阶段 3–5 已预支。最终计划必须改通用提示词，否则阶段恢复会忽略新闸门。
3. **AGENTS 与 Cursor 触发段不完全相同**（Cursor 多写“判断 Spec Kit 还是 ZL02”）。应收敛为短路由，细节只留 `04`。
4. **阶段 2 要求通读全部提示词**，与质量优先的“只读本阶段输入”冲突。阶段 2 作为预查可以通读一次；阶段 4 以后应按索引收紧。
5. **阶段 7R 禁止改提示词**。若审计发现提示词本身是阻塞缺陷，阶段 3 须规定：方法提示词缺陷走 7R 允许范围，或退回阶段 3 改计划，不能让执行者临场改提示词。
6. **技能通用底线用 plan→preflight→… 叙述，ZL00/04 用阶段 1–9。** 有项目方法时以 `04` 为准，应在技能里再写一句，避免外部项目或漏加载时各走各的。
7. 相对链接抽查：`00` → `../../../../ZL00_项目总控/04-…` 正确；`04` 实例链到旧治理包路径存在；提示词索引列出的 14 个提示词文件均在。未跑全仓链接爬虫。

## 6. 阶段 4 建议修改 / 新增的精确文件

最终以阶段 3 的 `04-最终执行计划.md` 为准。根据本检查，建议如下。

**修改（已有草稿，阶段 4 补齐而非另起一套）：**

- `ZL00_项目总控/04-双AI协作与独立审计.md`（唯一方法正文：版本号、新鲜度、未决闸门、契约、检查器指针、嵌套审计、候选指纹、故障停止、前置关账）
- `ZL00_项目总控/00-项目治理总纲.md`（若增加 `自动化/` 则列入目录）
- `ZL00_项目总控/01-权威职责与边界.md`（契约/检查器落点；仍不把过程材料放进 ZL00）
- `ZL00_项目总控/02-冲突裁决与变更机制.md`（附件/聊天 vs 现行权威的新鲜度）
- `ZL00_项目总控/03-关账与发布闸门.md`（异常暂存停止；下一产品分支依赖前置方法关账）
- `AGENTS.md`、`.cursor/rules/project-governance.mdc`（短触发 + 负例指针，不复制 `04`）
- `README.md`（已有 `04` 链接，无新需求则不动）
- `ZL01_具体说明/00-从这里开始.md`、`08-开发流程.md`、`14-项目当前状态.md`（消除“完整技术过程”指向旧包的歧义）
- `ZL01_具体说明/01-仓库地图.md`、`06-AI铁律清单.md`（各一行，反映 `04` 与触发）
- `ZL02_研发档案/第一阶段-密钥库/00-阶段总览.md`
- 本工作包：`测试用例/01`–`04`（补 `00A` 场景与触发矩阵）、`提示词/通用-只检查当前阶段.md` 及阶段 4–9、7R/7S/7T（对齐契约/检查器）、必要时 `00`/`00B` 只改与最终计划不一致的句子

**新增：**

- `ZL00_项目总控/自动化/README.md`
- `ZL00_项目总控/自动化/` 下契约模板 + 无第三方依赖的只读检查脚本
- 本工作包 `00C-任务契约.md`（或阶段 3 指定的同等文件）：示范契约，不是第二套方法
- 本机技能仅允许改 `~/.agents/skills/multi-ai-collaboration/SKILL.md`，**单独列出，不进提交 A/B**

**禁止（阶段 4 仍禁止）：**

- `ApiRelay/ApiRelay/**`、`ApiRelay/ApiRelayTests/**`、Xcode、constitution、ROADMAP、身份验证 spec/playbook 产品语义
- `tools/`、`.gitignore` 为本次而改、本机技能目录入库
- 暂存、提交、上传、切换分支、创建 `v1.13.9`
- 把附件产品方案写入任何权威；拍板 `revealAuthEnabled` / 取用验证出厂开关

`ZL02_研发档案/00-研发档案总览.md` 已指向第一阶段总览，可不改。`BRANCHES.md` 的“工作分支在做什么”是否阶段 4 改、还是提交 B 再改，由阶段 3 定，避免与 `14` 长期两句并存。

## 7. 能否进入阶段 3

**可以进入阶段 3，不能进入阶段 4。**

进入阶段 3 的条件已满足：工作链身份与 `bb5257fd` / `v1.13.8` 一致；暂存空；阶段 1 设计输入齐全；本报告为唯一新增；保护范围内无产品代码 diff。

不得把本报告当作实施授权。阶段 3 必须吸收第 3–6 节，写出 `04`/`05`，列出精确文件，并等待用户确认最终计划。下列方法缺口若写不进最终计划，以后阶段 5 应视为阻塞：触发负例与入口分叉、契约与方法版本缺失、新鲜度/未决闸门缺失、嵌套审计无可操作定义、四份测试未覆盖 `00A`、检查器边界未定、`14` 恢复入口指向歧义。

## 8. 方法观察

阶段 2 是用户粘贴自包含提示词进入的，同时仓库规则也要求启用本方法；两者指向同一工作包，没有另建一份。测试设计声称“四份用例是共同回归基线”，但产品压力场景只写在 `02` 检查计划正文里，这会造成阶段 5 按用例跑却漏掉 `00A`。最终计划应把基线收口到测试文件本身。
