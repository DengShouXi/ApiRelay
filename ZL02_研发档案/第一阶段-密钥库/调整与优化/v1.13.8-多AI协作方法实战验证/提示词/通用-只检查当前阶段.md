# 通用：只检查当前阶段

```text
请只读判断以下工作包已经完成到哪一步，不执行下一阶段：

ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-多AI协作方法实战验证/00-总流程操作台.md

先确认当前目录是仓库根，上一位AI已经停止。若`00C-任务契约.json`和`ZL00_项目总控/自动化/check_multi_ai_workflow.py`已存在，先运行`04-最终执行计划.md`第5.1节的只读命令；非零退出时只报阻塞，不再手工推测绕过。否则阅读操作台、提示词索引、所有现有阶段产物、整改轮次目录，并核对当前分支、HEAD、暂存区和工作区。

按证据依次判断：`00B`、`01`、`02`或四份`测试用例`缺失→阶段1；`03`缺失→阶段2；`04`/`05`缺失→阶段3；`06`缺失→等待用户批准后阶段4；`07`缺失→阶段5；`07`有阻塞且`08`缺失→阶段6；`09`缺失→阶段7；最新验收或复验不通过→阶段7R/7S/7T中第一个缺失步骤；最新有效报告通过但尚无本工作内容的A/B提交→等待用户授权阶段8；A/B和`v1`已更新→阶段9实时复核。

整改轮次另加两道优先关口：先核对该轮已批准`01`的固定指纹，执行者改写过定稿时停止；再读取该轮`01`要求的外部证据。若`02`已存在但关键证据仍为“未验证”，报告“等待外部证据”，不得把检查器机械返回的7T当成可执行许可，不得指向7T、创建`03`或下一轮。用户提供记录或明确授权Codex组织黑盒测试后，才能重新判断。

报告身份中的分支、起始HEAD、输入、方法版本或轮次不匹配时，该文件不得算有效。回答完成阶段、下一阶段、负责人、应使用的提示词、所需用户批准、阻塞和证据。最后必须用三行结尾：“下一步负责人：……”、“是否需要用户批准：……”和“下一步：执行〈实际存在的提示词绝对路径〉”。不得输出占位符。下一步不唯一时，第三行必须改为执行本文件自身的绝对路径。不得修改文件、暂存、提交、上传、切换或创建分支，不得读取或处理tools/。
```
