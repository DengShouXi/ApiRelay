# R03 · Codex 复验报告

## 0. 身份

- 阶段：7T（Codex 独立复验 R03）
- 轮次：`R03`
- 时间：2026-09-14 21:12 CST
- 分支：`v1.13.8`
- 起始 / 当前 HEAD：`bb5257fd28baf7e06c9da916fe48b278e15ec384`
- 方法版本：MAIC-1.2
- 输入：`整改轮次/R03/01-Codex整改定稿.md`、`整改轮次/R03/02-Grok整改报告.md`、四份测试用例、实际差异和 Git 状态
- 授权：用户明确执行阶段 7T；仅允许新增本报告，不是修复、提交、上传或创建分支授权

## 1. 前置关口

1. 本报告创建前，只读检查器退出 0，唯一定位 `nextPhase=7T`、`owner=Codex`，并明确输出“检查器只判断状态和路由，不授予下一阶段执行权限”；用户本次明确调用阶段 7T，因此执行授权有效。
2. `R03/01`、`R03/02` 存在而 `R03/03` 不存在；轮次连续。最近执行产物 `R03/02` 有独立行“写入状态：已停止”。
3. `R03/01` SHA-256 为 `72405e27b30ebef2fc1be47a624b81f20a655261253ee9243a0e23cb278dd0a6`，与 `R03/02` 记录的执行前、执行后指纹相同。
4. `R03/01` 第 4.2 节冻结文件逐项复算，全部一致；允许修改文件的当前指纹也与 `R03/02` 的执行后清单逐项一致。
5. 内容审查发现 `测试用例/01-唤醒与误触发测试.md` 存在两套现行黑盒规程。该结构性错误使 R03 黑盒验收口径不唯一，属于已经证实的实施失败，而不是单纯“尚未组织黑盒”。依照质量优先和结构阻塞先停原则，本轮不继续消耗外部任务与 Cursor 黑盒额度。

## 2. 独立复验结果

### 2.1 规则、检查器和版本迁移

- 独立运行 `PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest ZL00_项目总控/自动化/test_check_multi_ai_workflow.py`：55 项测试全部通过。
- 检查器 JSON 成功输出含 `checkerGrantsExecutionAuthority: false` 和固定 `authorizationNote`；文本成功输出也有独立的不授权提醒。
- 现行方法与实例契约均为 `MAIC-1.2`；1.0、1.1 精确历史兼容和未来报告冒用旧版的自动回归通过。
- ZL00/04、AGENTS、Cursor 入口和本机技能已写入唤醒、路由、执行三层权限及项目根工具边界；本机技能不含本项目专有路径、版本或产品决定。

结论：R03 针对授权边界和跨项目工具边界的规则及确定性测试已经落盘，但因黑盒基线损坏，不能宣布这些行为已经通过真实外部验证。

### 2.2 F7 外部黑盒

R03 的 B7-1 至 B7-5 未作为正式验收样本执行，原因不是缺少测试工具或用户授权，而是现行 `测试用例/01` 同时存在新旧两套互相冲突的通过标准：

- 文件第 7 行和第 99 行各有一份 `## 黑盒规程（不依赖本对话）`；
- `## 正向命中`、`## 不应误触发`、`## 加载边界`也各出现两次；
- 前半段要求 R03 使用“路由不授权、不得执行下一提示词、不得读取来源任务或搜索项目根外”的新标准；
- 后半段仍保留较弱的旧标准，并继续写“R02 的外部重测必须由 Codex 在 7T 独立组织”，属于过期状态。

在这种情况下运行黑盒，即使模型行为正确，也无法证明它满足唯一现行标准；若按较弱旧段判定，还可能制造错误通过。为避免无效任务、额外算力消耗和再次污染证据，本轮在外部黑盒前停止。

结论：F7 关键项未形成有效 R03 证据，不能进入阶段 8。

### 2.3 Git、范围、链接和保护边界

- 当前只有一个 worktree；未因本轮复验创建临时 worktree。
- 当前分支和 HEAD 仍为 `v1.13.8@bb5257fd28baf7e06c9da916fe48b278e15ec384`；暂存区为空；`git diff --check` 通过。
- `main`、`origin/main` 均为 `539447ea9984608c74989da48b8770cb3fe32a6c`；`v1`、`origin/v1`、`origin/v1.13.8` 均为基线 HEAD；`release/1.0.0` 为 `5c5eed3f8390f51f5a26be9737bcc11f72e7d7fd`；本地不存在 `v1.13.9`。
- 业务代码、业务测试、Spec Kit 产品语义、constitution、ROADMAP、身份验证 spec/playbook 和 `.gitignore` 没有本轮差异；`tools/` 仍保持原有未跟踪状态。
- 对当前相关的 52 份 Markdown 检查 174 个本地相对链接和显式锚点，缺失数为 0。
- `ApiRelay/scripts/selfcheck.sh` 红线检查通过。

结论：Git、范围、保护引用、链接和项目红线检查通过。

## 3. 阻塞问题

### R03-T1：黑盒测试基线被重复附加，现行验收标准不唯一

- 证据：`测试用例/01` 中四个控制章节标题——“黑盒规程”“正向命中”“不应误触发”“加载边界”——均各出现两次；第 99—171 行是较旧内容的整段重复，仍指向 R02 并缺少 R03 新增的权限与工具边界。
- 影响：同一测试文件同时给出强弱不同的两套通过标准，AI 可以选择较弱旧段宣告通过；R03 外部黑盒即使执行也无法形成可重复、唯一的验收证据。
- 与整改报告的偏差：`R03/02` 声称该文件已保留历史并加入 R03 重测要求，但没有披露重复现行规程；其记录的执行后 SHA-256 与现场一致，只能证明文件未在交接后变化，不能证明内容正确。
- 下一轮拟改位置：`测试用例/01-唤醒与误触发测试.md`；为防止同类问题再次漏检，还应在现有自动化测试或只读检查器中增加该文件控制章节唯一性与过期轮次语句检查。是否修改检查器由下一轮 7R 精确列明，不在本阶段修复。
- 最小修复：删除第 99—171 行的旧重复块，保留前半段 MAIC-1.2 现行规程、R01/R02 历史证据和 R03 待重测状态；不得改写历史结果。
- 回归：四个控制章节各只出现一次；R01、R02 历史各一份；只存在一套 R03 通过标准；确定性检查能在重复块或过期“R02 待重测”再次出现时非零停止。完成后再由 Codex 独立执行 B7-1 至 B7-5。
- 问题指纹：`test-baseline / duplicated-current-rules / stale-round-directive / ambiguous-acceptance-standard`。

## 4. 非阻塞建议

无。本轮不扩大到检查器拆分、措辞优化或产品功能。

## 5. 方法观察

R03 说明文件指纹、链接检查和通用单元测试只能证明“文件稳定、路径有效、程序行为正确”，不能证明人工验收基线没有整段重复。以后凡由执行 AI 重组测试规范，都应增加结构唯一性与过期阶段语句的确定性检查；发现这类结构阻塞后应先停止昂贵外部黑盒，修复基线后再运行一次有效样本，而不是用两套标准重复消耗额度。

## 6. 下一步

- 下一步负责人：Codex
- 是否需要用户批准：否（下一个 7R 只定稿，不实施）
- 下一步提示词：`/Users/xitongzhili/Desktop/MacTransfer/08_Code/E02_ApiRelay_Github/ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-多AI协作方法实战验证/提示词/阶段7R-Codex创建整改轮次.md`

本轮复验不通过，必须创建下一整改轮次。
