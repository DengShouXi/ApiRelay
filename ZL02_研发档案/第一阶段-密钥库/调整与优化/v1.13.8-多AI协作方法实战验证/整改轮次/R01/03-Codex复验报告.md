# R01 · Codex 复验报告

## 0. 身份

- 阶段：7T（Codex 独立复验 R01）
- 轮次：`R01`
- 时间：2026-09-11 20:10 CST
- 分支：`v1.13.8`
- 起始 / 当前 HEAD：`bb5257fd28baf7e06c9da916fe48b278e15ec384`
- 方法版本：MAIC-1.0
- 输入：`整改轮次/R01/01-Codex整改定稿.md`、`整改轮次/R01/02-Grok整改报告.md`、`测试用例/01-唤醒与误触发测试.md`、实际差异和 Git 状态
- 授权：用户明确执行阶段 7T；仅允许新增本报告，不是修复、提交或上传授权

## 1. 前置关口

1. 只读检查器退出 0，唯一定位 `nextPhase=7T`、`owner=Codex`；本报告创建前同轮 `01`、`02` 存在而 `03` 不存在。
2. 最近执行产物 `R01/02` 有独立行“写入状态：已停止”，没有并行写入者。
3. `R01/01` SHA-256 为 `f79a49dea1a7e91b7c79f4369b4bc263731ea0615cf98ab4cabb6566e1886e78`，与批准指纹完全一致。
4. `R01/01` 第 3.3 节列出的 19 份冻结文件逐项重算，全部与交接基线一致。
5. F7 已真实执行，不再是“未验证”：B7-1 至 B7-4 通过，B7-5 明确失败。因此符合“真实失败后进入 7T 给出复验结论”的启动条件。

## 2. 独立复验结果

### 2.1 F1—F6

- 使用 `PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest ZL00_项目总控/自动化/test_check_multi_ai_workflow.py` 独立重跑：35 项测试全部通过。
- 测试覆盖写入未停止、缺少停止标记、工作包任意文件、阶段 5 结论缺失、嵌套契约损坏和只读 worktree 脏污等 R01 反例。
- 实仓只读检查器退出 0，没有 traceback，方法版本、契约、产物身份和工作包范围均有效。

结论：F1—F6 通过。

### 2.2 F7 外部黑盒

- B7-1：新 Codex 任务正确唤醒方法、定位工作包并在异常 worktree 前停止，通过。
- B7-2：普通解释请求未误触发完整流程，通过。
- B7-3：新 Cursor 对话正确唤醒方法、定位工作包并停止，通过。
- B7-4：询问 AI 接口数量未误触发，通过。
- B7-5：在 `E03_VectorOps` 新任务中，模型先读取 E03 规则，随后却准备返回 E02 ApiRelay 的旧工作包。安全制止后确认没有写入、暂存、提交、上传或 Grok 执行，但跨项目隔离已经失效。

结论：F7 失败。该失败影响方法正确性，不能进入阶段 8。

### 2.3 Git、范围和保护边界

- 当前只有一个 worktree；F7 测试产生的两个临时 worktree 已在逐文件确认没有独有内容后，由用户明确授权移除。
- 当前分支和 HEAD 仍为 `v1.13.8@bb5257fd28baf7e06c9da916fe48b278e15ec384`。
- `main`、`origin/main` 均为 `539447ea9984608c74989da48b8770cb3fe32a6c`；`release/1.0.0` 为 `5c5eed3f8390f51f5a26be9737bcc11f72e7d7fd`。
- `v1`、`origin/v1`、`origin/v1.13.8` 均为基线 HEAD；本地不存在 `v1.13.9`。
- 暂存区为空，`git diff --check` 通过；业务代码、业务测试、constitution、ROADMAP、身份验证 spec/playbook 和 `.gitignore` 没有差异。
- `tools/` 仍是未跟踪保护路径，没有被纳入本轮。
- 对 46 份本次相关 Markdown 检查 232 个本地链接和锚点，缺失数为 0。
- 本机同名技能仅有 `/Users/xitongzhili/.agents/skills/multi-ai-collaboration/SKILL.md` 一份，未进入 Git 暂存范围。

结论：Git、范围、保护引用和链接检查通过。

## 3. 阻塞问题

### R01-T1：跨项目上下文污染

- 证据：B7-5 在 E03 项目中读取本项目规则后，又准备采用 E02 ApiRelay 的旧工作包。
- 影响：全局技能可能在另一个项目中引入错误路径、版本、计划或权限；继续执行会破坏项目隔离。
- 下一轮拟改位置：本机 `multi-ai-collaboration` 技能、ZL00/04 的跨项目边界、相应入口和黑盒用例。
- 必须增加的规则：当前任务所绑定的项目根是默认搜索和执行边界；父会话、历史任务、其他编辑器窗口或既有 Grok/Cursor 对话不能改变该边界。发现外部 AI 窗口属于另一仓库时必须停止并新建项目绑定会话，不能接管其旧工作包。
- 回归：在另一个自有规则仓库发送同一句话，完整一轮都不得读取、引用或返回 ApiRelay；验收不能只看回复前 20 行。
- 问题指纹：`project-isolation / reused-external-context / repository-boundary-bypass / cross-project-wrong-execution`。

### R01-T2：F7 证据结论与批准流程冲突

- 证据：`测试用例/01` 已记录 B7-5 真实失败，却仍写“不得创建 R01/03”；获批 `R01/01` 和阶段 7T 提示词规定“真实执行后明确失败”应进入 7T 并形成结论。
- 影响：下一位 AI 可能一边被检查器指向 7T，一边被证据文件要求禁止 7T，造成再次停滞。
- 下一轮拟改位置：`测试用例/01`、阶段 7T/7R 衔接说明；同时更新“临时 worktree 尚未删除”的过期现场描述。
- 回归：未验证时停止且不建 `03`；真实失败时允许 7T 创建 `03` 并转入下一轮；真实通过时允许 7T 给出通过结论。
- 问题指纹：`handoff-state / evidence-result-gate-conflict / contradictory-next-step / workflow-stall`。

### R01-T3：只读黑盒任务创建产生副作用

- 证据：最初以 worktree 方式创建只读黑盒任务时，复制了主工作区未提交内容并生成两个契约外 worktree，检查器因此退出 4；后续核实无独有内容并经用户授权才移除。
- 影响：只读验证本身改变工作区拓扑，制造额外清理和算力消耗。
- 下一轮拟改位置：F7 黑盒规程和多 AI 任务创建说明。
- 必须增加的规则：只读唤醒测试优先使用绑定现有项目的本地新任务/新对话，不创建 worktree；确需 worktree 时先登记、测试后核对独有差异，并取得删除授权。
- 回归：重复 B7-1/B7-2 时 `git worktree list` 前后不变。
- 问题指纹：`test-harness / read-only-test-side-effect / unregistered-worktree / avoidable-cleanup`。

## 4. 非阻塞建议

无。本轮只处理已证实的正确性和流程阻塞，不扩展措辞优化。

## 5. 方法观察

本次真实黑盒证明，仅检查“第一句话是否正确唤醒”不足以验证方法可靠性；必须观察完整一轮是否始终受当前项目边界约束。同时，测试编排工具自身也可能改变 Git 工作区拓扑，今后的只读黑盒计划必须把“测试前后拓扑一致”纳入验收。

## 6. 下一步

- 下一步负责人：Codex
- 是否需要用户批准：需要用户调用阶段 7R；这不是提交或上传授权
- 下一步提示词：`/Users/xitongzhili/Desktop/MacTransfer/08_Code/E02_ApiRelay_Github/ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-多AI协作方法实战验证/提示词/阶段7R-Codex创建整改轮次.md`

本轮复验不通过，必须创建下一整改轮次。
