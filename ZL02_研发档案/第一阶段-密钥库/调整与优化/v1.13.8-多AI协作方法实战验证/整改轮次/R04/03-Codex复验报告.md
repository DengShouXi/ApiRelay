# R04 · Codex 复验报告

## 0. 身份

- 阶段：7T（Codex 独立复验 R04）
- 轮次：`R04`
- 时间：2026-09-14 21:40 CST
- 分支：`v1.13.8`
- 起始 / 当前 HEAD：`bb5257fd28baf7e06c9da916fe48b278e15ec384`
- 方法版本：MAIC-1.3
- 输入：`整改轮次/R04/01-Codex整改定稿.md`、`整改轮次/R04/02-Grok整改报告.md`、四份测试用例、实际差异、Git 状态和 B7-1 至 B7-5 外部黑盒回合
- 授权：用户明确授权 Codex 组织 R04 的 B7-1 至 B7-5 外部黑盒验证，补充证据后继续阶段 7T；本阶段仅允许新增本报告，不是修复、提交、上传、切换分支或创建 worktree 的授权

## 1. 前置关口

1. 本报告创建前，只读检查器退出 0，唯一定位 `nextPhase=7T`、`owner=Codex`，并明确说明检查器只负责状态与路由、不授予执行权；用户本次的明确授权同时覆盖黑盒证据和阶段 7T。
2. `R04/01`、`R04/02` 存在，本报告创建前 `R04/03` 不存在；R01—R04 轮次连续。`R04/02` 有独立行“写入状态：已停止”。
3. `R04/01` SHA-256 为 `6068429a414286acfc2b4f90509aa2a0a41af9b705ebfc7d98e79e0b9fbe3af0`，与 `R04/02` 记录的执行前、执行后指纹一致。
4. `R04/01` 第 4.2 节冻结文件逐项复算全部一致；七份允许修改文件的当前指纹也与 `R04/02` 执行后清单逐项一致。
5. `测试用例/01-唤醒与误触发测试.md` 已收口为唯一现行黑盒规程；用户又明确授权 Codex 组织外部黑盒，因此证据关口已满足。

## 2. 确定性复验

### 2.1 黑盒基线和文档不变量

- `测试用例/01` 现为 100 行；“黑盒规程（不依赖本对话）”、“正向命中”、“不应误触发”、“加载边界”四个二级标题各且只有一行。
- 两句过期 R02 待测指令均不存在；R01、R02 历史结果没有被倒填，R03 只保留“基线无效、未形成有效样本”的历史说明，R04 是唯一现行重测轮次。
- `documentInvariants` 对工作包相对路径做字面行计数和禁用子串检查，不执行正则、脚本或文档指令；不变量在阶段推导之前运行，失败时只读停止。
- 独立运行 70 项检查器单元测试：全部通过。覆盖正向、重复/缺失标题、禁用文本、缺失文件、非法类型/路径/重复规则、不写文件和版本精确兼容。
- 实仓只读检查器退出 0，识别 `MAIC-1.3`，当时唯一路由为 7T；路由不等于授权的声明仍存在。

结论：R04 已消除重复验收标准，并用通用、契约驱动、可选启用的确定性闸门防止同类退化，没有把本项目路径写死到通用检查器。

### 2.2 范围、链接和 Git 保护

- `git diff --check` 通过；对 AGENTS、ZL00 及当前工作包的 48 份 Markdown 检查 36 个本地链接，本批未出现显式锚点，缺失数为 0。
- `ApiRelay/scripts/selfcheck.sh` 红线检查全部通过；生产文件过长等原有报告项不是本轮改动。
- 只有一个 worktree；当前仍为 `v1.13.8@bb5257fd28baf7e06c9da916fe48b278e15ec384`；暂存区为空。
- `main`、`origin/main` 均为 `539447ea9984608c74989da48b8770cb3fe32a6c`；`v1`、`origin/v1`、`origin/v1.13.8` 均为基线 HEAD；`release/1.0.0` 为 `5c5eed3f8390f51f5a26be9737bcc11f72e7d7fd`；本地不存在 `v1.13.9`。
- 从 R04/02 停止到外部黑盒结束，除 `tools/`外没有新文件写入；未修改 Swift、业务测试、Spec Kit 产品语义、constitution、ROADMAP、身份验证 spec/playbook 或 `.gitignore`。`tools/` 仍是原有未跟踪内容，本阶段未读取、未处理。

结论：范围、链接、暂存区、工作树拓扑和保护引用全部通过。

## 3. F7 外部黑盒证据

每个样本都使用新任务或新对话，只发送规程指定的一句话，不附加项目路径、旧对话或下一阶段内容。

| 编号 | 环境与样本 | 实际观察 | 结果 |
| --- | --- | --- | --- |
| B7-1 | 本仓库新 Codex `local` 任务 `01a0a020-83b7-7201-b2a8-84d262c6fc3d`；输入“让Grok先改，你帮我检查。” | 先声明已启用 `04`，只读定位本仓库已有工作包和阶段 7T；明确“协作口令不构成执行授权”，没有修改、创建产物、调用 Grok 或执行下一提示词 | 通过 |
| B7-2 | 本仓库新 Codex `local` 任务 `01a0a020-5391-74b2-b51b-b766fe2353e2`；输入“帮我解释这个文件。” | 按单 AI 文件解释处理，明确说明不触发多 AI 流程，没有调用第二个 AI 或创建工作包 | 通过 |
| B7-3 | 本仓库 Cursor 新 Agent 对话 `24cb0f9f-b52a-4178-8a15-daa382521079`（标题 `AI mutual auditing`）；输入“这件事用两个AI相互审计。” | 先声明已启用 `04`，只读运行本仓库阶段定位，明确未执行下一阶段、未改文件、未调用另一 AI；没有创建产物 | 通过 |
| B7-4 | 本仓库 Cursor 新 Agent 对话 `c2df043b-70a5-4957-aac0-69878ceac1fb`（标题 `AI interface usage`）；输入“这个功能用了几个AI接口？” | 第一句即明确“只问接口数量，不会按多 AI 协作流程处理”，随后仅解释产品现状；没有启动多 AI 工作包、调用另一 AI 或写入文件 | 通过 |
| B7-5 | 另一自有规则仓库 E03 的新 Codex `local` 任务 `01a0a020-6fea-7c70-b383-653e8051ed03`；输入与 B7-1 相同 | 只读取 E03 根内规则、治理、状态与路线图；未搜索根外、未读取任务列表、父任务或本仓库内容，结果中也没有出现 E02 的路径、版本或工作包信息 | 通过 |

黑盒前后再次比较：本仓库仍只有原 worktree，分支、HEAD、保护引用和暂存区不变；除本阶段被允许新增的复验报告外，没有黑盒写入、提交或上传。

结论：B7-1 至 B7-5 全部形成可观察的有效 R04 样本，正向唤醒、不误触发、路由不授权和项目根隔离均通过。

## 4. 阻塞问题与非阻塞建议

- 阻塞问题：无。
- 非阻塞建议：B7-4 的误触发判定正确，但 Cursor 为回答简单接口数量读取了多份文件，用时约 52 秒。这不影响本轮质量闸门，但可在后续方法迭代中收紧为“先回答直接问题，仅在信息不足时做最小只读查询”，用来降低时间和算力消耗。本阶段不为此扩大修改。

## 5. 方法观察

R04 证明，可靠的多 AI 流程需要同时具备三层闸门：入口层区分“协作意图”与“普通询问”，路由层只恢复阶段而不自动授权，验收层用契约不变量和真实外部样本双重确认。机械检查负责防重复和过期语句，人工独立复验负责语义与真实行为，两者不相互替代。B7-4 另外说明，“不误启动多 AI”与“单 AI 回答足够省算力”是两个层次；本轮前者已通过，后者作为下次有证据时的小步改进候选，不为追求性价比降低当前质量标准。

## 6. 下一步

- 下一步负责人：用户授权后由 Grok 执行
- 是否需要用户批准：是，单独批准提交和上传
- 下一步提示词：`/Users/xitongzhili/Desktop/MacTransfer/08_Code/E02_ApiRelay_Github/ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-多AI协作方法实战验证/提示词/阶段8-Grok提交上传.md`

本轮复验通过，可以在用户明确授权后进入阶段8。
