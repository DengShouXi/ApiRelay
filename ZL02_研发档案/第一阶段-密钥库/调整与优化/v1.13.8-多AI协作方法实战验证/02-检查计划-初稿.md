# 多 AI 协作方法实战验证：检查计划初稿

## 0. 身份

- 阶段：1（Codex制定初稿）
- 生成日期：2026-09-11 CST
- 分支：`v1.13.8`
- 起始HEAD：`bb5257fd28baf7e06c9da916fe48b278e15ec384`
- 输入：`00`、`00A`、`00B`、`01`、`测试用例/`、现行ZL00、AGENTS、Cursor规则和实际Git状态
- 状态：初稿；阶段2必须独立找遗漏

## 一、自动触发测试

至少覆盖以下说法：两个AI、多个AI、两个模型、两个编辑器、多个编辑器、Codex和Grok一起、Cursor与Codex协同、你们两个一起做、一个执行一个检查、交叉审计。分别验证AGENTS与Cursor`alwaysApply`入口都会路由到ZL00/04，且用户不必提供路径或旧聊天。

补充覆盖隐含接力说法：“交给另一个AI做，你来检查”“先让Grok执行”“让额度多的模型查、你来把关”“多个窗口配合”。验证触发后会优先定位既有工作包和有效阶段，不会重复建包或从阶段1重来。

检查本机`/Users/xitongzhili/.agents/skills/multi-ai-collaboration/SKILL.md`：描述能否在新Codex任务中覆盖上述意图；正文是否优先服从每个项目自己的规则；是否避免携带本项目路径、版本和决定；是否明确自动触发对未加载技能的工具无效；`~/.codex/skills`中是否没有同名重复副本。把它作为本地配置单独报告，不纳入Git提交演练。

还要验证：只说“上传”不会误触发多AI并扩大授权；外部编辑器未打开仓库根目录或未加载项目规则时，文档不会虚假承诺自动生效。

## 二、复杂场景压力测试

用`00A`逐项模拟：

- 旧计划说在13.8改代码，但现行权威说13.9；正确结果必须停止旧计划。
- `revealAuthEnabled`默认值未拍板；正确结果必须进入决定记录并阻止实施。
- 同时要求完善治理方法和实现密码功能；正确结果必须拆成ZL02与Spec Kit两个工作包。
- W0—W6高风险波次；正确结果必须允许为关键波次设置嵌套审计闸门。
- 当前有未上传治理修改；正确结果必须先关账前置工作，再创建13.9。

## 三、并发与交接测试

- 同工作区两个写入者必须被禁止；执行未停止时审计不得开始。
- 三个以上AI必须仍有一个协调者和一个当前执行者。
- 不同worktree必须记录唯一实施worktree和批准的整合方式。
- 每份产物必须含阶段、时间、分支、HEAD、输入和授权；身份不匹配的旧文件不能推进流程。
- 用户每步只复制当前提示词即可继续，不能要求复制旧回答。

## 四、可扩展失败轮次测试

模拟阶段7失败一次和连续失败两次：应依次创建`整改轮次/R01`、`R02`，每轮固定含定稿、整改、复验三份产物；不得覆盖旧报告，不得临时新增7A/7B/7C提示词，不得让阶段8读取较旧的通过结论。

## 五、权威、链接和防重复

- 完整方法正文只能在ZL00/04；AGENTS、Cursor、README、ZL01只路由或通俗说明。
- 工作包留在ZL02；附件不进入正式产品权威。
- 所有相对链接、文件名和阶段产物前置关系有效。
- ZL01/14不保存精确内部阶段；通用提示词从产物和Git推导。

## 六、Git与范围保护

- 阶段2—7T无暂存、提交、上传、分支切换或新建13.9。
- 业务代码、测试、Xcode工程、constitution、ROADMAP、身份验证spec和产品playbook语义无差异。
- main、release tag、历史提交和冻结分支不动。
- `tools/`只通过Git状态确认未跟踪，不读取或处理。

## 七、上传演练

- 内容提交A必须能由最终范围生成显式路径清单。
- A成功上传后，提交B严格只修改ZL01/13、ZL01/10、ZL01/14、BRANCHES四个文件；不得产生第三次递归记账。
- B成功后才能纯快进`v1`；阶段9以远程实时引用复核；不得同时创建`v1.13.9`。

## 八、质量、算力和学习闭环

- 用一个轻量任务、一个普通跨文件任务和密码高风险任务验证三级流程选择；不得把高风险任务降级。
- 验证额度较多的Grok承担扫描、机械核对、批量修改和测试，Codex聚焦边界、计划和独立验收；必要证据必须由Codex实际抽查。
- 检查提示词是否只要求读取当前阶段所需输入，是否避免复制聊天、重复全仓扫描和无变化报告。
- 每份阶段报告是否有简短“方法观察”；无问题时允许一句结束，有真实缺陷时必须留下证据、影响和候选改法。
- 当前安全/正确性缺陷必须阻塞并进入独立ZL02方法改进工作包；非阻塞建议可以积累，不能阻止当前任务关账或触发无限迭代。
- 验证跨项目入口只负责唤醒与寻找项目内规则，不携带本项目的路径、版本和产品结论。
- 评估是否需要并能安全实现不依赖第三方包的只读状态检查器；必须明确它的输入、输出、失败方式、权威边界和未来工作包复用方式。
- 检查固定任务契约是否只保存稳定事实，不与中文当前状态或阶段产物重复维护动态阶段。

## 九、黑盒与回归执行方式

- 阶段2先审查四份`测试用例/`是否覆盖真实风险，并补充遗漏，不实施检查器。
- 阶段5在不依赖当前聊天的前提下，用实际文件、模拟输入和Git证据执行测试；不得只搜索关键词后宣布通过。
- 阶段7重跑全部阻塞用例和保护范围。未变化的低风险用例可引用阶段5证据，但触发、阶段恢复、旧HEAD失效、执行者与审计者分离必须重新验证。
- 测试结果必须写清输入、预期、实际结果和证据；无法在当前工具中验证的项目必须标为未验证，不得推测通过。

## 十、通过标准

所有测试必须有实际文件或Git证据。任何自动触发缺口、双写风险、陈旧计划可直接执行、未决安全决定可越过、高风险流程被省略、执行者自我验收、方法缺陷无法积累或无限自我迭代、失败轮次不可扩展、断链或越界修改均为阻塞问题。
