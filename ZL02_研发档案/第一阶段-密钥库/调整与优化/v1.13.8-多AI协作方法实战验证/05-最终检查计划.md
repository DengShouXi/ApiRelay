# 多 AI 协作方法实战验证：最终检查计划

## 0. 身份

- 阶段：3（Codex 确定最终检查计划）
- 生成时间：2026-09-11 14:57 CST
- 分支：`v1.13.8`
- 起始 / 当前 HEAD：`bb5257fd28baf7e06c9da916fe48b278e15ec384`
- 输入：`00`–`04`、`00A`、`00B`、四份`测试用例/`、现行治理/入口文件、全部后续提示词和实际 Git 状态
- 方法版本目标：`MAIC-1.0`
- 状态：**等待用户确认**；本文件不是实施或上传授权
- 修订：2026-09-11 15:29 CST，增加下一步提示的正常链、条件分支、异常后备与绝对路径验证

## 一、检查原则

1. 检查对象是实际文件、Git 差异、脚本行为和黑盒结果，不是 Grok 的口述。
2. 机械项交给标准库自测、只读检查器和 Git；Codex 独立核对权威边界、风险、失败路径和关键样本。
3. 每项记录“输入—预期—实际—证据—结论”。无法真实验证时标记“未验证”，不猜测通过。
4. 安全边界、阶段唯一性、独立验收、未决事项和较新失败报告为阻塞项；措辞和可选易用性优化为非阻塞项。
5. 每个阶段报告的最终回复必须同时给出下一步负责人、批准要求和绝对路径执行句。检查不仅看“有下一步”字样，还要根据实际分支验证路径、负责人、批准闸门和条件是否正确。异常时必须指向通用阶段检查，不得推荐一个可能跳步的阶段。

## 二、阶段4实施者必须执行的检查

### 2.1 现场和范围

- 确认当前分支为`v1.13.8`、HEAD为`bb5257fd28baf7e06c9da916fe48b278e15ec384`、暂存区为空、只有契约记录的一个实施 worktree。
- 将实际修改/新增逐项与`04`第二节比较；清单外任何变化都停止。
- 确认暂存区仍为空，`tools/`仍只是未跟踪路径；不读取其内容。
- 保护 refs 保持：`main` / `origin/main`=`539447ea9984608c74989da48b8770cb3fe32a6c`，`release/1.0.0`=`5c5eed3f8390f51f5a26be9737bcc11f72e7d7fd`，阶段8前`v1`、`origin/v1`、`origin/v1.13.8`仍为`bb5257fd28baf7e06c9da916fe48b278e15ec384`，不存在`v1.13.9`。

### 2.2 静态和单元检查

```bash
git diff --check
python3 -m unittest ZL00_项目总控/自动化/test_check_multi_ai_workflow.py
python3 ZL00_项目总控/自动化/check_multi_ai_workflow.py --work-package "ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-多AI协作方法实战验证"
```

自测必须在临时 Git 仓库创建夹具，覆盖：正常阶段恢复、非仓库/缺契约/JSON错误、方法版本不符、旧 HEAD、缺前置却有后续产物、较新失败压过旧通过、异常暂存、保护路径差异、双写入声明、RNN断号/覆盖和非唯一工作包。测试结束不得往主仓库写入夹具或产物。

另作用 Python 标准库的链接检查，验证本次改动 Markdown 的本地相对路径和显式锚点。不为此增加第三方依赖或另一套长期脚本。

## 三、阶段5独立审计矩阵

### 3.1 唤醒与误触发

- 显式正例：两个AI、多AI、Codex+Grok、Cursor+Codex、多编辑器。
- 隐含正例：另一AI执行你检查、额度多的模型干重活、复核完再上传、多窗口配合。
- 负例：解释文件、改错字、“用了几个 AI 接口”、“上传流程是什么”、明确只让单 AI 修一个小文件。
- 每个样本检查 AGENTS、Cursor、本机 Codex 技能或自包含提示词中的预期路由。正例必须先声明启用、找到已有工作包且不扩大上传权；负例不得强制建包。
- 在可行的新 Codex 任务和 Cursor 新对话中黑盒运行；无法新建时标未验证，不拿当前对话冒充。

### 3.2 契约、版本和阶段恢复

- JSON 契约字段与模板一致，不含手写动态阶段；`workPackageId`、工作区、分支、HEAD、允许/禁止范围和保护 refs 符合本计划。
- ZL00/04 只有一个`方法版本：MAIC-1.0`，契约引用一致；更改行为却不升版必须报错。
- 无聊天时，检查器对“已有最终计划、已实施、审计失败、整改后、验收通过、A/B后”都只输出一个下一阶段/负责人/提示词。
- 旧 HEAD、身份不匹配、缺产物、更新的失败报告、RNN异常、范围异常、暂存异常和保护 refs 异常都安全停止。

### 3.3 单写入者与交接

- 契约只有一个实施 worktree，阶段所有者唯一；三个以上 AI 时其他角色只交只读报告。
- 模拟两份同时进行的写入声明或两个 worktree 都自称实施点，检查器必须非零停止。
- 文档如实说明无法从仓库内锁住外部编辑器；不得把事后检测宣传为实时互斥锁。

### 3.4 新鲜度、拆包、未决事项和高风险

- 用`00A`测试：陈旧“13.8 改代码”被现行`13.9`路牌拦下；附件不得取代 spec。
- `revealAuthEnabled`默认值未决时，规格定稿和代码实施均被拦下，且只指向用户决定与权威回写。
- 一条指令同时要“改方法 + 实施密码”时，得到 ZL02 方法包与 Spec Kit 产品包，只记依赖，不复制方案。
- 轻量错字使用简化流程并留另一角色短报告；普通跨文件使用完整包；密码、迁移、CloudKit、删除、发布和历史不得降级。
- W0–W6 样本必须生成依赖波次和可操作的“执行—针对审计—放行”闸门；波次失败不能继续，末尾仍需完整阶段5/7。

### 3.5 额度与质量

- 确认 Grok 做广搜、机械核对、批量修改和重复测试，Codex 做范围、冲突、风险、定稿和独立抽查。
- 确认两个模型没有重复通读无关聊天、重复跑未变全仓扫描；但 Codex 实际重跑安全边界、旧 HEAD、独立性和关键失败样本。
- 任何节省都不能删除用户批准、独立验收、保护范围或高风险闸门。

### 3.6 方法学习和 RNN

- 无问题只写一句；有问题必须含证据、影响、拟改位置、回归和指纹。
- 相同指纹追加证据而非重复建档；一个安全/正确性阻塞或两个独立包的同类非阻塞证据才晋级。
- 晋级后使用独立 ZL02 方法包和另一角色验证；执行者不得自改 ZL00 后自验。
- 连续两次失败必须得到`R01`、`R02`各自完整的三份产物，旧文件未覆盖，阶段8只认最新通过结论。
- 提示词/契约本身是阻塞时，7R 可精确批准修复；扩大战略、产品语义或超出最终计划则退回阶段3。

### 3.7 权威、中文可读性和链接

- 方法正文只在 ZL00/04，自动化只在 ZL00/自动化；AGENTS、Cursor、README、ZL01不得再复制完整矩阵或流程。
- ZL01/14 只用通俗中文说“在整理什么、下一步去哪”，不要求用户读 JSON、Git hash 或内部阶段规则；精确恢复由检查器/通用提示词承担。
- 修改过的 Markdown 本地链接、显式锚点和目录均存在；旧工作包只是历史实例，当前完整技术过程明确指向本包。

### 3.8 本机技能

- 位置唯一为`/Users/xitongzhili/.agents/skills/multi-ai-collaboration/SKILL.md`，`/Users/xitongzhili/.codex/skills/`无同名副本。
- 只允许`04`第十三节的负例和项目优先句最小调整；不携带 ApiRelay 路径、版本或产品决定。
- 技能差异在报告中单列，永远不出现在提交 A/B 路径或暂存区。

### 3.9 下一步提示稳定性

- 正常链路逐项验证：1→2→3→用户批准→4→5→6（有阻塞时）→7→8（另行授权）→9。
- 验收失败必须指向7R；7R定稿后先等用户批准再指向7S；7S指向7T；7T再失败指回7R，通过则等待上传授权后指向8。
- 任一阶段发现已有自己的产物、前置缺失、HEAD/方法版本错配或下一步不唯一，必须给出`通用-只检查当前阶段.md`的绝对路径，不直接推荐某一业务阶段。
- 阶段9通过时必须写“当前工作包已关账，无自动下一步”；失败时说明外部条件修复后重新执行阶段9，不自动创建`v1.13.9`。

## 四、阶段7最终验收

阶段7必须重跑所有阻塞用例和完整保护边界；触发、无聊天阶段恢复、旧 HEAD/较新失败、单写入者、未决事项、执行/验收分离和检查器只读性必须实际重验。阶段5后未变的低风险排版可引用证据，但要确认差异未覆盖它。

必须明确回答：陈旧13.8附件、未决默认值、W0–W6、跨来源拆包、多编辑器、自动唤醒/误触发、阶段恢复、故障停止、额度分工、学习闭环、RNN和分支关账是否都通过。任一关键项未验证或失败，最终结论必须不通过。

## 五、提交 A/B 和远程演练

阶段5/7在不暂存的情况下生成 A 的显式候选路径，验证每个路径都来自`04` 2.1/2.2且实际通过；本机技能、`tools/`、业务代码、spec、constitution、ROADMAP、`.gitignore`为禁止项。

模拟 B 时只许`ZL01/13`、`ZL01/10`、`ZL01/14`、`BRANCHES.md`，内容只能引用已发生 A 的 hash/远程事实，不预写 B 自己或阶段9。只有 A/B 都成功后才演练`v1`纯快进；不能纯快进则必须停止。

阶段9必须用不改本地状态的远程实时查询验证 A/B、`v1.13.8`、`v1`、`main`、tag和无`v1.13.9`。网络或权限失败时写“无法验证”，不宣布关账。

## 六、阶段产物与通过标准

| 阶段 | 产物 | 最小检查 |
| --- | --- | --- |
| 4 | `06-Grok本地实施报告.md` | 本节第二部全部 + 四组用例自测 + 保护边界 |
| 5 | `07-Codex独立审计报告.md` | 第三部全部，列阻塞/非阻塞 |
| 6 | `08-Grok整改报告.md`（按需） | 只重跑受影响项 + 完整保护边界 |
| 7 | `09-Codex最终验收报告.md` | 第四部；最新有效结论唯一 |
| 7R/S/T | `整改轮次/RNN/01`–`03` | 精确失败范围 + 受影响用例 + 完整保护边界 |
| 8 | A/B 提交与对话报告 | 第五部；用户单独授权 |
| 9 | 对话内可重复的只读复核 | 远程实时状态；无法验证不关账 |

阻塞标准：越界、断链、重复权威、方法版本漂移、非唯一阶段、可写检查器、需第三方依赖、误触发/漏触发、双写无法拦截、陈旧计划可执行、未决安全值可越过、高风险降级、执行者自验、新失败被旧通过覆盖、候选无法去重/晋级、RNN无法修复方法自身、保护 refs/分支/暂存异常或本机技能进入提交。

全部阻塞项通过，且没有未验证的关键项，才能在阶段7给出通过句。验收通过仍不等于提交或上传授权。

## 七、方法观察

阶段2发现回归样本分散在`00A`和`02`，只按四份测试文件执行会遗漏陈旧13.8、未决默认值和跨来源拆包。本计划将它们强制收口到四组用例并纳入阶段5/7的阻塞矩阵；指纹类别为“测试基线 / 用例分散 / 验收遗漏”。
