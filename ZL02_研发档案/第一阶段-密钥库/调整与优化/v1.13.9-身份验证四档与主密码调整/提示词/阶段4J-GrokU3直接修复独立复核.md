# 阶段4J：Grok集中复核U3直接修复

> **已封存（2026-09-23）：** 本提示词属于旧 MAIC-1.3 阶段链，只保留为历史过程，不再用于判断当前阶段、分派角色、修改代码、提交或上传。现行结论与路由见 [19-现行结论与历史材料边界.md](../19-现行结论与历史材料边界.md)。除非用户针对新范围重新明确授权并刷新计划，否则不得执行下文。

本提示词由Grok执行。仅审计本次Codex直接修复，不重复旧轮次计划，不执行原W6，不提交上传。

新增前置：读取18-U4手机循环解锁修复.md，审计最新场景绑定及真实async锁屏路径；旧99项不能代替U4测试。17的旧代码指纹不更新为“通过”，只有取得对应新构建真实观察才重录。重点复测原手机系统验证成功后进入内容、不循环，取消及后台不放行。

启用ZL00/04的MAIC-1.5先交付流程。当前方法由Codex修订，尚待独立复核，旧S1快照不覆盖新指纹；本次把方法及U3实现集中检查，不逐个文案来回交接。审计先跑最小完整操作，再检查记录，不能只看AI报告。无可用运行环境明确保留缺证据，不重建计划。

只读运行`PYTHONDONTWRITEBYTECODE=1 python3 -B ZL00_项目总控/自动化/check_delivery_evidence.py --work-package "ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.9-身份验证四档与主密码调整"`。当前17-U3用户操作证据.json观察为空，应明确阻断功能验收，不伪造填满。运行环境/设备具备才填写实际观察及包内证据（列为本阶段允许新增的验证记录）；无设备只明确待用户实测，不反复返回计划。方法自测不是产品实测，方法待独立复核不意味着代码修复必须重新规划。

先读取本工作包14-U3实测要求与局部界面修复.md及04/05中的U3增补，再读正式identity-auth/spec.md的U3现行条款。核实当前分支v1.13.9，保留现有未提交修改，不读取或处理tools/及工程manifest，不改main/tag/历史。

重点审计SettingsView.swift、RevealGate.swift、Localizable.xcstrings、RevealGateTests.swift当前U3实现：四项固定名及顺序；默认设备验证；组合先纯生物、失败后设备/应用两种显式路径；设备密码只由系统验证；应用密码只校验现有材料，普通入口不设密；取消、后台、换窗无授权；系统sheet短暂inactive仅暂停写入、原窗口恢复才继续，真实后台撤销不可恢复；Keychain复查及偏好persist成功状态不混淆；已有密码页面无重复改密表单且状态清晰。检查所有调用入口是否真实经过该实现，而不是只改标签。

读取14中实际测试证据；缺失或过期须重新定向测试，不得以注入单元测试代替原手机保存及系统回退/视觉真机证据。代码正确但实测尚缺，明确告诉用户还需验证，不能宣称全部完成。改名导致旧静态断言失效时区分过时断言与真实回归，禁止删除安全测试来获得通过。

本阶段先只读审计；报告安全阻塞与普通细节。普通范围内已确定原因的小修按现行方法直接修并定向测试，不引入新产品决定；安全/架构问题停止，不擅自扩大整改。写入本包15-GrokU3集中独立复核报告.md，列明实际读写文件与测试。

完成后唯一下一步交Codex核实报告与最新证据（有阻塞则处理阻塞，不跳上传）：

执行 `/Users/xitongzhili/Desktop/MacTransfer/08_Code/E02_ApiRelay_Github/ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.9-身份验证四档与主密码调整/提示词/阶段4K-CodexU3复核收口.md`
