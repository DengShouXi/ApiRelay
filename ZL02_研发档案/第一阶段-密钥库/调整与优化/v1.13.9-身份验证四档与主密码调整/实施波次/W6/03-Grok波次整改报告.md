# 阶段4：Grok波次整改报告（W6）

写入状态：未停止

- 阶段：4
- 波次：W6 整改（关闭 `实施波次/W6/02-Codex波次审计报告.md` 的 B2，给出 B1 人工填写表，并记录 B3 只读来源证据）
- 时间：2026-09-15
- 分支：`v1.13.9`
- HEAD：`6ec5ff7c67547637bbd55f528276422395e28c03`
- 方法版本：`MAIC-1.3`
- 工作包：`ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.9-身份验证四档与主密码调整/`
- 输入：已用于 W0—W6 的修订版 `04-最终执行计划.md`、`05-最终检查计划.md`、`00C-任务契约.json`、`实施波次/W6/01-Grok实施报告.md`、`实施波次/W6/02-Codex波次审计报告.md`
- 实际授权：用户执行 `提示词/阶段4-Grok执行下一波.md`。W6/02 `失败类型：普通整改`。本轮只修该报告列出的本波阻塞，创建本文件，不覆盖 01/02，不进入 4B，不处理 `manifest` / `tools/`。

文末 `写入状态：已停止` 为交接用最终状态。

## 1. 入口核对

- 当前分支 `v1.13.9`，HEAD 仍为契约基线 `6ec5ff7c67547637bbd55f528276422395e28c03`。
- 暂存区空；单一 worktree；未提交、未上传、未换分支。
- `main` / `origin/main` = `539447ea9984608c74989da48b8770cb3fe32a6c`；`release/1.0.0` = `5c5eed3f8390f51f5a26be9737bcc11f72e7d7fd`。
- 上一写入者：W6/02 已停止，结论为普通整改。本文件为下一个连续奇数报告，创建时已有文首草稿，本轮写完全文后停止。
- 未读取、未修改 `tools/`。未改 Swift。未创建阶段 4B 的 `06-Grok本地实施报告.md`。未读取 `xcodecloud/manifest.json` 正文，未删除、暂存、移动或改契约白名单。
- 未改 `04` / `05`。未把检查器退出码 4 写成已通过。

## 2. B2 已关闭：状态页拆开验收闸门与后续上传

只改 `ZL01_具体说明/14-项目当前状态.md`（W6 精确允许清单内）。不改代码、不改产品答案。

Codex 点名的冲突是：原先把「锁矩阵三端手测、CloudKit 双设备手测、Archive 1.0.1 (9)」捆成一行，并写「不作为本次上传条件」。那会让人以为可以跳过本次身份验证手测去上传。

现文案拆成四类：

| 事项 | 现写法 |
| --- | --- |
| 身份验证三端手测与双设备缺密码材料 | 本次验收尚未完成，缺一项即阻塞验收；必须测本机新包，不是旧 TestFlight |
| 历史锁矩阵三端手测（13.6/13.7） | 仍欠产品负责人；与本次身份验证手测分开记 |
| CloudKit 卫生双设备手测（T008） | 仍欠产品负责人；不是本次身份验证验收的替代项 |
| Archive 1.0.1 (9) / 商店上传 | 须另句授权，不是当前动作 |

§3 下一步同步写成：先用本机新包做完身份验证三端和双设备缺材料检查；未另授权前不提交、不上传；Archive 必须另句授权。§2 仍写「还要完成本机验收（含真机和双设备手测）」。本页不写内部波次号。

`git diff --check` 对该文件通过。本轮未重跑 358 项测试：没有改 Swift。

## 3. B1 未关闭：人工填写表（结果列全部留空）

按 W6/02 第 2、4 条：本报告只准备清单和空表。准备了清单但没有结果，不构成补齐。Grok 不能代替用户勾选。设备不可用时保持「未验证」，不得自动打勾。失败项另附截图或错误原文。

### 3.1 谁做、测哪个包

- **谁做：** 产品负责人在真机上做。任何 AI 不得代测系统弹窗、Face ID / Touch ID、多窗或双设备 CloudKit。
- **测哪个包：** 从当前 `v1.13.9` 工作区用 Xcode **Run** 装到真机的本机新包。不要用商店或旧 TestFlight 包冒充本次身份验证结果。
- **怎么记：** 每行填设备/系统、测试包来源、操作、预期、实际、日期。实际与日期未填 = 未验证。
- **缺一项：** 仍阻塞 W6 通过、阻塞阶段 4B 的 `06-Grok本地实施报告.md`。358 项单测不能替代。
- **不要现在交阶段 4A：** 空表再审计只会空转。有填写结果后，把结果发回并再执行阶段 4，由 Grok 写入下一个奇数报告后再交 4A。未填结果时不要再贴空的阶段 4。

### 3.2 覆盖范围

下表覆盖 `05` 第八节人工证据 1–7，以及第七节界面检查中的 VoiceOver、Dynamic Type、iPhone/iPad/Mac 布局。

| # | 设备/系统 | 测试包来源 | 操作 | 预期 | 实际 | 日期 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | （待填：iPhone 型号与系统） | （待填：Xcode Run 本机新包 / 若误用旧包须写明） | 设备验证档：成功取出明文 | 系统弹窗过设备主人后才看到/复制；取消则零副作用 | （待填） | （待填） |
| 2 | 同上 | 同上 | 设备验证档：取消系统弹窗 | 不取出、不复制、不改数据 | （待填） | （待填） |
| 3 | 同上 | 同上 | 组合档：生物取消 | 立即结束；不回落设备密码；不自动出现应用密码框 | （待填） | （待填） |
| 4 | 同上 | 同上 | 组合档：点「使用应用密码」并提交正确口令 | 只在已绑定待办时出现该按钮；正确口令完成原操作 | （待填） | （待填） |
| 5 | （待填：iPad 型号与系统） | 同上 | 与 1–4 相同 | 与 iPhone 相同 | （待填） | （待填） |
| 6 | 同上 | 同上 | 分栏或关掉详情后再复制/再查看 | 详情关闭后授权已清；须重新走当前验证方式 | （待填） | （待填） |
| 7 | （待填：Mac 型号、系统、有无 Touch ID） | 同上 | 组合档 / 设备验证：Touch ID 有或无 | 有 Touch ID 时走生物；Mac 登录密码只在设备验证档出现，组合档取消不弹登录密码 | （待填） | （待填） |
| 8 | 同上 | 同上 | 快捷键复制、第二个窗口打开另一详情 | 不得复用第一个详情的已验证授权 | （待填） | （待填） |
| 9 | iPhone | 同上 | 设为不验证；自动锁曾开启 | 冷启动/回前台直接进列表，无假锁屏 | （待填） | （待填） |
| 10 | iPad | 同上 | 同 9 | 同 9 | （待填） | （待填） |
| 11 | Mac | 同上 | 同 9 | 同 9 | （待填） | （待填） |
| 12 | iPhone | 同上 | 从不验证改回有验证档 | 原先自动锁设置重新生效 | （待填） | （待填） |
| 13 | iPad | 同上 | 同 12 | 同 12 | （待填） | （待填） |
| 14 | Mac | 同上 | 同 12 | 同 12 | （待填） | （待填） |
| 15 | iPhone | 同上 | 同详情：先查看再立即复制 | 取用开启且非不验证时不二弹；显示明文 ≠ 已授权 | （待填） | （待填） |
| 16 | iPad | 同上 | 同 15 | 同 15 | （待填） | （待填） |
| 17 | Mac | 同上 | 同 15 | 同 15 | （待填） | （待填） |
| 18 | iPhone | 同上 | 关详情 / 换密钥 / 真正离开前台后再复制 | 须重新走当前验证方式 | （待填） | （待填） |
| 19 | iPad | 同上 | 同 18 | 同 18 | （待填） | （待填） |
| 20 | Mac | 同上 | 同 18 | 同 18 | （待填） | （待填） |
| 21 | （待填：设备 A / 设备 B，同一 iCloud） | 两台都是本机新包 | A 设应用密码档并同步到 B；B 无本机密码材料 | B 保持锁定；可经设备主人恢复后设密；不能静默放行 | （待填） | （待填） |
| 22 | （待填：至少一端真机） | 本机新包 | 备份导入/导出：不验证 | 只需破坏性/文件确认，不弹身份框；取消零副作用 | （待填） | （待填） |
| 23 | 同上 | 同上 | 备份导入/导出：设备验证、应用密码、组合档 | 确认后走当前方式；组合档取消不回落设备密码；取消任一步零副作用 | （待填） | （待填） |
| 24 | 同上 | 同上 | 单条永久删除：不验证 | 先破坏性确认，再删除；取消确认则条目仍在 | （待填） | （待填） |
| 25 | 同上 | 同上 | 单条永久删除：三种有验证档 | 顺序为破坏性确认 → 身份验证 → 删除；取消任一步零副作用 | （待填） | （待填） |
| 26 | iPhone | 本机新包 | VoiceOver 读设置三行与组合档按钮 | 标签能区分验证方式、暂停提示、使用应用密码 | （待填） | （待填） |
| 27 | iPhone | 同上 | Dynamic Type 最大档 | 设置行与详情不互相遮挡，仍能完成查看/复制 | （待填） | （待填） |
| 28 | iPhone / iPad / Mac | 同上 | 四档设置页与详情布局 | 三行各管一件事；ⓘ 贴标题右侧；分栏/多窗不错位 | （待填） | （待填） |

当前任务不做 CloudKit Production 部署。第 21 行测的是已登录同一 iCloud 的两台真机上的本机材料隔离，不是商店发布。

## 4. B3 未处理该文件：只读来源证据，等待用户处置

路径：`ApiRelay/ApiRelay.xcodeproj/xcshareddata/xcodecloud/manifest.json`

本轮 **没有** 读正文、删除、暂存、移动、改 `00C` 白名单或把该路径加入允许修改清单。

| 只读检查 | 结果 |
| --- | --- |
| `git status --porcelain` | 目录为 `??`（未跟踪）。xcodeproj 下仅此未跟踪项 |
| `git ls-files` 该精确路径 | 未知于 Git（`did not match any file(s) known to git`） |
| `git log --all --` 该路径 | 空；从未入库 |
| 已跟踪的 `xcshareddata` | 只有 `xcschemes/ApiRelay.xcscheme` |
| `git check-ignore -v` | 无输出；未被 ignore |
| `stat` | size `163`；birth/mtime 均为 `2026-09-15 12:36:03`；权限 `-rw-r--r--` |
| `00C` `forbiddenPrefixes` | 含 `ApiRelay/ApiRelay.xcodeproj/` |
| `00C` `knownUntracked` | 只有 `tools/`，不含该路径 |
| `04` 全局禁止 | 含 `ApiRelay/ApiRelay.xcodeproj/` |

时间关系（不能据此指名创建者）：

- 本对话开始时的工作区快照已把它列为未跟踪，早于 W6/01。
- W6/01 也把它记为原有未跟踪，并写明未纳入、未删除。
- birth `12:36:03` 早于 W6 自动测试（约 12:56 之后）。因此 **不是本轮 W6/03 新建**，也 **不是 W6/01 为了过检查而放入暂存区**。
- 不能从上述证据断定是用户在 Xcode Cloud 界面生成，还是某次本机 Xcode / `xcodebuild` 副作用。创建者未知。

检查器退出码 4（`不在允许范围的差异: …/xcodecloud/manifest.json`）在该文件仍未跟踪、且前缀被禁止时是 **预期红结果**。不得把它标成已通过，也不得为了变绿而扩白名单或 `git add`。

### 请用户三选一（本轮不代选）

1. **继续保持未跟踪，永远不要 `git add`。** AI 继续不理它。检查器仍会退出码 4；下次审计应把「已决定保留未跟踪」视为处置完成，而不是再要求处理该文件。
2. **用户自己删掉该文件**（Finder 或 Xcode）。AI 仍不得删。删后检查器才可能不再报这一条。
3. **若要入库或改检查器白名单：** 必须回阶段 3 修订 `04`/`00C` 并重新批准。本轮明确不授权。

在用户写出上述选择之前，B3 只做到「来源证据已记录、边界已写清」，不算用户处置完成。

## 5. 本轮实际修改

| 文件 | 做什么 |
| --- | --- |
| `ZL01_具体说明/14-项目当前状态.md` | 关闭 B2：拆开本次必需手测、历史手测、T008、Archive |
| 本文件 | B1 空表；B3 只读证据与待决处置 |

未改 Swift、宪法、正式规格、`04`/`05`/`00C`、`tools/`、xcodeproj。未暂存、未提交。

## 6. 收尾四问

1. **外部资源：** 本轮没有新碰 Keychain / SwiftData / CloudKit / UserDefaults / App Group / 文件系统写入（除本报告与 `14` 文案）。未打开 `manifest.json` 正文。测试隔离不因本轮变化；未重跑 358。
2. **失败路径：** 身份验证手测仍全部未填，阻塞验收；用户若拿旧 TestFlight 测，会看到旧默认，不能当作本机新包通过。B3 在用户未选择前，检查器会继续红。提交/Archive 仍须另句授权。没有新的产品失败文案。
3. **接缝：** 未新增 protocol，未改测试替身。
4. **文件粒度：** `14-项目当前状态.md` 仍远低于 400 行。本报告因人工表变长，不顺手拆。

## 7. 未验证 / 仍阻塞

- B1：第 3.2 表实际/日期全空 → 未验证，阻塞 W6 通过和 `06`。
- B3：用户尚未选择保留或自行删除 → 处置未决。检查器退出码 4 仍在。
- 不因此改实现。不得进入 4B。不得把「用户以后可以接受风险」写成已豁免。

## 8. 方法观察

审计把「状态页沿用旧上传口径」和「检查器红结果被说成本波无关」分成两类阻塞是对的。前者可以在允许文件里改字；后者没有创建者证据时只能停下来让用户选，不能靠扩大白名单假装范围检查全绿。空的人工表不能再拿去审计。

下一步负责人：用户（填写第 3.2 表，并在第 4 节三选一）
是否需要用户批准：需要用户完成手测并给出 B3 处置；不需要批准计划修订
下一步：有填写结果后再执行 `/Users/xitongzhili/Desktop/MacTransfer/08_Code/E02_ApiRelay_Github/ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.9-身份验证四档与主密码调整/提示词/阶段4-Grok执行下一波.md`。现在不要执行阶段 4A，也不要再贴空的阶段 4。

写入状态：已停止
