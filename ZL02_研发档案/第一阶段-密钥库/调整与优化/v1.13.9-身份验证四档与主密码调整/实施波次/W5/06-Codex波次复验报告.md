# 阶段4A：Codex 波次复验报告（W5）

写入状态：未停止

- 阶段：4A
- 波次：W5 第二次整改复验
- 审计对象：`实施波次/W5/05-Grok波次整改报告.md`及其对应本地改动
- 时间：2026-09-15
- 分支：`v1.13.9`
- HEAD：`6ec5ff7c67547637bbd55f528276422395e28c03`
- 方法版本：`MAIC-1.3`
- 审计方式：Codex 独立只读审计；未修改被审计实现、测试或正式规格

## 1. 交接与保护边界

- W0—W4已通过，最早未通过波次仍是 W5。
- W5/05 已写`写入状态：已停止`，并正确引用 W5/04 五个阻塞；分支、HEAD、方法版本和授权链匹配。
- W5 下原不存在`06-Codex波次复验报告.md`；本报告按连续偶数编号新建，未覆盖以前证据。
- 暂存区为空，仅一个 worktree；`main`、`origin/main`和`release/1.0.0`保持契约固定对象。
- `git diff --check`通过；未读取或修改`tools/`，未切分支、暂存、提交或上传。
- W5/05 声明的修改文件在已批准 W5 精确范围内，未发现需要新增产品决定或扩大计划的情况。

## 2. W5/04 实现阻塞复验

### B1 待办泄漏：已关闭

- `VaultHomeViewModel.abandonCombinationPending()`统一取消当前验证并清空原操作、入口显示、口令页、设密状态和交付的明文。
- 关详情、换 key、真正离前台、自动锁、会话锁、窗口销毁和开始编辑的七个生产失效入口都同时放弃组合档待办。
- `VaultHomeView`切换顶层页签时也会放弃待办；设置页和三类备份页在离开时分别清空各自待办。
- 新测试覆盖了“无待办不启动”、“关详情后待办消失”、“会话锁后待办消失”和“放弃后不能再打开口令页”。

### B2 无待办按钮：已关闭

- `CombinationExplicitAuth.shouldShowExplicitEntry`只在“当前策略是组合档且已绑定原操作”时允许显示应用密码入口。
- Vault、设置、清空和三类备份页都传入自己的当前待办状态；无待办时不再显示无反馈的“武装下一次”按钮。
- App 锁保留显式入口是合理例外，因为锁屏本身就是已绑定的当前解锁待办。

### B3 旧授权残留：已关闭

- `.unauthenticatedDisplay`和`.viewCancelledOrFailed`现在都将已有`DetailRevealReuse`清空。
- 查看取消、密钥缺失和其他失败路径都进入`.viewCancelledOrFailed`。
- 安全偏好成功持久化后，`VaultHomeView`接收统一通知并清除已有取用授权。
- 测试已增加“先建立旧授权，再发生无验证显示、取消/失败或安全设置变化”的反例，三者都不再允许复用。

### B4 七种真实失效事件证据：实现接线已关闭，运行结果仍受 B5 限制

- 七种事件已分成七个生产方法；每个方法同时清取用授权和组合档待办。
- `KeyDetailView`、`VaultHomeView`、自动锁/会话锁均显式调用相应生产方法。
- 行为测试逐个调用七个生产事件方法；配线回归测试绑定了实际 SwiftUI 事件位置与这些方法的调用符号，删掉当前某一接线会使该测试失败。
- 源码符号断言仍是较脆弱的回归手段，但它与七个生产方法的状态行为测试结合，能关闭本波“仅直接传 reason”的具体缺口。后续 W6 仍需用人工多端/多窗矩阵验证真实 SwiftUI 生命周期。

## 3. 仍未关闭的阻塞：Xcode 许可使目标测试无法启动

- Codex 独立执行 W5 要求的`KeyVaultServiceTests`、`AppPrivacyControllerTests`、`RevealGateCoordinatorTests`、`RecentlyDeletedBatchTests`、`DataLifecycleTests`、`SecureBackupTests`和`ConsumerToolSessionLockTests`时，`xcodebuild test`在启动测试前显示“You have not agreed to the Xcode and Apple SDKs license”并等待交互。
- 本次已立即中止，未显示或接受许可正文，未使用`sudo`，未使用旧`xcresult`。
- 因此本波仍无可证明当前代码可编译、目标测试可运行且新用例全绿的当前证据，也没有可记录的测试数、失败数或本轮`xcresult`路径。
- 按已批准`05-最终检查计划.md`，这一项是阻塞；本波不能因静态审计和项目自检通过而判为通过。

## 4. 独立检查结果

- `bash ApiRelay/scripts/selfcheck.sh`：红线全部通过。
- `git diff --check`：通过。
- 共享分流、组合档显式入口、待办销毁、旧授权清理和七种失效方法：已核对生产调用与测试断言，未发现 W5/04 之外的新结构阻塞。
- 目标测试：未启动，退出原因为 Xcode/Apple SDK 许可未接受。

## 5. 整改与停止要求

1. 用户需在本机亲自阅读并接受 Xcode/Apple SDK 许可；AI 不得代替用户执行该动作。
2. 许可处理后，Grok 只重跑 W5 规定的七组目标测试和必要编译，记录精确测试数、失败数、结果路径和与 W5 阻塞对应的用例。
3. 若测试失败，只修与 W5 本轮实现直接相关的失败，然后重跑；不进入 W6，不修计划外问题。
4. 本波不需要再修订`04`或`05`，不修改已确认关闭的实现，除非新运行结果提供了明确失败证据。

## 6. 非阻塞建议与未验证项

### 非阻塞建议

- 源码字符串配线断言是过渡性保护，不应成为长期 UI 测试架构；可在未来已授权的 UI 可测性改造中替换，不在本波扩大范围。
- 不因超长文件开启无关重构。

### 未验证项

- W5 目标测试和编译结果。
- VoiceOver、Dynamic Type、iPhone/iPad/Mac 布局、真实 Face ID/Touch ID、多窗和双设备同步；这些仍属 W6 人工矩阵。

## 7. 方法观察

本轮不再发现新方法缺陷；实现阻塞与环境阻塞已能分开记录。为避免重复消耗，许可处理前不应再重复调用阶段4A；下一轮应由 Grok 只补当前测试证据，然后 Codex 一次复验。

失败类型：普通整改

下一步负责人：Grok
是否需要用户批准：否（但需要用户亲自完成 Xcode/Apple SDK 许可确认）
下一步：执行 `/Users/xitongzhili/Desktop/MacTransfer/08_Code/E02_ApiRelay_Github/ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.9-身份验证四档与主密码调整/提示词/阶段4-Grok执行下一波.md`

写入状态：已停止

波次审计不通过，只能整改本波阻塞。
