# 阶段4A：Codex波次审计报告（W0）

- 阶段：4A
- 波次：W0（规格唯一化与当前路牌）
- 日期：2026-09-14
- 分支：`v1.13.9`
- 基线 HEAD：`6ec5ff7c67547637bbd55f528276422395e28c03`
- 方法版本：`MAIC-1.3`
- 审计对象：`01-Grok实施报告.md`及当前工作区实际差异
- 审计性质：独立只读审计；除本报告外未修改任何文件

## 1. 接管条件

- `01-Grok实施报告.md`末行是`写入状态：已停止`，Grok已经停止写入。
- 报告中的分支、HEAD、方法版本和工作包与任务契约一致。
- 审计前W0目录只有奇数报告`01`，不存在同编号偶数审计报告；本报告是首次`02`，未覆盖旧报告。
- 当前只有一个worktree；暂存区为空；`main`、`origin/main`、`release/1.0.0`仍分别为契约记录的保护值。
- 当前差异恰为W0允许的20个已跟踪文件，没有Swift差异；另有既存未跟踪工作包和`tools/`。本审计未读取、修改或纳入`tools/`。

## 2. 独立核查结果

### 已符合

- 宪法已升至v2.14.0，Sync Impact列出了四档、三行设置、取用复用、当前方式确认及下游同步范围；13.7锁生命周期条款仍保留。
- `spec.md`中的FR编号集合与基线完全一致：修改前后均为81个，无新增、删除或重复编号，未发生重排或复用。
- 宪法、FR、data-model、contracts、quickstart、research、身份验证方案及主要阶段路牌，对四档名称、出厂设备验证、取用验证默认开启、组合档取消不回落设备密码、管理类凭证例外和同详情复用边界的现行结论基本一致。
- `v1.13.9`路牌已改为“已创建、规格已回写、代码尚未改、尚未提交上传”，没有把W1以后工作写成已经完成。
- 独立检查全部已修改Markdown中的本地相对链接，目标路径均存在；本波新增行没有带锚点的链接。

### 阻塞问题

#### W0-B01：确定性格式闸门失败

`git diff --check`失败，共有6处新增行尾空白：

- `ApiRelay/specs/001-key-vault/follow-ups/identity-auth/spec.md:4`
- `ApiRelay/specs/playbooks/身份验证方案.md:4`
- `ApiRelay/specs/playbooks/身份验证方案.md:6`
- `ApiRelay/specs/playbooks/锁-加固计划.md:3`
- `ApiRelay/specs/playbooks/锁-加固计划.md:5`
- `ZL01_具体说明/14-项目当前状态.md:102`

即使其中部分双空格原意是Markdown强制换行，最终检查计划已经把`git diff --check`列为确定性闸门；闸门未通过时本波不能放行。整改只能消除这6处检查错误，并在新奇数报告中附完整复跑结果。

#### W0-B02：历史旧结论没有按W0规则紧邻标废

`ApiRelay/specs/001-key-vault/checklists/requirements.md:67`仍写“`CL-004`（应用专属主密码）本期明确不做”。虽然同文件后文记录了后来决定“做”，但这条旧结论旁边没有明确的“已被13.9覆盖”标记和唯一现行链接。

这不满足最终执行计划W0停止点：“历史完成项可以保留原文，但必须紧邻明确覆盖标记和唯一现行链接”。读者从该段进入仍可能得到与FR-036～038相反的答案。整改应保留历史事实，同时在该句紧邻位置明确标废并链接现行身份验证规格；不得删除历史或另建第二套答案。

#### W0-B03：新增覆盖说明截断了Markdown表格

`ApiRelay/specs/001-key-vault/checklists/requirements.md:86`新增的引用说明位于“第三次修订”表格的倒数两行之间。CommonMark表格在引用块前已经结束，后面的“上架时机”行不再属于原表格，导致文档结构和阅读结果被破坏。

整改应让覆盖说明不拆断表格，同时继续紧邻“应用主密码”历史行表达覆盖关系；不得借此改动其他历史内容。

## 3. 非阻塞建议

- 正式四档名称已经统一为“应用密码”，但部分历史标题和解释仍使用“应用专属主密码/主密码”。历史术语可以保留；后续写新现行文案时应只用“应用密码”，避免再次形成名称漂移。本项不要求在本轮扩大整改范围。

## 4. 未验证项

- W0不改业务代码，本轮没有运行Xcode测试、真机验证或CloudKit验证；这些属于W1—W6，不能据此声称运行时身份验证已经实现。
- 本轮验证了本地链接目标存在，但没有做完整Markdown视觉渲染；已通过源码结构确定发现W0-B03。
- `BRANCHES.md`、`ZL01/10`和`ZL01/13`不在W0允许清单，本轮未要求清除其中历史路牌；它们仍按最终计划留到W6处理。

## 5. 方法观察

- 逐波“Grok实施、Codex独立审计”有效发现了实施报告未披露的`git diff --check`失败，以及关键词数量检查无法识别的历史语境和表格结构问题。
- 下一轮只需整改上述三个W0阻塞并生成新的奇数报告，不需要重新通读或改写已经一致的全部规格，也不得提前进入W1；这样既保持质量，又避免重复消耗。

波次审计不通过，只能整改本波阻塞。
