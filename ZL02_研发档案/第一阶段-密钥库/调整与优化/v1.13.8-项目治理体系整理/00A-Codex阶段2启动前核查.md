# Codex 阶段2启动前核查

> 核查日期：2026-09-10  
> 结论：修正下列保护后，允许启动阶段2；本文件是已知现状和检查线索，不代替Grok的全仓实施前检查。

## 一、已确认的安全基线

- 当前分支为 `v1.13.8`。
- 本地HEAD与本地记录的`origin/v1.13.8`均为`94fefc3ae5a9ff6c22b9db81ef182c2d2af2a97a`。
- 工作区中有两类预期未跟踪内容：本治理工作包，以及原来就存在的`tools/`。
- 旧的ZL00工作包路径已经删除，仓库中没有旧路径文字引用。
- 没有修改任何已跟踪文件，没有提交或上传。

注意：上述远程结论只表示本地保存的远程跟踪指针。阶段2禁止`fetch`、`pull`或其他会改变Git引用的网络操作；真正远程状态留到获得上传授权后的阶段处理。

## 二、已经完成的流程结构检查

- 阶段2至阶段9共八份阶段提示词全部存在。
- 另有提示词索引和“只检查当前阶段”辅助提示词。
- 阶段2的输入已经存在，输出路径唯一。
- 阶段3至阶段9的输入均由前一阶段产生。
- 阶段6是条件阶段：阶段5无阻塞时跳过；有阻塞时必须执行。
- 阶段4和阶段8分别保留“用户批准最终计划”和“用户授权上传”两个不可自动跨越的关口。

## 三、Grok阶段2必须重点核实的已知风险

1. `.cursor/rules/`当前有10份规则，其中9份标记为始终生效。它们是本次治理检查对象；若与阶段2的只读范围冲突，只能记录冲突，不能因此修改项目。
2. `ZL01_具体说明/09-上传流程.md`当前规定上传前后要同步AI对话记录、`BRANCHES.md`和上传记录；阶段8草稿要求最终验收后不再改变治理内容并尽量一次提交。二者是否能同时真实完成必须在阶段2查清，并由阶段3的最终计划明确解决，禁止在上传时临时决定或伪造记录。
3. 当前仍有多处把`v1.13.8`和身份验证联系在一起。必须区分历史事实、当前治理整理、未来`v1.13.9`工作和含义不清项；本工作包自身用于说明“顺延”的文字不是遗漏路牌。
4. ZL02现有事故仍位于旧的顶层“事故复盘”结构；未来移动前必须查全引用。本工作包已经位于目标阶段结构中，不得在重组时再次移动。
5. 工作包目前未被Git跟踪，普通`git diff`不能发现它被误改。阶段2开始前应记录本工作包已有文件的内容摘要，结束后确认除`03-Grok实施前检查报告.md`外均未变化；不得读取或计算`tools/`内容摘要。

## 四、资料分流基准

- ZL00：长期治理方法论，不放具体工作包。
- `ApiRelay/specs/`：通过Spec Kit开展的完整工作过程。
- ZL02：非Spec Kit调整、优化和事故的完整工作过程。
- ZL01：中文项目状态、通俗说明、AI对话索引和演进记录；不承担完整技术工作包。

本次工作包属于实际使用后产生的非Spec Kit治理调整，因此固定保留在ZL02。
