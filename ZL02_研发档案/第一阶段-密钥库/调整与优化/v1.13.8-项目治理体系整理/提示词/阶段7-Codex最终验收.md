# 阶段 7：发给 Codex（最终只读验收）

```text
请执行以下 ZL02 工作包的“阶段 7：最终只读验收”：

ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-项目治理体系整理/00-总流程操作台.md

若 `09-Codex最终验收报告.md` 已存在，说明本轮阶段 7 已执行；立即停止，不覆盖旧报告。若旧报告不通过，应先报告需要另开一轮带编号的整改/复验，不得自行发明文件名或覆盖历史。

确认仓库根目录、当前 `v1.13.8` 分支以及 `04`—`08` 中实际存在文件属于同一轮。读取 `04-最终执行计划.md`、`05-最终检查计划.md`、`06-Grok本地实施报告.md`、`07-Codex独立审计报告.md`；若 `07` 有阻塞，还必须读取 `08-Grok整改报告.md`，否则 08 可以不存在。

依据当前实际差异完整重跑 `05` 的最终通过条件，不依赖上一轮结论。必须确认：治理职责和冲突裁决闭环、Spec Kit/ZL02 双向反馈、身份验证版本顺延、ZL01 中文可读性和唯一当前状态、ZL02 阶段与事故分类、AI 记录真实性、链接/路径/锚点、提示词链和防漂移机制全部成立。

必须确认差异严格属于 `04` 第 4 节；业务代码、constitution/ROADMAP 实质、`.gitignore`、历史、main、tag、其他冻结引用均未改变；`tools/` 仍未跟踪且只从 Git 状态检查。本工作包固定路径和阶段 2—9 提示词必须完整。

必须再次只读演练阶段 8：提交 A 的显式路径集合等于最终验收范围，不使用 `git add .`、`git add -A` 或宽泛通配；A 成功上传后，B 只改 ZL01/13、ZL01/10、ZL01/14、BRANCHES，且 B 豁免递归记账；B 上传后 `v1` 快进到 B，main/tag 不动。确认 ZL01/14只写 A 已确认上传及后续需实时核对，不预填 B、`v1` 或阶段 9成功。任一步失败都有停止点，不需要临时发明第三次提交或提前伪造事实。

还必须确认：工作包当前阶段能从实际产物/Git状态推导，提示词索引不再手写当前进度；上传完整正文只存在 ZL01/09，`热修-save.md` 和 Cursor 规则只是路由或不重叠补充；ZL01/10不再保存第二套现行路线。

本阶段只读；唯一允许新增或修改：

ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-项目治理体系整理/09-Codex最终验收报告.md

报告记录阶段 7、时间、分支、起始 HEAD、输入文件、逐项证据，并且结尾只能给出以下结论之一：

1. `验收通过，可以在用户明确授权后提交并上传 v1.13.8。`
2. `验收不通过，必须退回阶段 6 修复以下阻塞问题。`

不得修复、暂存、提交、上传、切换分支或创建 `v1.13.9`。完成后立即停止。验收通过本身不等于用户授权上传。
```
