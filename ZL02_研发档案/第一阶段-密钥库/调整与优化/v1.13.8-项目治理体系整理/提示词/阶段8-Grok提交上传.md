# 阶段 8：用户明确授权后发给 Grok（提交和上传）

```text
用户现已明确授权提交并上传 `v1.13.8`。请执行以下 ZL02 工作包的“阶段 8：提交和上传”：

ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-项目治理体系整理/00-总流程操作台.md

先确认仓库根目录正确，并完整阅读 `04-最终执行计划.md`、`05-最终检查计划.md`、`09-Codex最终验收报告.md`，以及实际存在的后续整改/复验文件。若 `09` 已通过，只有其结尾逐字写明“验收通过，可以在用户明确授权后提交并上传 v1.13.8。”才可继续；若 `09` 不通过，则必须存在同一轮的 `10-Codex第二轮整改定稿.md`、`11-Grok第二轮整改报告.md`、`12-Codex第二轮复验报告.md`，且 `12` 结尾逐字写明“第二轮复验通过，可以在用户明确授权后提交并上传 v1.13.8。”才可继续。任何旧报告不得覆盖，最新验收未通过时立即停止。

上传通用顺序唯一正文是整改后的 `ZL01_具体说明/09-上传流程.md` §1；`热修-save.md` 和 Cursor 规则只负责路由或分支边界。若它们仍保存冲突的第二套 A/B正文，立即停止，不得上传。

开始前确认：当前分支是 `v1.13.8`；没有已暂存的计划外内容；main 和 `release/1.0.0` 等于 `04` 记录的基线；本工作包仍在固定路径；业务代码、`.gitignore` 和 `tools/` 不在验收差异中；不创建 `v1.13.9`；不重写历史、不 force push。

如果 ZL01/13、BRANCHES 和实际远端已经显示本治理整理完成过 A/B上传，立即停止，不得重复创建上传记录或重复提交。状态不一致时也停止并报告，不能猜测重跑。

严格执行以下固定两次提交，禁止改成一次或增加第三次：

一、提交 A（内容提交）

1. 从实际差异逐项生成显式路径清单，并与 `04` 第 4、5 节及最新有效验收报告范围逐项比对。提交 A 应包含全部验收通过的治理整理、本工作包既有文件与实际产生的阶段报告；若走过第二轮，必须包含`10`、`11`、`12`。不存在的可选产物不得伪造。
2. 必须逐个显式路径暂存，禁止 `git add .`、`git add -A`、目录根通配或任何可能纳入 `tools/` 的方式。
3. 暂存后检查文件名和完整暂存差异。任何路径不在批准范围、业务代码混入、`.gitignore` 改变、`tools/` 被暂存，立即取消本阶段并报告。
4. 创建一次内容明确的提交 A，并上传到 `origin/v1.13.8`。不得预先把“上传成功”、远端一致或未知 hash 写入文件。
5. 如果 A 提交或上传失败，停止；不得开始 B，不得移动 `v1`。

二、提交 B（上传记账尾单）

只有确认 A 已成功存在于 `origin/v1.13.8` 后才继续。此时只允许修改并显式暂存以下四个文件：

- `ZL01_具体说明/13-上传记录.md`：追加这次实际上传记录，不写 hash；
- `ZL01_具体说明/10-AI对话相关记录.md`：只记录此时已经确认的“内容 A 已上传”；不得提前把 B、`v1` 或阶段 9标为完成；
- `ZL01_具体说明/14-项目当前状态.md`：把“本地未上传”改为“A 已确认上传；B、`v1` 和远端最终一致性以实时 Git/阶段 9核对为准”，不得提前写 B或阶段 9成功；
- `ApiRelay/specs/playbooks/BRANCHES.md`：按已批准惯例把 `v1.13.8` 的内容 tip 指向提交 A。

不得再改 ZL00、身份验证语义、其他状态页、提示词或任何计划外文件。提交 B 是这次上传交易的记账尾单，按最终制度豁免再次为 B 生成上传记录；因此不得创建第三次“记录 B 的记录”提交。

检查 B 的暂存差异严格只有上述四文件后，创建提交 B 并上传到 `origin/v1.13.8`。失败则停止并准确报告已成功和未成功的状态，不 force push。

三、更新稳定线 `v1`

只有 B 成功上传后，才按最终计划把本地 `v1` 快进到 B 并上传 `origin/v1`，再回到 `v1.13.8`。禁止合并提交、禁止改 main、禁止改/建 tag、禁止创建 `v1.13.9`。若不能纯快进，停止并报告，不能自行解决分叉。

完成后在对话中报告：A/B 完整 hash、各提交文件、上传的 `v1.13.8` 和 `v1`、本地与远端指向、main/tag 基线、当前分支、工作区剩余内容，以及 `tools/` 是否仍为未跟踪且未纳入。报告后立即停止，不进入阶段 9。
```
