# 通用：只检查当前进行到哪一步（ZL02工作包）

```text
请只读检查以下工作包当前进行到哪个阶段：

ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-项目治理体系整理/00-总流程操作台.md

先确认当前仓库根目录同时包含`.git/`、`ApiRelay/`、`ZL01_具体说明/`和`ZL02_研发档案/`；若不是，停止并说明，不要猜测路径。

请阅读总流程、提示词索引和当前已存在的阶段产物，并核对实际 Git 状态。回答：

1. 已经完成到哪个阶段；
2. 上一阶段是否满足完成条件；
3. 下一阶段编号及负责人；
4. 下一步应该使用哪个提示词文件；
5. 是否存在阻塞、越界修改或缺失的前置文件。
6. 已有阶段产物记录的分支、起始HEAD和输入文件是否属于同一条工作链，是否可能是旧文件。

不要相信操作台或索引里可能残留的手写“当前进度”，按证据依次判断：缺 `03`→阶段2；有 `03`但最终计划缺失/被审计退回且未修订→阶段3；缺 `06`→阶段4；缺 `07`→阶段5；`07`有阻塞且修订计划仍待用户确认→停在确认关口；修订已确认且缺有效`08`→阶段6；缺有效`09`→阶段7；`09`不通过且缺`10`→阶段7A；有`10`但缺`11`→等待用户确认后阶段7B；有`11`但缺`12`→阶段7C；最新有效验收报告（`09`或`12`）通过但尚无实际A/B提交→等待用户授权阶段8。A/B与`v1`已上传后，直接按阶段9提示词重新核对实际本地/远端引用；阶段9是无副作用、可重复的状态核对，不依赖聊天中的“上传报告”或另一个完成标记。文件存在但元数据不匹配时不得算完成。

本指令只允许判断状态。不要新建或修改文件，不要执行下一阶段，不要暂存、提交或上传，不要切换分支，不要处理 tools/。即使判断下一阶段可以开始，也只能报告，不能自动开始。
```
