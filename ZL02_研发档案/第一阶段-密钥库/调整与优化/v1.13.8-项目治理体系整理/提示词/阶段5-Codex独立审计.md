# 阶段 5：发给 Codex（独立审计）

```text
请执行以下 ZL02 工作包的“阶段 5：独立审计”：

ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-项目治理体系整理/00-总流程操作台.md

若 `07-Codex独立审计报告.md` 已存在，说明本轮阶段 5 已完成；立即停止，不覆盖旧报告。需要复查整改时应执行阶段 7，不重复阶段 5。

先确认仓库根目录和当前分支正确，并核对 `04-最终执行计划.md`、`05-最终检查计划.md`、`06-Grok本地实施报告.md` 的分支、起始 HEAD 和输入关系属于同一轮。陈旧、不匹配或缺失时不得审计通过。

完整阅读上述三份文件，再以实际工作区差异为准逐项执行 `05`，不得把 Grok 自检当作证据。本阶段只读；唯一允许新增或修改：

ZL02_研发档案/第一阶段-密钥库/调整与优化/v1.13.8-项目治理体系整理/07-Codex独立审计报告.md

必须重点核查：

1. 实际差异是否严格落在 `04` 第 4 节清单；业务代码、constitution/ROADMAP 实质、`.gitignore`、Git 历史、main、tag 和其他冻结引用均未变；`tools/` 只从 Git 状态确认仍未跟踪，不读取内容。
2. ZL00 是唯一治理方法权威但不取代产品权威；specs、ZL02、ZL01、AI 对话的职责没有交叉复制。
3. “Spec Kit 计划被实际使用推翻”时存在可执行的裁决、用户批准和正式权威回写闭环，不会长期各说各话。
4. 全局核查身份验证路牌：历史事实保留，未来实施全指向计划中且尚未创建的 `v1.13.9`，四档语义未改变。
5. ZL01 对非技术用户可读；当前状态只有一个维护点；AI 记录没有虚构 composerId/UUID。
6. ZL02 真正按阶段、调整、事故独立文件夹组织；事故 10 个文件无丢失或重复；本工作包路径未改变。
7. 所有链接、路径、锚点、规则入口和阶段 2—9 提示词完整、自洽。
8. 状态漂移防线真实可执行，而不是新增一套重复说明。

还必须只读演练阶段 8：提交 A 使用 `04` 的显式路径范围；A 成功上传后，提交 B 只改 ZL01/13、ZL01/10、ZL01/14、BRANCHES；B 是豁免递归记账的尾单；随后 `v1` 快进到 B。确认 ZL01/14不会在上传后仍写“未上传”，无需临场决定、不会预填 B/`v1`/阶段 9结果、不使用宽泛暂存、不纳入 `tools/`，且 main/tag 不动。不得实际暂存、提交、上传或切换分支。

报告逐项写明通过、阻塞、非阻塞建议和无法验证项，结尾只能写以下一句之一：

1. `存在阻塞问题，必须进入阶段 6 修正。`
2. `没有阻塞问题，跳过阶段 6，可以直接进入阶段 7 最终验收。`

完成报告后立即停止，不直接修复。
```
