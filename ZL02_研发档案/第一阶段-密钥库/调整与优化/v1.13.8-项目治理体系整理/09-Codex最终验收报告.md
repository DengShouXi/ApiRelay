# v1.13.8 项目治理体系整理：Codex 最终验收报告

## 0. 本报告身份

- 阶段：7（Codex 最终只读验收）
- 生成时间：2026-09-10 23:07:52 CST
- 当前分支：`v1.13.8`
- 起始/当前 HEAD：`94fefc3ae5a9ff6c22b9db81ef182c2d2af2a97a`
- 输入：`04-最终执行计划.md`、`05-最终检查计划.md`、`06-Grok本地实施报告.md`、`07-Codex独立审计报告.md`、`08-Grok整改报告.md`
- 检查方式：重新读取现行文件并以实际工作区差异、Git 引用、链接目标和阶段提示词为证据；未用 `08` 的自检结论代替验收
- 本阶段动作：除本报告外未修改项目内容；未暂存、提交、上传、切换分支或创建 `v1.13.9`
- 结论：**验收不通过，不得进入阶段 8。**

## 1. 总体结论

阶段 5 的 B1—B3 已基本整改到位：提交 B 已固定为四文件；完整上传正文只保留在 ZL01/09；工作包操作台和提示词索引不再分别手写当前阶段。ZL01/10 也已把“当前正在做什么”改为链接 ZL01/14，保留的两张表属于对话子任务索引和建议顺序，不作为当前状态权威。

治理分层、冲突裁决、Spec Kit 与 ZL02 双向反馈、事故归档、Git 保护和大部分身份验证路牌均符合最终计划。但是独立复跑 `05` 后仍发现三个会造成状态漂移或版本误读的阻塞问题：唯一状态页会在本报告生成后立刻过期；BRANCHES 仍保存第二份动态“下一步”；锁验收矩阵仍有一条把现行验证行为标为 13.8；阶段 9 的完成状态没有可持久核验的来源，且操作台与提示词互相矛盾。

因此当前不能确认“整理后的体系不会再次产生状态漂移”，也不能批准上传。

## 2. 已通过项目

### 2.1 同一工作链与安全基线：通过

- `04`—`08` 的分支、起始 HEAD 和输入关系属于同一轮；`04`、`05` 带 2026-09-10 22:01:29 CST 审计修订标识。
- 当前仍在 `v1.13.8`，HEAD 仍为 `94fefc3ae5a9ff6c22b9db81ef182c2d2af2a97a`，没有已暂存内容，也没有新增提交。
- `main` 仍为 `539447ea9984608c74989da48b8770cb3fe32a6c`；`v1` 仍为 `94fefc3ae5a9ff6c22b9db81ef182c2d2af2a97a`；`release/1.0.0` 仍为 `5c5eed3f8390f51f5a26be9737bcc11f72e7d7fd`。
- 本地与本地保存的远程跟踪引用均不存在 `v1.13.9`。
- App 源码、测试、Xcode 工程、`.gitignore`、constitution 和 ROADMAP 均无差异。
- `tools/` 仍未跟踪、未暂存；本验收没有读取或处理其内容。

### 2.2 治理职责、冲突裁决与双向反馈：通过

- ZL00 四文件分别保存总纲、职责边界、冲突裁决和关账闸门，没有承载本次工作包或当前状态。
- 用户、治理权、产品内容权、Spec Kit、ZL02、ZL01 和 AI 对话的职责边界清楚；根 `AGENTS.md` 与 `project-governance.mdc` 只做路由。
- 冲突机制包含停止、分类、证据、方案、批准、权威回写、ZL01 更新和一致性检查；战略、版本、安全、发布和不可逆操作必须由用户批准。
- Spec Kit 可以因实施反馈修改原规格，ZL02 实践也可以改变主体结构；两种情况都要求关账前回写真正的产品权威并消除两个现行答案。

### 2.3 ZL01 与 AI 记录：部分通过

- `00-从这里开始.md` 与 `14-项目当前状态.md` 使用中文，能让非技术读者理解治理整理、身份验证顺延、尚未上传和受保护事项。
- ZL01/10 的新增 Codex/Grok 记录使用模型、日期和工作包链接，没有为这些记录虚构 Cursor UUID；现有 Cursor 深链得到保留。
- ZL01/09 已成为上传完整正文的唯一位置；`热修-save.md` 只保留分支选择和禁止项，两个 Cursor 规则只触发并路由。
- 唯一当前状态的持续正确性未通过，见阻塞 F1。

### 2.4 ZL02 分类与事故移动：通过

- 存在 ZL02 总览和第一阶段总览；没有创建空的第二、第三阶段目录。
- 治理工作包仍位于第一阶段“调整与优化”的固定路径。
- 第一次事故只存在于第一阶段“事故复盘”，旧目录已不存在；10 个文件全部存在。
- 其中 9 个文件与移动前内容散列一致；`01-事故复盘.md` 只把 ZL01 相对链接多退一级，没有改写事故结论。

### 2.5 链接、路径、锚点与规则清单：通过

- 对本轮 37 个新增/修改 Markdown/规则文件复查相对链接，没有发现本轮真实链接目标缺失。
- 检查器命中的 `uuid`、`{入口文件名}`、`../specs/spec.md` 和 `#usage` 位于 ZL01/11、12 的格式示例，是原有模板占位，不是本轮真实断链。
- 事故旧路径只剩在初稿、检查报告和实施报告中作为历史移动记录，没有现行入口继续指向旧目录。
- `.cursor/rules/` 实际有 11 份规则，包含 `implementation-closeout.mdc`；`.cursor/commands/closeout.md` 实际存在。

### 2.6 差异范围：通过

- 已跟踪差异均属于 `04` 第 4 节列出的治理、ZL01、路牌、事故移动范围；未跟踪的新文件属于 ZL00、ZL01/14、ZL02 总览、固定工作包、事故新位置和阶段报告。
- 没有业务代码、历史、main、tag、`.gitignore` 或保护外产品文件混入。
- `git diff --check` 仅报告 Markdown 有意的行尾双空格换行，没有冲突标记或补丁残留；列为非阻塞格式现象。

### 2.7 阶段 8 只读演练：结构通过

- 提交 A 可从实际差异、`04` 第 4—5 节和本报告验收范围生成逐文件显式路径集合；不需要 `git add .`、`git add -A` 或宽泛通配，也不包含业务代码、`.gitignore` 或 `tools/`。
- A 成功上传后，提交 B 固定只改 ZL01/13、ZL01/10、ZL01/14、BRANCHES 四个文件；B 是记账尾单并豁免递归记账，不产生第三次“记录 B”的提交。
- B 成功上传后才允许把 `v1` 纯快进到 B；main、release tag 和其他分支不动，不 force push，不创建 `v1.13.9`。
- A、B 或 `v1` 任一步失败均有停止点，不要求伪造尚未发生的 hash、上传或远端一致事实。
- 由于防漂移与阶段链仍有阻塞，本节的“结构通过”不构成上传许可。

## 3. 阻塞问题

### F1：唯一当前状态页会在阶段 7 产物生成后立即过期，BRANCHES 又保存了第二份动态下一步

证据：

- `ZL01_具体说明/14-项目当前状态.md` 第 39、47 行仍写“下一步是 Codex 做阶段 7 最终验收”。本报告一旦生成，这两句立即不再是真实下一步；若本报告不通过，应回到整改；若通过，应等待用户授权阶段 8。
- 同页第 97 行又规定“当前分支、正在做的事、下一步、是否已上传只改本页”。阶段 7 提示词却只允许生成本报告，不能同步该页。
- `ApiRelay/specs/playbooks/BRANCHES.md` 第 91 行另存一份动态“下一步：本线先走治理审计/验收……”，与“动态状态只在 14”的规则冲突，也会随阶段推进而过期。

影响：无需等到上传，执行阶段 7 本身就重新制造状态漂移；用户从唯一中文状态入口看不到真实下一步。提交 B 虽能在以后改 14 和 BRANCHES，但无法修复阶段 7 到用户授权上传之间的错误状态。

必须修复：不要让 ZL01/14 或 BRANCHES 手写需要每个工作包阶段同步的精确阶段。ZL01/14 可保留稳定的“本治理包尚未关账/上传须另授权”，具体阶段与下一提示词只路由到工作包的证据推导入口；BRANCHES 只维护分支 tip、用途和稳定边界，不维护动态下一步。若选择在阶段 7 同步 14，则必须先修订阶段 7 的允许文件范围和后续 A 范围，不能临场越权。

### F2：锁验收矩阵仍保留一条含义不清的 `v1.13.8` 身份验证实施路牌

证据：

- `ApiRelay/specs/playbooks/锁-验收矩阵.md` 第 5 行已正确说明：身份验证只是在 `v1.13.8` 定稿，代码计划在尚未创建的 `v1.13.9`。
- 但同一现行验收表第 56 行仍写“关锁 / 改为更弱档：用当前验证方式确认（13.8）”。这不是过去时，也没有说明“13.8 仅为定稿版本”，普通读者会把它理解成 13.8 包应具备的新行为。

影响：同一文件同时给出“13.9 才有代码”和“当前验证方式（13.8）”两个版本信号，不满足 `05` 要求的历史与未来清楚分开，也可能让后续 TestFlight 验收拿错包。

必须修复：把第 56 行改成版本中性的产品行为，或明确标注该行为要在计划中的 `v1.13.9` 实现/验收；不得改变已经定稿的四档语义。

### F3：阶段 9 没有可持久识别的完成来源，操作台、通用判断提示词和阶段 9 提示词互相矛盾

证据：

- `00-总流程操作台.md` 第 39 行规定阶段 9 的唯一产物是“对话内复核结论”，不产生工作包文件。
- 同一操作台第 71 行却规定完成与否看“阶段 9 报告和实际 Git 引用”；现行阶段 9 提示词也没有创建这份报告。
- `提示词/通用-只检查当前阶段.md` 第 19 行只能根据“已有 A/B 上传报告”进入阶段 9，但阶段 8 同样只在对话中报告；更重要的是，阶段 9 完成前后 Git 引用完全相同，之后没有任何工作包产物可区分“待复核”和“已复核”。

影响：换一个新对话或模型后，固定文件夹无法证明阶段 9 是否执行过；通用提示词可能反复要求阶段 9，或在没有持久验收结论时误判已经关账。这违背“无需复制旧聊天、所有阶段靠固定工作包继续”的目标。

必须修复：先统一选择一种可长期验证的关账来源。若以实际 Git/远端引用作为唯一关账证据，就应把阶段 9 设计为可重复、无副作用的确定性检查，并统一删除不存在的“阶段 9 报告/上传报告”依赖，明确通用检查如何从引用得出结论；若必须保存阶段 9 报告，则要先解决报告如何进入工作包且不违反“两次提交、没有第三次提交、关账后工作区干净”的既定边界。该选择会影响最终计划和提示词，不能由阶段 8 临场决定。

## 4. 其他检查结论

### 4.1 身份验证路牌总体情况

除 F2 外，现行入口已经把“2026-09-10 在 `v1.13.8` 讨论并定稿”写成历史事实，把 W0、Swift 和相关验收写成计划在尚未创建的 `v1.13.9`；没有把 `v1.13.9` 写成已创建或已完成，也没有修改四档产品语义。

### 4.2 B1—B4 整改复核

| 原阻塞 | 结果 | 说明 |
| --- | --- | --- |
| B1：B 漏掉 ZL01/14 | 通过 | 04、05、09、阶段 4—9 提示词均统一为四文件 B |
| B2：工作包多处手写当前阶段 | 通过 | 操作台改为按阶段产物与 Git 推导；提示词索引不再写当前进度 |
| B3：上传正文重复 | 通过 | 完整 A/B 正文只在 ZL01/09；热修 save 与 Cursor 规则只路由或保存不重叠边界 |
| B4：ZL01/10 充当第二状态页 | 通过 | “现行路线”结论改为链接 14；任务表仍作为对话/欠项索引，符合 ZL01/11 既有格式职责 |

### 4.3 非阻塞事项

- `04`、`05` 文首仍写“等待用户确认”，属于生成计划时的历史元数据；本轮用户已通过执行阶段 6 明确确认修订版。
- `06`、`07` 中的旧“三文件 B”属于当时报告，不是现行规则；现行计划、检查和阶段提示词已经统一为四文件。
- Markdown 行尾双空格用于强制换行，不建议在本轮扩大为全仓格式化。

## 5. 安全下一步

1. 不执行阶段 8，不提交、不上传、不创建 `v1.13.9`。
2. 先修订 `04`、`05` 和受 F1、F3 影响的阶段提示词，明确状态与关账的唯一持久来源；若修改新的文件范围，需要用户确认。
3. 让 Grok 只修 F1—F3，生成一份新的、带编号的整改报告，不覆盖现有 `08`。
4. 再创建新的、带编号的复验报告；不得覆盖本报告。

## 6. 固定结论

验收不通过，必须退回阶段 6 修复以下阻塞问题。
