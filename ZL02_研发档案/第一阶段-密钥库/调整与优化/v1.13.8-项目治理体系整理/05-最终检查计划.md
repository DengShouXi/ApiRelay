# v1.13.8 项目治理体系整理：最终检查计划

## 0. 本计划身份

- 阶段：3（Codex 确定最终计划）
- 初版时间：2026-09-10 20:38:12 CST
- 审计修订时间：2026-09-10 22:01:29 CST
- 第二轮整改定稿时间：2026-09-10 23:21:12 CST
- 目标分支：`v1.13.8`
- 起始 HEAD：`94fefc3ae5a9ff6c22b9db81ef182c2d2af2a97a`
- 受保护引用基线：`main`=`539447ea9984608c74989da48b8770cb3fe32a6c`；`v1`=`94fefc3ae5a9ff6c22b9db81ef182c2d2af2a97a`；`release/1.0.0`=`5c5eed3f8390f51f5a26be9737bcc11f72e7d7fd`
- 输入：`01-执行计划-初稿.md`、`02-检查计划-初稿.md`、`03-Grok实施前检查报告.md`、`06-Grok本地实施报告.md`、`07-Codex独立审计报告.md`、`08-Grok整改报告.md`、`09-Codex最终验收报告.md`
- 状态：**第二轮整改定稿版，等待用户确认。确认后用于阶段 7B整改和阶段 7C独立复验。**

检查以实际文件和 Git 状态为证据，不以 Grok 或 Codex 的聊天总结代替。每项结论必须标为“通过、阻塞、非阻塞建议、无法验证”之一。

## 1. 基线与越界检查

1. 当前分支必须是 `v1.13.8`，阶段 4 开始时 HEAD 必须与最终计划基线一致。
2. 阶段 4—7C 不得出现提交、上传、历史重写或分支/tag 指针变化。
3. 差异路径必须全部属于 `04-最终执行计划.md` 的新增、修改、移动清单或规定的阶段报告。
4. App 源码、测试、Xcode 工程、constitution/ROADMAP 实质内容、main、release tag、其他冻结分支均不得改变。
5. `.gitignore` 不得修改；`tools/` 仍为未跟踪目录，只通过 Git 状态确认，不读取其内容。
6. 本工作包仍位于固定 ZL02 路径，阶段 2—9及第二轮阶段 7A/7B/7C提示词均存在。

任何越界路径、意外 Git 引用变化或 `tools/` 被纳入暂存都属于阻塞问题。

## 2. 身份验证路牌完整性

对仓库进行全局检索，逐条人工分类所有同时涉及“身份验证”和 `v1.13.8` 的表述：

- 真实历史：允许保留，但必须使用过去时，不能让读者误以为仍在 13.8 实施。
- 当前/未来实施：必须指向“计划中的、尚未创建的 `v1.13.9`”。
- 本治理工作包中的顺延说明：允许保留。

重点复核最终计划列出的 identity follow-up、主 spec 六个入口/注释、身份验证 playbook、BRANCHES、重要说明索引、锁计划/矩阵、P03、P06、P13 TestFlight 和 ZL01 当前状态。还要检索中文近义词，如“面容/触控/系统密码/主密码/四档/认证/验证”。

通过标准：不存在把未来身份验证实施、验收或当前工作继续标成 `v1.13.8` 的路牌；不存在把 `v1.13.9` 写成已经创建或已经完成；四档语义和历史事实未被篡改。

第二轮必须单独检查 `ApiRelay/specs/playbooks/锁-验收矩阵.md` 第 5 与第 56 行：第 5 行保留“13.8 定稿、13.9落地”的区分；第 56 行不得再用“（13.8）”暗示新验证行为已经实现，应明确随计划中的、尚未创建的 13.9 实现/验收或使用不带版本歧义的产品表述。

## 3. 单一治理权威与冲突裁决

检查 ZL00 是否只保存稳定方法论，并回答以下问题：

- 用户、ZL00、constitution/ROADMAP/spec、ZL02、ZL01、AI 对话各自能决定什么？
- 同层冲突由什么记录解决，谁批准，结果回写到哪里？
- 涉及战略、版本、安全、发布、不可逆操作时是否必须用户批准？
- ZL00 与产品三层权威是否分属“治理权”和“产品内容权”，没有相互吞并？
- 根 AGENTS、Cursor rule、README 是否只链接 ZL00，没有复制第二套规则？

通过标准：面对“Spec Kit 说 A、ZL02 实践说 B”“ZL01 状态过期”“旧规则与新规则冲突”等案例，均能沿固定流程得到唯一当前答案；不能依赖模型自行猜测或简单采用最新文件。

## 4. Spec Kit 与 ZL02 双向反馈

分别模拟两种情况：

1. Spec Kit 实施时发现规格不适用；
2. App 使用后的 ZL02 调整需要改变主体结构。

检查制度是否要求：先在原工作来源记录证据，正式决定后回写真正的产品权威，再更新 ZL01；不得让 specs 与 ZL02 永久各自保留一个有效答案。检查 ZL02 也没有被误写成“永远不能改主体”。

通过标准：既允许实践改变原计划，也能在关账前消除双重答案。

## 5. ZL01 非技术可读性

让未掌握 Git、代码和英文框架的读者只从 `00-从这里开始.md` 与 `14-项目当前状态.md` 回答：

- 当前版本主要在做什么？
- 身份验证什么时候做？
- 当前是否已经上传/关账？
- 下一步该看哪里？
- 想了解方法、Spec Kit 工作或实际调整时分别去哪里？

检查中文是否先讲结论再给技术链接；是否避免在多页复制当前状态和动态数量；英文术语是否有中文解释。ZL01 可以索引 AI 对话，但不能承载完整技术工作包或伪造对话深链。

通过标准：上述问题能得到一致、清楚、无技术背景也可理解的答案。ZL01/14 可以告诉用户去固定工作包检查具体阶段，但不得手写“下一步是阶段 N”这种一生成报告就过期的内部进度。

## 6. ZL02 阶段与事故分类

检查：

- 存在 `ZL02_研发档案/00-研发档案总览.md` 和第一阶段总览；
- 没有为了占位创建空的第二、第三阶段；
- 治理整理工作包仍在第一阶段“调整与优化”中；
- 第一次事故完整位于第一阶段“事故复盘”中，原目录不残留重复副本；
- 事故 10 个原文件全部存在，内容除必要链接外未被改写；
- 后续每次事故有独立文件夹，而不是混在同一长文件中。

少文件、双份目录、错误分类或工作包被移到 ZL00 均为阻塞问题。

## 7. 链接、路径、锚点与 AI 记录

1. 检查所有 Markdown 相对链接目标存在，目录移动后的旧路径无现行引用。
2. 检查 ZL01、ZL02、specs、README、AGENTS 和 Cursor 路由之间的链接不形成循环权威。
3. 检查带锚点链接的标题仍能生成对应锚点；路径中的中文、空格和大小写正确。
4. 检查 ZL01/10 与 11：真实 Cursor 深链保留；无真实 `composerId` 的 Codex/Grok 记录使用来源、模型、日期和工作包链接，不伪造 UUID。
5. 检查 `closeout.md`、Cursor 规则清单和 ZL01 文件范围与实际目录一致。

断链、伪造对话标识或把摘要链接误当正式权威属于阻塞问题。

## 8. 状态漂移防复发

全局检查同一个“当前版本、当前工作、下一步、规则数量、上传状态”是否仍被多处手工维护。允许历史记录和入口链接重复，不允许多个文件各自声明当前真相。

通过标准：

- 动态项目状态只在 ZL01/14 维护；其他说明页链接它。
- 产品语义只在相应产品权威维护；ZL01/ZL02 不复制一整套。
- 治理方法只在 ZL00 维护；AGENTS/Cursor/README 只路由。
- 工作包关账闸门包含权威回写和 ZL01 更新检查。
- 文件中的数量要么自动从实际目录判断，要么明确核验日期和唯一维护点。
- 工作包当前阶段只按实际阶段产物和 Git 状态推导；操作台与提示词索引不得各写一份会过期的当前进度。
- BRANCHES 只维护分支 tip、用途、历史和稳定保护边界；不得再保存当前工作包的动态“下一步”。当前任务只链接 ZL01/14。
- 上传步骤完整正文只有 ZL01/09 一份；热修 playbook 与 Cursor 规则只保存各自不重叠的边界并链接 09。
- 阶段 9 关账以当前可重新核对的 Git/远端状态条件为准；不得依赖聊天上传报告或一份无法进入两次提交的事后报告。重复执行阶段 9 应得到同一事实结论且不改变仓库状态。

## 9. 上传流程演练（阶段 5、阶段 7和阶段 7C必查）

只做演练，不暂存、不提交、不上传：

1. 能否从最终计划得到提交 A 的显式路径集合，并证明不含业务代码、`.gitignore` 和 `tools/`。
2. A 前是否没有预填 hash、远端一致或“已经上传”的事实。
3. A 成功上传后，B 是否只允许修改 ZL01/13、ZL01/10、ZL01/14、BRANCHES 四个文件。
4. B 是否被明确定义为记账尾单并豁免递归记录，不会产生第三次提交。
5. B 后是否只把 `v1` 快进到 B；main、tag 和其他分支不动。
6. 任一步失败时，是否有明确停止点，且不 force push、不重写历史。

若仍需到阶段 8 才决定提交次数、记录方式或暂存范围，属于阻塞问题，不得验收通过。

## 10. 提示词链检查

逐份读取阶段 4—9及第二轮阶段 7A/7B/7C提示词，验证：

- 每阶段前置文件真实存在或会由紧邻前序阶段产生；
- 阶段角色和唯一输出文件正确；
- 阶段 6 可在无阻塞时跳过；
- 阶段 4—7C 均禁止提交上传；阶段 8 必须用户另行授权；阶段 9 只读；
- 阶段 8 固定采用 A/B 两次提交、显式暂存、更新 `v1`，且不创建 `v1.13.9`；
- 阶段 8 的 B 固定为四文件，并且 ZL01/14只写 A 已确认上传以及 B/`v1`须实时核对，不提前宣称阶段 9通过；
- 阶段 9独立核对两个提交、两个远端分支和受保护引用；
- `09`不通过时，提示词索引和通用判断会依次路由到7A→7B→7C；阶段8只接受最新有效通过报告，第二轮存在时不得拿旧`09`进入上传；
- 阶段9明确为可重复、无副作用的实时状态核对；操作台和通用提示词不再引用不存在的阶段9报告或聊天上传报告；
- 不存在让执行者去别处复制聊天内容或自行补全关键方案的要求。

## 11. 最终通过条件

阶段 7或阶段 7C只有同时满足以下条件才能写“验收通过”：

- 执行计划全部完成，差异无越界；
- 身份验证路牌无遗漏，历史与未来清楚分开；
- ZL00、specs、ZL02、ZL01 和 AI 对话职责唯一且可执行；
- 冲突裁决和双向反馈可闭环；
- ZL01 易读，ZL02 分类完整；
- 链接、路径、锚点和提示词链无阻塞；
- 状态漂移防线成立；
- A/B 上传流程无需临场设计；
- 业务代码、历史、main、tag、`.gitignore` 和 `tools/` 均未被误改。

第一轮任一项未通过，应列为阻塞并进入对应整改；第二轮阶段 7C 任一项未通过，应写入 `12` 并停止，不得自行再次扩充编号或直接修复。需要扩大最终计划文件范围时，必须重新取得用户批准。

第二轮阶段 7C 还必须逐项确认 `09` 的 F1—F3 已解决，且原 B1—B4没有回归。阶段 7C只允许新增`12-Codex第二轮复验报告.md`；任何其他修改均为阻塞。

## 12. 阶段 9 远程关账检查

阶段 9 使用不改变本地引用的远程查询独立确认。它是可安全重复的状态检查；不要求证明以前是否已经运行过，也不创建第三次提交或未提交报告：

- 提交 A 覆盖最终验收的内容范围；
- 提交 B 只有 ZL01/13、ZL01/10、ZL01/14、BRANCHES；
- 本地 `v1.13.8`、`origin/v1.13.8`、本地/远端 `v1` 均指向 B；
- main 与 release tag 仍为基线值，没有 force push 或历史重写；
- 工作区只剩原有未跟踪 `tools/`，或报告中已批准的其他明确内容；
- 本工作包、阶段 2—9提示词以及实际启用的第二轮7A/7B/7C提示词完整。

只有全部成立，才能宣布 `v1.13.8` 当前满足完全关账条件。以后重新执行阶段 9时仍按同一组实时条件判断；创建 `v1.13.9` 必须是以后单独授权的下一项工作。
