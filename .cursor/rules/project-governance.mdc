---
description: 项目治理入口。冲突、资料分类、双/多 AI 协作、关账和上传闸门先读 ZL00，不要在本文件复制规则。
alwaysApply: true
---

# 项目治理

治理方法只以 `ZL00_项目总控/` 为准：

- `ZL00_项目总控/00-项目治理总纲.md`
- `ZL00_项目总控/01-权威职责与边界.md`
- `ZL00_项目总控/02-冲突裁决与变更机制.md`
- `ZL00_项目总控/03-关账与发布闸门.md`
- `ZL00_项目总控/04-双AI协作与独立审计.md`

当前中文状态只读 `ZL01_具体说明/14-项目当前状态.md`。Spec Kit 过程在 `ApiRelay/specs/`，非 Spec Kit 调整在 `ZL02_研发档案/`。

本规则只指路，不另写一套最高规则。与本规则冲突时：停止受影响动作，按 ZL00 的冲突裁决处理，不得用其他 `.mdc` 扩大当前任务授权。

自动触发：用户表达两个或多个 AI / 模型 / 编辑器协作、交叉检查、角色接力或按额度分工时，MUST 先声明已启用 `ZL00_项目总控/04-双AI协作与独立审计.md`，再**在当前打开或绑定的仓库根内**只读定位已有工作包。仅协作意图时只运行只读检查器并给出下一提示词，不得读取并执行该阶段，不得创建产物或调用另一 AI。检查器给出的下一阶段不是执行授权；只有用户点名具体任务、具体阶段或现有提示词路径，才可继续。用户不必提供路径或旧对话，也不得沿用父任务、其他窗口、来源任务或其他项目的旧上下文，不得搜索当前项目根之外。只问 AI 接口数量、解释文件、改错字、询问上传流程或明确只要单 AI 改一处时，不得新建完整多 AI 包，也不得把“上传”当成上传授权。详细矩阵只在 `04`。
