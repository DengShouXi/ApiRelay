---
description: 用户要求更新/整理/补写「10」AI对话记录，或改对话排版/编号/状态时，必须按 11 逐步执行，不得凭记忆偷工。
globs: ZL01_具体说明/10-AI对话相关记录.md, ZL01_具体说明/11-AI对话相关格式要求.md
alwaysApply: true
---

# 更新 `10`（AI 对话记录）

用户只要说「更新 10」「整理 10」「按对话补进 10」等（**不必**再贴格式提示），MUST：

1. **先打开并通读** `ZL01_具体说明/11-AI对话相关格式要求.md` 的 **§〇「接到更新 10」→ §一 → §四对应清单**（记号查 §三；增删走 §五）。
2. **按 §四清单逐项勾完**再改 `10`；禁止只改一半（只加对话不加子任务、只改详情不同步「五」两张表、漏返回目录/大小分隔/`{stage}-tN`）。
3. **页内跳转验收只用 Cmd+Shift+V**（或大纲）；Cursor 标签「Preview」对 `#锚点` 已知坏，MUST NOT 在 Preview 下改锚点当「修文档」。
4. **改格式先改 `11` 再改 `10`**；冲突以 `11` 为准。
5. 细则只以 `11` 正文为准；`10` 文首引用与注意事项第二条与本规则同效，本规则不另写第二套排版。
