---
description: 改动范围纪律——防止大范围重写，确保每次修改可审查、可回滚
alwaysApply: true
---

# 改动范围纪律

**一次对话只改一件事。** 用户说「优化 X」时，只改 X 相关的文件，不顺手重构别的。

## 硬性规则

- 修改前 MUST 先读目标文件，不得凭猜测重写。
- 优先用**局部替换**而非整文件重写。重写整个文件仅限：文件确实作废、或用户明确要求。
- 单次改动 MUST 限定在**一个模块**内（`Business/Vault/`、`Business/Insights/` 等）。
  跨模块改动须先说明为什么无法避免。
- 发现顺路可优化的东西，**只报告不动手**。等用户决定。
- 删除代码前必须说明它现在被谁引用、删掉后谁受影响。

## 禁止

```text
❌ 「我把整个 Service 重写了一遍，顺便统一了命名风格」
❌ 「顺手把其他几个文件的类似问题也改了」
❌ 「重构了一下目录结构，更清晰了」
✅ 「只改了 KeyVaultService.copySecretToClipboard 这一个方法，
    另外发现 revealSecret 有类似问题，要不要一起改？」
```

## 规范类文件的改动

`ApiRelay/.specify/memory/constitution.md`、`ApiRelay/specs/**/spec.md`、
`ApiRelay/specs/ROADMAP.md` 是**决策记录**，不是可随手调整的实现细节。

- 修改它们 MUST 先说明「哪条决策变了、为什么、影响哪些下游文档」，等用户确认。
- 宪法修改 MUST 同步更新其 Sync Impact Report 与版本号。
- 需求编号（FR-xxx）**MUST NOT 重排或复用**，下游文档大量引用这些编号。
  废弃的需求标记为废弃，不删除编号。
